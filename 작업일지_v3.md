# erp_updater 작업일지 (v3.0.0 개발용 인수인계서)

## 목표
RPA 요금 수정기에서 **시작일/종료일 분리(기간 입력) 모드**를 지원합니다.
* 평소에는 8열(기존 단일 날짜) 모드로 동작하다가, 요금이 동일한 연속 기간을 일괄 수정할 때 체크박스를 키면 9열(시작일/종료일 분리) 모드로 동적 전환됩니다.
* 엑셀 파일을 불러올 때 열 개수 및 헤더를 분석하여 단일 날짜/기간 모드를 자동으로 판별하고 전환합니다.

---

## 1. 현재 완료된 작업 내용 (v3.0.0 진행 상황)

### [완료] excel_loader.py 수정
* `load_and_validate_fares` 함수가 `(valid_rows, is_period_excel)` 튜플을 반환하도록 시그니처를 변경했습니다.
* 엑셀 헤더에 '종료' 키워드가 있거나 9열 이상인 경우 `is_period_excel = True`로 자동 인식합니다.
* 기간 모드일 경우 시작일(0번)과 종료일(1번)을 각각 파싱하고 요금 정보는 한 칸씩 밀어서 안전하게 로드합니다.

### [완료] gui.py UI 및 레이아웃 동적 제어 추가
* **체크박스 추가:** 요금 입력표 툴바 우측에 `[ ] 시작일/종료일 분리 (기간 입력)` 체크박스를 연동했습니다 (`self.period_mode_var`).
* **동적 열 전환 구현 (`on_toggle_period_mode`):**
  * 토글 시 8열 ↔ 9열로 그리드를 재구성합니다.
  * 단일 → 기간 모드 전환 시 기존 날짜 데이터를 시작일/종료일 양쪽에 복사해 두고, 수식(`self.formulas`) 좌표를 1칸씩 밀어 갱신합니다.
* **엑셀 불러오기 자동 전환:** `import_excel_to_sheet`에서 로드된 엑셀 형식을 감지해 체크박스를 자동 토글하고 그리드 컬럼을 맞춤형으로 다시 로드합니다.
* **엑셀 저장 및 양식 다운로드 연동:** `export_sheet_to_excel` 및 `download_excel_template`에서 현재 활성화된 모드(8열 혹은 9열)에 맞게 엑셀 파일을 생성하도록 처리했습니다.
* **RPA 워커 루프 (`rpa_worker_loop`):**
  * 각 행의 시작일(`date`)과 종료일(`date_end`)을 추출해 ERP의 시작/종료 검색 창에 각각 입력하도록 변경했습니다.
  * 날짜 검색 안정화 판정 검증 방식을 범위형 검사(`dates_seen and all(norm_date <= d <= norm_date_end for d in dates_seen)`)로 통일했습니다. (단일 날짜 검색 시 기존 검증과 동일하게 작동)
  * 실행 결과 로그와 리포트에 기간 범위(예: `2026-10-12 ~ 2026-10-19`)로 이력이 남도록 포맷을 맞췄습니다.

---

## 2. 다음 AI가 수행해야 할 남은 작업 (TO-DO)

> **2026-06-02 갱신:** 아래 수식 모듈 동적화(1~3번)와 버전 상향(4번 코드부)을 모두 반영했습니다. `python -m py_compile gui.py excel_loader.py packaging/setup_installer.py` 통과. **남은 작업은 인스톨러 빌드/스모크 테스트뿐입니다.**

### [x] 1. gui.py 수식 이름 매핑 함수 동적화
* **대상:** `_name_to_col(name, cur_col)` → `@staticmethod`를 인스턴스 메서드로 전환. `offset = 1 if self.period_mode_var.get() else 0`을 요금 열 상수(`COL_ADULT_AIR` 등)에 더해 실제 열을 계산. `시작일/출발일`→0, `종료일/도착일`→1(기간 모드) 매핑 추가. A~I 열 문자는 모드 무관 절대 매핑.
* **대상:** `_ref_name_for_col(c)` → 인스턴스 메서드로 전환, `self.sheet.headers()[c]`를 안전하게 반환하도록 교체.

### [x] 2. gui.py 수식 입력줄 및 툴바 관련 체크 보정
* **대상:** `_commit_formula_bar` → `c != COL_DATE`를 `not self.is_date_col(c)`로 교체 (시작일/종료일 모두 수식 등록 차단).
* **대상:** `_apply_formula_to_column` → 상태 메시지 `SHORT_COL_NAMES[col]`를 `self.sheet.headers()[col]`(예외 시 `'{col}열'` 폴백)로 교체.
* **추가:** `_eval_cell` 리졸버의 `target == COL_DATE` 체크도 `self.is_date_col(target)`로 보정 (기간 모드 종료일 열 참조 차단).

### [x] 3. gui.py의 grid helper constants 의존성 제거
* `_sync_formulas_from_cells`, `_recalc_formulas`는 인수인계 시점 기준 이미 `self.get_fare_cols()` / `num_cols = 9 if is_period else 8`로 반영되어 있었음(확인 완료, 추가 수정 없음).

### [x] 4-a. 버전 상향 (코드)
* 세 파일 모두 `v3.0.0`으로 상향 완료:
  1. `gui.py` → `APP_VERSION = "v3.0.0"`
  2. `packaging/setup_installer.py` → `APP_VERSION = "v3.0.0"`
  3. `build_setup.ps1` → `$Version = "v3.0.0"`

### [x] 4-b. 테스트 인스톨러 빌드 (2026-06-02 완료)
* `build_setup.ps1` 실행 성공(exit 0). 산출물:
  * `release/NaeilERPUpdater_Setup_v3.0.0.exe` (약 64.5 MB)
  * `release/NaeilERPUpdater_Setup.exe` (별칭 복사본)
  * SHA256: `C2C61F4C445543233DDEC4F83EEA1696BE72E85DB7D5E0D1CE9649B98DA86CD8`
* `latest.json`(루트 + release) v3.0.0으로 갱신됨.
* 빌드 경고: `Hidden import "jinja2" not found!` — pandas 스타일링 훅의 기존 경고로 동작 무관.

### [ ] 4-c. 남은 확인 (실기동 스모크 테스트 / 배포 전 정리)
* 설치본을 실제 설치·기동해 기간 모드(9열) 전환 후 `=항공비+호텔비` 등 이름 참조가 **밀리지 않고** 정확한 열을 계산하는지, 클릭 참조로 삽입되는 헤더명이 올바른지 확인.
* 정식 배포 시: `latest.json`의 `notes`가 아직 v2.x 내용("가이드 매뉴얼 웹페이지 연동")이므로 기간 모드 관련 릴리스 노트로 교체 필요. GitHub 릴리스 업로드 전까지 download_url은 미존재 경로를 가리킴.

---

## 3. v3.0.0 추가 개선 (2026-06-02, 사용자 요청 반영)

### [x] 5. 엑셀 양식 다운로드 — 양식 선택창 추가
* **대상:** `download_excel_template` + 신규 `_ask_template_format` ([gui.py](gui.py))
* 다운로드 시 라디오 버튼 선택창(커스텀 Toplevel)을 먼저 띄움: `단일 날짜 양식` / `기간 입력 양식`, 하단 `다운받기`·`취소`. 현재 그리드 모드를 기본 선택값으로 둠. Enter=다운받기, Esc/창닫기=취소.
* 선택한 양식 헤더로 빈 엑셀 저장(파일명 기본값도 양식별로 구분).

### [x] 6. 기간 엑셀 자동 감지 개선
* **대상:** `load_and_validate_fares` 감지부 + 신규 `_is_date_like` ([excel_loader.py](excel_loader.py))
* 기존 "헤더 '종료' 키워드 OR 9열 이상" 조건이 **헤더 없는(빈 첫 행) 7열 기간 파일**을 못 잡던 문제 수정.
* 이제 **열 개수와 무관하게** 1·2번째 열의 날짜 비율을 검사 → 둘 다 60% 이상 날짜면 기간 양식으로 확정. `_is_date_like`가 연(2000~2100)/월/일 범위까지 봐서 8자리 요금(예: 41000000)을 날짜로 오인하지 않음.
* 검증: `괌 대한항공 테스트.xlsx`(7열, 빈 첫 행) → `is_period=True`, 시작/종료일 정렬 정상. 단일 모드 파일 → `is_period=False` 유지(회귀 없음).

### [x] 7. 날짜 필터 3종 기간 모드 대응 (겹침 기준)
* **대상:** `_apply_date_filter` ([gui.py](gui.py))
* 기존 시작일(`date`)만 보던 필터를 종료일(`date_end`)까지 고려한 **겹침 기준**으로 변경. (단일 모드는 `date_end == date`라 동일 동작)
  * 특정일 이후: `date_end >= 기준일`
  * 특정 날짜 지정: 지정일 중 하나라도 `date <= 지정일 <= date_end`
  * 기간 범위 지정: `date <= e and date_end >= s` (겹치면 포함)
  * 전체 대상: 변경 없음
* 결정 근거: 사용자 확정 — ① 실무 파일은 9열 정석 유지, ② 필터는 "겹치기만 하면 포함".

### [x] 8. 재빌드 (위 5~7 반영 인스톨러) — 2026-06-02 완료
* `build_setup.ps1` 재실행 성공(exit 0). `release/NaeilERPUpdater_Setup_v3.0.0.exe`(약 64.5 MB) 갱신.
* 새 SHA256: `32E062E5183AD9D8C5CDB4AB71C931B22BA4E0C18ED5C6F74C6916368E3311CD` (이전 빌드 대비 변경 확인 → 코드 반영됨).
* `latest.json`(루트 + release) 동일 해시로 갱신.

### [x] 9. RPA 종료일 입력 견고화 — 2026-06-02 (실기동 검증 필요)
* **증상:** 기간 행(예: 06-07~06-10) 실행 시 ERP 출발일자 종료칸이 시작일(06-07)로 남음 → 06-08~06-10 상품 누락 위험. 검증 로직은 조회된 날짜가 범위 안이면 통과하므로 "성공"으로 오기록됨.
* **원인 추정:** ERP 날짜위젯이 시작일 변경 시 종료일을 시작일로 자동 리셋 → 뒤이은 종료일 `send_keys`가 비동기 콜백에 덮임.
* **수정:** `rpa_worker_loop` 날짜 입력부 ([gui.py](gui.py) 2210 부근) — ①시작일 입력 후 `change/blur` 명시 발생으로 리셋 선처리 ②종료일 마지막 입력 + 표시값 재확인하며 최대 4회 재입력 ③그래도 어긋나면 경고 로그 출력. 시작일은 기존 `send_keys` 유지.
* **남은 확인:** ERP 실기동으로 종료칸에 06-10이 유지되는지, 06-08~06-10 상품까지 조회/수정되는지 확인 후 인스톨러 재빌드.
