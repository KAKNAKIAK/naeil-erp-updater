# erp_updater 작업일지 (현재 버전: v3.0.0)

> 최종 갱신: 2026-06-02

## 목표

내일투어 ERP 시스템(erp.naeiltour.co.kr)에서 항공 요금을 엑셀 기반으로 자동 입력·저장하는 RPA 도구.
Selenium + Python으로 디버깅 모드 크롬에 붙어서, 날짜별로 조회 → 전체 선택 → 요금 모달 오픈 → 값 주입 → 저장을 반복한다.

## 현재 상태

**정상 작동 중.** LIVE 모드에서 엑셀 10건 기준 전건 SUCCESS 확인 완료 (2026-05-28 17:17).

## 프로젝트 구조

```
erp_updater/
├── gui.py              ← GUI 앱 (tkinter 다크 모드). 실사용 진입점
├── main.py             ← CLI 스크립트. gui.py와 별도로 run.bat에서도 구동 가능
├── excel_loader.py     ← 엑셀 파싱 + 유효성 검사. GUI/CLI 공용
├── config.json         ← 셀렉터, 포트, dry_run 등 전체 설정
├── chrome_debug.bat    ← 디버깅 크롬 기동 (포트 9222, ERP 로그인 페이지 자동 열림)
├── run.bat             ← CLI 실행용 배치
├── run_gui.bat         ← GUI 실행용 배치 (venv 자동 생성·패키지 설치 포함)
├── build_setup.ps1     ← Windows EXE + SETUP 파일 생성 스크립트
├── packaging/          ← 설치용 IExpress payload 스크립트
├── requirements.txt    ← selenium, pandas, python-dotenv, webdriver-manager
├── .venv/              ← Python 가상환경
├── ChromeProfile/      ← 디버깅 크롬 전용 프로필 (세션 유지용)
├── logs/
│   ├── update_history.csv   ← 실행 이력 (날짜, 요금, 상태, 에러)
│   └── screenshots/         ← 저장 전후 증적 캡처
└── templates/          ← 엑셀 템플릿 (A열=날짜, B열=성인항공비, C열=알선수익)
```

## EXE / SETUP 배포 상태

2026-05-28 18:07 기준으로 Windows 배포본 생성 완료.

- 포터블 실행 폴더: `release/NaeilERPUpdater/`
- 실행 파일: `release/NaeilERPUpdater/NaeilERPUpdater.exe`
- 설치 파일: `release/NaeilERPUpdater_Setup.exe`
- 설치 방식: PyInstaller 기반 onefile setup
- 설치 위치: `%LOCALAPPDATA%\NaeilTour\ERPUpdater`
- 설치 시 생성 항목: 바탕화면 바로가기, 시작 메뉴 `Naeil Tour/Naeil ERP Fare Updater`

검증 내용:

- 기존 IExpress SETUP에서 `Command.com /c ... install.bat` 파일 없음 오류가 발생해 SETUP 방식을 교체
- 새 SETUP은 `Command.com`/IExpress를 사용하지 않음
- `release/NaeilERPUpdater_Setup.exe --silent --install-dir <TEMP>\NaeilERPUpdaterInstallTest --no-shortcuts` 테스트 성공
- 테스트 설치 폴더의 `NaeilERPUpdater.exe` 존재 확인
- 테스트 설치본 실행 후 5초 이상 GUI 프로세스 유지 확인
- SETUP 파일 생성 확인: 59,549,316 bytes
- SHA256: `CD6324CCFF4B850B7E1191E91CC009924EECEEF3F2805A9154336865D2BE73CE`

배포 메모:

- 다른 PC에는 `release/NaeilERPUpdater_Setup.exe`만 전달하면 된다.
- Python 설치는 필요 없다.
- Google Chrome은 필요하다.
- 첫 실행 전 일반 Chrome 창을 모두 닫고, 앱의 `ERP 켜기` 버튼으로 디버깅 크롬을 열어야 한다.
- Selenium/WebDriverManager가 크롬 드라이버를 맞추는 구조라, 최초 실행 PC에서는 인터넷 연결이 필요할 수 있다.

재빌드 명령:

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
.\build_setup.ps1
```

주의: 현재 `build_setup.ps1`은 남아 있는 `release/NaeilERPUpdater/` 포터블 폴더를 payload로 묶어 SETUP만 재생성한다. 원본 소스 복구 전까지는 앱 본체 EXE 재빌드는 불가하다.

현재 루트 복구 상태:

- `config.json`, `chrome_debug.bat`, `templates/`, `requirements.txt`는 루트에 복구됨
- `gui.py`, `main.py`, `excel_loader.py`, `run.bat`, `run_gui.bat`, `README.md`는 현재 루트에서 확인되지 않음
- `release/NaeilERPUpdater/` 포터블 실행본은 유지됨
- `recovered/gui.pyc_payload`에는 EXE에서 추출한 `gui` 코드 객체 payload가 있으나, 바로 수정 가능한 `.py` 원본은 아님

## 핵심 작동 흐름

1. `chrome_debug.bat` → 디버깅 크롬(포트 9222)이 ERP 로그인 페이지로 열림
2. 사용자가 ERP에 수동 로그인 → 요금 조회/제어 화면으로 이동
3. `run_gui.bat`(또는 `run.bat`) 실행
4. GUI에서 엑셀 파일 선택 → "요금수정 시작" 클릭
5. 날짜마다: 시작일/종료일 주입 → 조회 → 전체 선택 체크박스 → 요금업데이트 모달 → 항공비·알선수익 입력 → 이중 검증 → 저장 → 얼럿 수락

## 해결한 주요 문제와 결정 사항

### 1. iframe 구조

ERP 페이지가 iframe 안에 요소를 렌더링한다. `find_and_switch_to_element_frame()` (main.py) / `find_and_switch_frame()` (gui.py) 함수가 최상위 → 1단계 iframe을 자동 순회하며 셀렉터를 찾는다. 2단계 이상의 중첩 iframe은 현재 없음.

### 2. 전체 선택 체크박스 셀렉터

AUI Grid의 헤더 체크박스 셀렉터가 `div.aui-grid-renderer-header-checkbox input`에서 `td.aui-grid-row-check-header input`으로 바뀌어 있었다. config.json에 후자로 저장되어 있고, 코드에는 1차 시도 실패 시 `input.aui-checkbox`를 순회해서 부모 td 클래스를 확인하는 대체 로직도 있다.

### 3. 저장 버튼 클릭 시 얼럿 처리

`#priceSave` 클릭 즉시 브라우저 confirm 얼럿("기존 요금 삭제 후 입력하신 금액으로 업데이트 됩니다. 저장하시겠습니까?")이 뜬다. 일반 `click()` 호출 시 `UnexpectedAlertPresentException`이 터지므로 `execute_script("arguments[0].click();", save_btn)`으로 클릭하고, 직후 `driver.switch_to.alert`로 얼럿을 잡아서 `accept()` 처리한다. 저장 완료 후 추가 얼럿이 뜰 수 있어 최대 3회 반복 루프로 처리.

### 4. dry_run 모드 제거

GUI에서 DRYRUN/LIVE 선택 라디오를 삭제하고, 항상 LIVE(실제 저장) 모드로 강제 설정하도록 변경했다. config.json의 `dry_run`은 `false`로 고정.

### 5. 디버깅 포트 입력란 제거

사용자에게 보여줄 필요 없는 기술 정보이므로 GUI에서 숨겼다. config.json에 `127.0.0.1:9222`가 하드코딩 수준으로 남아 있다.

### 6. 조회 버튼 클릭 가로채기 오류 핫픽스

LIVE 실행 중 일부 행에서 `element click intercepted` 오류가 발생했다. 실제 ERP 조회 버튼은 `img#gridMain_r`가 `a` 태그 안에 들어있는 구조로 보이며, 이미지에는 `pointer-events: none`이 걸려 있어 Selenium의 일반 클릭이 간헐적으로 실패했다.

현재 배포 폴더에는 소스 파일(`gui.py`, `main.py`, `build_setup.ps1`)이 없고 `release/NaeilERPUpdater`만 남아 있어, 실행본이 읽는 `release/NaeilERPUpdater/config.json`을 우선 핫픽스했다.

- 변경 전: `#gridMain_r`
- 변경 후: `a:has(#gridMain_r), #gridMain_r`

이 설정은 `img#gridMain_r`를 직접 누르기보다, 해당 이미지를 감싼 클릭 가능한 `a` 태그를 먼저 선택한다. mock 화면처럼 `#gridMain_r` 자체가 `a`인 경우에도 fallback으로 계속 동작한다.

### 7. 1회성 실행 기준으로 이력 로그 의존 제거

2026-05-28 18:21 기준 배포 실행 파일을 직접 패치했다. 원본 소스 파일이 없는 상태라 `release/NaeilERPUpdater/NaeilERPUpdater.exe` 내부의 `gui`와 `excel_loader` 바이트코드를 수정했다. 원본 실행 파일은 `recovered/NaeilERPUpdater_original_20260528_1809.exe`에 보관했다.

반영 내용:

- 조회 결과 행 대기 시간을 10초에서 15초로 연장
- `config.json`의 `timeout` 값을 15로 변경
- 엑셀 로더에서 기존 `logs/update_history.csv`의 `SUCCESS` 이력 기반 자동 스킵 제거
- 실행 중 `write_log()`는 no-op 처리하여 `update_history.csv`에 신규 이력을 누적하지 않음
- 콘솔/화면 처리 로그의 `성인`, `아동` 표현을 `항공비`, `알선수익`으로 변경
- GUI 섹션명 `실시간 작업 로그`를 `실시간 작업 내용`으로 변경
- 설치 파일 `release/NaeilERPUpdater_Setup.exe` 재생성 완료

검증:

- 패치 후 실행 파일 내부 상수 확인: 조회 대기 `15`, 출력 문구 `항공비/알선수익`, `write_log` no-op 확인
- 포터블 실행 파일을 5초간 기동해 GUI 프로세스 유지 확인 후 종료
- 기동 확인 중 `update_history.csv` 수정 시각이 변경되지 않음

추가 반영(2026-05-28 18:26):

- 저장 전/후 화면 캡처 함수 `save_screenshot()`을 no-op 처리하여 신규 캡처 파일 생성 중단
- 날짜 행 XPath를 `2025-01-02`, `2025.01.02`, `2025/01/02`, `20250102` 표시까지 대응하도록 정규화 비교 방식으로 변경
- 스킵 문구를 `[조회결과 없음] ... 반영하지 못했습니다. ERP에서 직접 날짜 조회를 확인해 주세요.`로 변경
- 패치 후 포터블 EXE 5초 기동 확인: 스크린샷 파일 개수 증가 없음

추가 반영(2026-05-28 18:32):

- 저장 완료 직후 ERP 내부 로딩이 남아 다음 날짜 입력/조회가 꼬이는 상황을 막기 위해 저장 성공 후 다음 행으로 넘어가기 전 3초 안정화 대기 추가
- 기존 화면 캡처 함수 호출 위치를 재사용하되 파일 저장은 하지 않고, `LIVE_POST` 시점에만 대기하도록 처리
- 포터블 EXE 재패치 및 설치 파일 재생성 완료
- 포터블 EXE 일반 기동 후 프로세스 유지 확인

추가 확인/조정(2026-05-28 18:38):

- 디버깅 크롬 포트 `127.0.0.1:9222`에 직접 붙어 ERP 화면 캡처 확인
- 실제 화면에서 검색 조건은 `2026-06-12`인데 그리드 결과는 `2026-06-11`로 남아 있는 상태 확인
- 저장 후 안정화 대기를 3초에서 8초로 연장
- 조회 결과 행 대기를 15초에서 25초로 연장
- `config.json`의 `timeout` 값을 25로 변경
- 설치 파일 재생성 완료

## config.json 셀렉터 매핑

| 키 | 값 | 대상 |
|---|---|---|
| `search_date_input` | `#searchStDate` | 출발 시작일 입력 |
| `search_date_end_input` | `#searchEnDate` | 출발 종료일 입력 |
| `search_button` | `a:has(#gridMain_r), #gridMain_r` | 조회 버튼 |
| `header_all_checkbox` | `td.aui-grid-row-check-header input` | 전체 선택 체크박스 |
| `update_button` | `#priceUpdate` | 요금업데이트 버튼 |
| `adult_fare_input` | `#addAir01` | 항공비 입력 필드 |
| `child_fare_input` | `#addProfit41` | 알선수익 입력 필드 |
| `save_button` | `#priceSave` | 저장 버튼 |
| `cancel_button` | `#popCloseBtn` | 취소/닫기 버튼 |

## GUI 버튼 구성 (최종)

| 버튼 | 기능 |
|------|------|
| ERP 켜기 | chrome_debug.bat 실행 (디버깅 크롬 + ERP 로그인 페이지) |
| 찾기... | 엑셀 파일 선택 대화상자 |
| 요금수정 시작 | RPA 루프 시작 (백그라운드 스레드) |
| 일시 중지 / 다시 시작 | 현재 행 처리 완료 후 대기/속행 토글 |
| 중지 | 안전 중단 (확인 팝업 후 플래그 설정) |

## 엑셀 포맷

- A열: 날짜 (YYYY-MM-DD 또는 YYYYMMDD 또는 Timestamp)
- B열: 성인 항공비 (정수, 콤마 허용)
- C열: 알선수익 (정수, 콤마 허용)
- 1행은 헤더로 간주하지 않고 pandas 기본 동작을 따름

## 주의사항

- 크롬은 반드시 **모든 일반 크롬 창을 닫은 뒤** 디버깅 모드로 기동해야 한다. 안 그러면 디버깅 포트에 접근 불가.
- GUI(`gui.py`)와 CLI(`main.py`)는 로직이 거의 동일하지만 **별도 유지보수**가 필요한 코드 이중화 상태다. 한쪽을 고치면 다른 쪽도 동기화해야 한다.
- `gui.py`의 `rpa_worker_loop`는 daemon 스레드에서 돌아가므로, tkinter 위젯 조작은 `self.root.after(0, ...)` 를 통해서만 해야 한다. 현재 `status_lbl.config(...)` 등 일부는 직접 호출하고 있어서, 드물게 스레드 안전성 이슈가 생길 수 있다.
- ERP DOM 구조가 업데이트되면 config.json의 셀렉터를 다시 확인해야 한다.
- 배포용 인스톨러 생성 시, 한글 사본 파일명(예: `ERP 항공요금수정_배포용_v1.x.x.exe`)은 생성하지 않고, 오직 **영문 인스톨러 파일(`NaeilERPUpdater_Setup_v1.x.x.exe`)만 단독 생성 및 관리**한다. (사용자 요청 사항, 2026-05-29 15:09)

## 다음에 할 수 있는 개선 작업

- [ ] `gui.py`와 `main.py`의 로직 이중화 해소 (공통 모듈 추출)
- [ ] tkinter 위젯 조작의 스레드 안전성 전면 정비 (`root.after` 통일)
- [ ] 엑셀 1행 헤더 자동 감지 또는 명시적 스킵 옵션
- [x] 이미 성공한 날짜 자동 스킵 기능 (현재 GUI 쪽은 history 연동 없이 전부 실행)
- [x] 실행 결과 요약 리포트 (성공/실패 날짜 목록 자동 생성)
- [ ] 다중 시트 또는 다중 엑셀 연속 처리

## 작업 이력 추가 (2026-05-29)

### 1. 날짜 필터 기능 및 요약 보고서 완료
- **날짜 필터 구현**: `gui.py`에 '전체 대상', '특정일 이후', '특정 날짜 지정' 라디오 버튼과 텍스트 입력창이 온전하게 추가되었으며, `excel_loader.py`에 작성된 날짜 포맷 정규화 및 필터링 처리가 연동되었습니다.
- **요약 보고서**: RPA 루프(`rpa_worker_loop`) 완료 시점에 성공, 단발성 실패, 연속 실패 목록(기간 및 대표 오류)을 집계하여 GUI 결과 팝업과 콘솔 로그에 한눈에 보이게 노출되도록 구현했습니다.

### 2. maximum recursion depth exceeded 에러 해결
- **문제 원인**: 기존 바이너리(EXE) 패치(`patch_release.py`) 작업 중 `load_and_validate_fares` 함수 내에서 `import excel_loader`를 한 뒤 모듈 함수를 호출하는 과정에서 꼬임이 발생해, 함수가 자기 자신을 무한히 재귀 호출하는 오류였습니다.
- **해결 조치**: 불안정한 바이너리 패칭 방식 대신, 복구된 깨끗한 소스 코드로 처음부터 컴파일하는 방식으로 전면 수정했습니다. 

### 3. 작업 환경 정리 및 재빌드 락(Lock) 해결
- **프로세스 락 해제**: 구글 드라이브 동기화 락을 쥐고 유령 구동 중이던 구버전 `NaeilERPUpdater.exe`(PID: 10544)와 열려 있던 인스톨러 `NaeilERPUpdater_Setup.exe` 프로세스(PID: 14324, 8784)를 찾아 모두 강제 종료(`taskkill`)했습니다.
- **Selenium 모듈 누락(ImportError) 해결**: PyInstaller 패키징 시 하위 모듈이 누락되어 발생한 `No module named 'selenium.webdriver.chrome.options'` 문제를 막기 위해 `gui.py` 최상단에 관련 모듈들을 명시적으로 수동 임포트해 주었습니다.
- **최종 빌드 성공**: 로컬 임시 폴더를 통해 온전하게 재빌드한 포터블 릴리즈를 정상 복사하고, 최종 셋업 배포본인 `release/NaeilERPUpdater_Setup.exe` 생성을 완료했습니다.

### 4. adult/child KeyError 버그 해결 (v1.1.1)
- **문제 원인**: `excel_loader.py`는 요금 데이터를 추출할 때 `adult_fare`와 `child_fare` 키를 딕셔너리에 넣어 반환하였으나, `gui.py` 내의 `rpa_worker_loop`에서는 `adult`와 `child` 키를 참조하여 `KeyError: 'adult'`가 발생하며 작업이 중단되었습니다.
- **해결 조치**: `gui.py`가 `"adult_fare"` 및 `"adult"` 모두를 정상적으로 지원하도록 `row.get("adult_fare", row.get("adult"))` 듀얼 체크 방식으로 로직을 수정하여 런타임 중단을 방지했습니다.

### 5. 조회 버튼 클릭 미작동 버그 해결 (v1.1.2)
- **문제 원인**: 조회 버튼 셀렉터(`search_button`)가 `a:has(#gridMain_r), #gridMain_r`로 설정되어 첫 번째 요소인 부모 `a` 태그를 찾아 클릭하였습니다. 그러나 실제 ERP 구조는 자식 이미지 태그인 `#gridMain_r`에만 이벤트 핸들러가 바인딩되어 있어 부모를 누를 경우 쿼리가 유발되지 않았습니다.
- **해결 조치**: 셀렉터를 `#gridMain_r` 이미지 엘리먼트 자체로 단일화 복원하고 `config.json` 및 `gui.py` 기본값을 수정하여, 정확한 이미지 요소 타겟 강제 자바스크립트 클릭이 호출되도록 조치했습니다.

### 6. 저장 후 스마트 대기(스마트 폴링) 적용
- **적용 내용**: 저장 버튼 클릭 및 얼럿 수락 직후, ERP AUIGrid 차단막이 해제될 때까지 1초 단위로 확인하는 스마트 폴링 방식을 `gui.py`에 정식 반영했습니다.
- **감지 대상**: `.aui-grid-mask`, `.aui-grid-loading`, `.aui-grid-loading-loading` 표시 여부를 확인합니다.
- **대기 방식**:
  - 저장 직후 차단막이 아직 뜨기 전의 짧은 순간을 완료로 오판하지 않도록, 차단막을 한 번도 보지 못한 경우에도 최소 2초간 안정 상태를 재확인합니다.
  - 차단막을 감지한 경우에는 차단막이 사라지는 즉시 다음 날짜로 진행합니다.
  - 최대 대기 시간은 `config.json`의 `timeout` 값을 따르며, 루트/릴리즈 모두 `25`초로 맞췄습니다.
- **빌드 반영**:
  - 포터블 실행 파일 재빌드 및 교체 완료: `release/NaeilERPUpdater/NaeilERPUpdater.exe`
  - 설치본 재생성 완료: `release/NaeilERPUpdater_Setup_v1.1.2.exe`
  - 전달용 기본 설치 파일도 최신본으로 동기화: `release/NaeilERPUpdater_Setup.exe`
  - SHA256: `0A98D5F7A0C75E76DB5C3C26D1F67CC6A99DB57785E96FB9B83A8DED0527B3E1`
- **검증**:
  - `gui.py`, `excel_loader.py`, `check_chrome.py` 문법 체크 통과
  - 포터블 EXE 5초 기동 유지 확인
  - 설치본을 임시 폴더에 silent 설치 후 `config.json`의 `timeout=25`, `search_button=#gridMain_r` 확인
  - 작업 종료 후 남아 있는 `NaeilERPUpdater` 관련 프로세스 없음

### 7. 조회결과 인식 불가 근본 원인 수정 + 그리드 안정화 매칭 (2026-05-29 11:05)

라이브 디버깅 크롬(포트 9222, ERP 로그인 상태)에 직접 붙어 실제 DOM을 검사하며 비파괴 방식(조회만, 저장 미실행)으로 전 구간을 테스트했다.

- **버그 1 (치명적): `date_cell_in_row` 셀렉터 오류**
  - `td.cell-date`는 실제 ERP 요금 그리드(AUIGrid)에 **존재하지 않는다**(라이브 검사 결과 0개). 그래서 조회 후 결과 대기가 항상 타임아웃 → **모든 날짜가 "조회결과 없음"으로 스킵**되어 사실상 동작 불능 상태였다.
  - 실제 날짜 셀은 `.aui-grid-default-column`에 `YYYY-MM-DD`(예: `2026-10-13`) 형태로 렌더링된다.
  - 조치: `config.json`(루트 + `release/NaeilERPUpdater/`)과 `gui.py` 기본값의 `date_cell_in_row`를 `.aui-grid-default-column`으로 교체.

- **버그 2 (다중 날짜 시 오작동): 조회 결과 안정화 미대기**
  - 기존 코드는 조회 직후 `presence_of_element` 한 번만 확인하고 즉시 날짜 매칭을 수행했다. 그러나 AUIGrid 조회는 **약 10초 지연**이 있고, 전환 중에는 **이전 날짜 행과 새 날짜 행이 잠시 공존**(라이브 관측: ~9.5초 두 날짜 혼재 → ~13초에 대상 날짜 단일로 안정)한다. 이 순간에 전체선택을 누르면 이전 날짜 행까지 선택될 위험이 있었다. (작업일지 18:38의 "검색 2026-06-12인데 그리드 2026-06-11" 증상이 이것.)
  - 조치: `gui.py` 조회 매칭 로직을, 그리드의 날짜 셀 집합이 **대상 날짜 단일(`== {대상날짜}`)로 안정될 때까지 폴링**하는 방식으로 교체. 빈 결과(데이터 없는 날짜)는 집합이 비어 타임아웃 → 정상적으로 SKIP 처리.

- **부수 수정**: `rpa_worker_loop`의 디버거 주소 조회를 `debugger_address` 설정키를 우선 읽도록 보정(기존엔 오타성 `debuggerAddress` 키만 봐서 항상 기본값으로 폴백).

- **라이브 검증 (비파괴, 조회만)**:
  - 셀렉터 체인 전 구간 정상 확인: 날짜 주입 → 조회 클릭(`#gridMain_r` JS click이 실제 쿼리 트리거함) → 결과 렌더 → 전체선택 체크박스 → 요금업데이트 → 모달 입력필드(`#addAir01/#addProfit41/#priceSave/#popCloseBtn`) 모두 동일 프레임에 정상 노출 → 닫기.
  - 안정화 매칭 4케이스 통과: 데이터 있는 날짜(2026-05-29, 2026-10-13)는 8~12초에 정확히 매칭, 데이터 없는 날짜(2030-01-01)는 올바르게 미매칭(타임아웃), 전환 중 이전 날짜 잔상 거짓매칭 없음.
  - **저장(`#priceSave`)은 실데이터(운영 ERP 요금) 변경이라 의도적으로 실행하지 않음.** 실제 저장 1건 검증은 사용자가 직접 날짜를 골라 실행 권장.

- **빌드 반영**:
  - PyInstaller로 소스에서 포터블 EXE 재빌드 → `release/NaeilERPUpdater/NaeilERPUpdater.exe` 및 `_internal` 교체(지원 파일·config은 보존).
  - 새 EXE 6초 기동 유지 확인.
  - `build_setup.ps1`로 인스톨러 재생성: `release/NaeilERPUpdater_Setup_v1.1.2.exe` + 별칭 `release/NaeilERPUpdater_Setup.exe`.
  - Setup SHA256: `d4155d0a0cef2a177c0863c0b6a892345d3f62610b0c5c32b3f9f7e71d4ef0f2`
  - 검증용 임시 스크립트(`_inspect*.py`, `_test_*.py`)와 빌드 산출물(`dist_new`, `build_new`)은 정리 완료.

> 비고: 빌드 스크립트의 버전 문자열은 `v1.1.2`로 유지되어 설치 파일명도 v1.1.2 그대로다. 내부 로직은 위 수정이 반영된 최신본이다.

### 8. 앱 아이콘 교체 (2026-05-29 11:10)

- 사용자 제공 이미지(`C:\Users\naeiltour0001\Desktop\프로필\Gemini_Generated_Image_cbglhzcbglhzcbgl.png`)를 Pillow로 정사각 센터크롭 후 다중 해상도(16~256px) `app_icon.ico`로 변환해 프로젝트 루트에 저장.
- `NaeilERPUpdater.spec`의 `EXE(...)`에 `icon='app_icon.ico'` 추가 → 포터블 앱 EXE 아이콘 반영.
- `build_setup.ps1`의 인스톨러 PyInstaller 호출에 `--icon app_icon.ico` 추가 → Setup.exe 아이콘도 동일하게 반영.
- 재빌드: 포터블 EXE + `_internal` 교체, 인스톨러 재생성. 앱 EXE 5초 기동 유지 확인.
- 새 Setup SHA256: `d29556da69470f9b1316124235cfb8aeaa15439336e35c32664da50505e7f88a`
- 빌드 산출물(`dist_new`, `build_new`) 정리 완료. `app_icon.ico`는 빌드 입력이므로 루트에 보존.

추가 교체(2026-05-29 11:18):

- 아이콘을 다른 이미지(`ChatGPT Image 2026년 4월 22일 오후 05_48_41 (4).png`, 오렌지 캐릭터 얼굴)로 다시 교체. 동일 변환 절차로 `app_icon.ico` 갱신 후 EXE·인스톨러 재빌드.
- 새 Setup SHA256: `bb362fc22a90f0e686727bb812ea13bd229d4234a0dfd7eaff3972997e7ffddf`

## 아이디어 메모: 동일 요금 연속 구간 범위 일괄 처리 (2026-05-29, 미구현)

사용자와 나눈 대화를 아이디어 차원으로만 기록한다. **아직 구현/수정하지 않았다.**

- 사용자 관찰: 엑셀을 보니 연속된 날짜(예: 2026-10-12 ~ 2026-10-19)의 요금이 전부 동일(320000 / 32000)한 경우가 많다.
- 제안: 지금처럼 하루씩 시작=종료로 돌리지 말고, 시작일=2026-10-12·종료일=2026-10-19로 **범위 조회 1회 → 전체선택 → 요금 1회 입력 → 저장 1회**로 묶으면 작업 수가 크게 줄어든다.
- 성립 근거: ERP 조회가 원래 출발일 '범위' 검색이고, 요금업데이트는 '선택된 행 일괄 수정'이라 다중 날짜 선택에도 같은 요금이 적용된다. 효과는 날짜 8개 기준 8회 → 1회 수준.
- 묶음 조건: ① 요금이 완전히 동일, ② 날짜가 연속(중간 누락·요금 변동 없음), ③ 범위 내 모든 상품 행에 같은 요금이 박히는 점 감안(특정 행만 골라야 하면 부적합).
- 구현 시 고려: 현재 매칭 로직은 "그리드에 대상 날짜 단일만 떠야 성공"(잔상 방지용)이라, 범위 처리하려면 "조회된 날짜들이 시작~종료 범위 안에 있는지"로 검증을 바꾸고, 엑셀에서 동일 요금 연속 구간을 자동으로 묶는 전처리가 필요하다.


## 9. 최종 배포본 정리 + 폴더 클린업 (2026-05-29)

- 최종 배포 인스톨러를 `release/ERP 항공요금수정_배포용.exe`로 이름 변경(기존 `NaeilERPUpdater_Setup.exe` 알리아스를 그대로 리네임).
- 소스·빌드 도구는 사용자 선택에 따라 **유지**(gui.py, excel_loader.py, config.json, chrome_debug.bat, check_chrome.py, NaeilERPUpdater.spec, build_setup.ps1, requirements.txt, app_icon.ico, packaging/, templates/, .venv/, ChromeProfile/, 작업일지.md).
- 삭제한 불필요 파일/폴더:
  - 루트: `recovered/`, `pycdc.exe`, `patch_release.py`, `gui.pyc`, `excel_loader.pyc`, `__pycache__/`, `build/`, `screenshot_after_a_click.png`, `screenshot_after_img_click.png`, `tmp_live_erp_screen.png`
  - release: `NaeilERPUpdater_Setup_v1.1.0.exe`, `NaeilERPUpdater_Setup_v1.1.1.exe`, `NaeilERPUpdater_Setup_v1.1.2.exe`, `NaeilERPUpdater_old_20260529_1000/`, `NaeilERPUpdater_old_20260529_1004/`
- release 폴더 최종 구성: `ERP 항공요금수정_배포용.exe`(약 56.5MB) + 포터블 빌드 입력 `NaeilERPUpdater/`(build_setup.ps1 재빌드용으로 유지).
- 배포 파일 자체는 직전 v1.1.2 인스톨러(아이콘 교체 반영본)와 동일 바이너리이며 파일명만 배포용으로 정리한 것.

## 10. UI 개선 + 실행 로딩 표시 + 캐릭터 애니메이션 (2026-05-29)

### 변경 요약
- **ERP 켜기 버튼 이동**: 설정 카드 안 → 헤더 우상단(제목 줄 높이에 맞춘 수직 중앙 정렬). 헤더를 좌(제목·버전배지·설명)/우(버튼) 구조로 재배치.
- **다크 테마 디자인 정돈** (tkinter 내):
  - 색 팔레트 재정의(더 깊은 배경, 카드/입력 테두리 하이라이트, 포커스 시 파란 테두리).
  - ttk Progressbar를 `clam` 테마 + 초록 채움(두께 18)으로 스타일링(`Accent.Horizontal.TProgressbar`).
  - 상태 표시를 `상태:` 텍스트 → 색상 점(●)+텍스트로 변경(`set_status` 헬퍼). 대기=회색/진행=주황/성공=초록/오류=빨강.
  - 버튼 호버 색 전환(`_add_hover`), 손가락 커서, 아이콘 기호(▶/‖/■). 이모지는 폰트 깨짐 우려로 안전 기호로 대체.
  - 제목 옆 `v1.1.2` 배지, 로그창 테두리 래핑 + 안쪽 여백.
- **실행 로딩 가시화**(중복 클릭 방지 목적):
  - PyInstaller **스플래시**(`splash.png`) 추가 → 실행 즉시 "프로그램을 준비하고 있습니다…" 창. `.spec`에 `Splash(...)` + `splash.binaries` 연결, `gui.py` 진입부에서 `pyi_splash.close()`.
  - **중복 실행 방지**: Windows 네임드 뮤텍스(`_acquire_single_instance_lock`). 이미 실행 중이면 "이미 실행 중" 안내 후 종료.
  - **ERP 켜기 즉시 피드백**: 클릭 시 버튼 "여는 중…"으로 비활성화(3초) + 상태 갱신.
  - 비콘솔 실행 대비 stdout 가드(`NullStream`) 추가.
- **캐릭터 로딩 애니메이션**:
  - 카카오 캐릭터 GIF(`001_여행가자_animated_sample.gif`)를 `assets/loading.gif`로 복사, `.spec` `datas`에 추가해 번들.
  - `get_resource_path()`로 `_MEIPASS`/앱폴더 양쪽에서 리소스 탐색.
  - Pillow `ImageTk`로 16프레임을 140px로 미리 로드 → RPA가 ERP에 연결하는 동안 화면 중앙 오버레이로 캐릭터 재생("ERP에 연결하고 있어요…"). 연결 완료/실패/중단 시 자동 종료(`show_loading`/`hide_loading`/`_animate_loading`).

### 빌드/배포
- `python -m PyInstaller --noconfirm --clean NaeilERPUpdater.spec`로 onedir 앱 재빌드(스플래시+GIF 포함). 빌드 EXE 단독 실행 스모크 테스트 통과(창 제목 정상).
- exe 옆 런타임 파일(config.json·chrome_debug.bat·templates) 복사 후 `release/NaeilERPUpdater/` 교체. 출하 config의 `date_cell_in_row`=`.aui-grid-default-column` 확인.
- `build_setup.ps1`로 인스톨러 재생성 → `NaeilERPUpdater_Setup.exe` 별칭을 **`release/ERP 항공요금수정_배포용.exe`**(약 57.2MB)로 리네임. 버전드 사본 삭제.
  - 배포 인스톨러 SHA256: `4853D646D60C1464296084EF8B1231D5C242A6E5A3B352D9538CD01CAFE390B7`
- 빌드 산출물(build/, dist/, __pycache__) 정리.

> 배포 방법: 대상 PC에서 `ERP 항공요금수정_배포용.exe` 실행 → payload 자동 설치. 사용자는 이 파일 하나만 받으면 됨.

## 11. 설치 프로그램 바로가기 오류 수정 + v1.1.3 (2026-05-29)
- **증상**: 다른 PC에서 배포용 설치 실행 시 `create_shortcuts.ps1 returned non-zero exit status 1`로 "설치 중 오류" 발생. 실제로는 파일 복사는 끝났고 마지막 바로가기 생성 단계만 실패한 것이라 설치 전체가 실패로 처리되는 게 과한 동작이었음.
- **수정**(`packaging/setup_installer.py`):
  - `create_shortcuts`: PowerShell 출력 캡처(`capture_output`) 후 실패 시 실제 에러 메시지를 포함해 `RuntimeError` 발생. 스크립트를 `utf-8-sig`(BOM)로 저장해 PS 5.1 인코딩 인식 문제 예방.
  - `install_payload`: 바로가기 생성을 try/except로 감싸 **비치명적 경고**로 처리. 파일 설치가 끝났으면 설치는 성공으로 간주하고 `(exe_path, shortcut_warning)` 반환.
  - `main`: 바로가기만 실패한 경우 "설치 완료 + 바로가기 수동 생성 안내 + 실행 파일 경로 + 원인" 메시지 표시.
- **버전 표기 일괄 상향 v1.1.2 → v1.1.3**: `gui.py`(타이틀바, 버전 배지), `build_setup.ps1`(`$Version`, 설치 파일명).
- **빌드/배포**: onedir 재빌드(exit 0, `_internal/assets/loading.gif` 포함) → 런타임 파일 복사 → `release/NaeilERPUpdater` 교체 → `build_setup.ps1`(exit 0) → `release/ERP 항공요금수정_배포용.exe`(약 60MB) 교체. build/dist/__pycache__ 정리.
  - 배포 인스톨러 SHA256: `417B390CA6C9816BA0BE26FBBF7C9DB2E42C365F25EB422A043F87CE04351B3D`

## 12. 설치 진행 화면(로딩) 추가 + 스플래시 v1.1.3 (2026-05-29)
- **목적**: 배포용 EXE 설치 시 "설치 중"임을 직관적으로 알 수 있게(중복 실행 방지).
- **설치 진행 창**(`packaging/setup_installer.py` `run_gui_install`): 다크 테마 창 + "파일을 설치하고 있습니다…" + 애니메이션 진행 막대(ttk clam, 초록). 설치는 백그라운드 스레드로 실행, 완료/경고/오류를 같은 창에서 메시지 + "확인" 버튼으로 표시. silent/GUI 불가 환경은 기존 텍스트 출력 유지.
- **설치 스플래시**(`build_setup.ps1`에 `--splash install_splash.png` 추가): onefile 부트로더가 60MB를 임시폴더에 푸는 동안의 빈 화면을 덮음. 진행 창이 뜨면 `pyi_splash.close()`로 닫음(`_close_pyi_splash`).
- **스플래시 이미지 재생성**(`make_splash.py` 신규): 앱용 `splash.png`(v1.1.2→v1.1.3 갱신) + 설치용 `install_splash.png`(신규) 생성. 맑은 고딕, 480x270, 다크/액센트/초록 진행칩.
- **검증**: 임시 폴더 무인 설치 스모크 테스트 → exe·_internal·config·templates·logs·ChromeProfile 정상 생성 확인.
- **빌드/배포**: onedir 재빌드(새 splash 반영) → release 교체 → `build_setup.ps1`(설치 스플래시 포함) → `release/ERP 항공요금수정_배포용.exe`(약 60MB) 교체. build/dist/__pycache__ 정리.
  - 배포 인스톨러 SHA256: `0AA5BD9C378F7D591E2770E0C0AC30F8417A9C3B4D0337D884355A1A7F835E0F`
- 신규 파일: `make_splash.py`, `install_splash.png`.

## 13. 설치 폴더 선택 기능 추가 (2026-05-29)
- **목적**: 설치 시 사용자가 설치 위치를 직접 지정.
- **폴더 선택 화면**(`packaging/setup_installer.py` `run_gui_install` 재구성): 설치 흐름을 `① 폴더 선택 → ② 진행 막대 → ③ 결과` 3단계로 분리.
  - 1단계: 설치 폴더 Entry + "찾아보기"(`filedialog.askdirectory`) + "설치"/"취소" 버튼. 기본값 `%LOCALAPPDATA%\NaeilTour\ERPUpdater`. 다른 폴더 선택 시 그 안에 `ERPUpdater` 하위 폴더를 두어 파일이 흩어지지 않게 함.
  - `--install-dir` 인자가 주어지면(자동/silent) 선택 화면을 건너뜀(`allow_choose=False`).
- **검증**: 소스에서 선택 창 띄워 정상 렌더링 확인 + 설치본 빌드.
- **빌드/배포**: 설치본만 재빌드(onedir release는 최신이라 재사용). 최종 배포본 **`release/ERP 항공요금수정_배포용_v1.1.3.exe`**(약 60MB).
  - 배포 인스톨러 SHA256: `9B77F0172F048AF3D1D415F02076F3D899FE05AE2783AFD66F34BDCCF891CBA5`
  - 참고: 카논 이름 `ERP 항공요금수정_배포용.exe`는 빌드 중 Google Drive File Stream 삭제-동기화 잠금에 걸려, 사용자 결정에 따라 **버전 포함 이름을 최종 배포명으로 채택**.

## 14. 중지 버튼 클릭 시 앱 종료 문제 수정 + v1.1.4 (2026-05-29)

- **증상**: RPA 진행 중 `중지`를 누르면 중단 확인 후 프로그램 창 자체가 닫히는 현상.
- **추정 원인**: 백그라운드 RPA 스레드가 `print()`를 통해 Tkinter `ScrolledText` 로그창을 직접 수정하고, 종료/중단 시 결과 보고서 생성까지 워커 스레드에서 실행하던 구조. Tkinter는 메인 스레드 외 위젯 조작에 취약해서 메시지박스/중단 처리와 겹칠 때 앱이 비정상 종료될 수 있음.
- **수정**(`gui.py`):
  - `GUIConsoleRedirector`를 큐 기반으로 변경하여 워커 스레드 로그는 큐에 넣고, 메인 스레드의 `after()` 폴링에서만 로그창에 반영.
  - RPA 종료 `finally`에서 결과 보고서 생성과 UI 복구를 `finish_rpa_on_ui()`로 묶어 Tkinter 메인 스레드에서만 실행.
  - `중지` 클릭 즉시 `일시 중지`/`중지` 버튼을 비활성화하고 상태를 `중지 요청됨 · 현재 단계 정리 중`으로 변경.
  - 연결 실패 시 중복 UI 복구 예약을 제거하고 공통 종료 경로로 정리.
- **버전 표기 상향**: `gui.py`, `build_setup.ps1`, `make_splash.py`, `packaging/setup_installer.py`를 v1.1.4로 갱신.
- **빌드/배포**:
  - `make_splash.py` 재실행으로 앱/설치 스플래시 v1.1.4 갱신.
  - PyInstaller onedir 재빌드 후 `release/NaeilERPUpdater/` 교체. `config.json`, `chrome_debug.bat`, `templates/` 런타임 파일 포함 확인.
  - `build_setup.ps1`로 `release/NaeilERPUpdater_Setup_v1.1.4.exe` 생성 후 전달용 `release/ERP 항공요금수정_배포용_v1.1.4.exe` 복사.
- **검증**:
  - `gui.py`, `excel_loader.py`, `check_chrome.py`, `packaging/setup_installer.py`, `make_splash.py` 문법 체크 통과.
  - Tkinter 숨김 창에서 워커 스레드 로그 큐 전달 테스트 통과(`threaded log redirect ok`).
  - 포터블 EXE 및 silent 설치본 EXE 각각 6초 이상 프로세스 유지 확인.
  - silent 설치 테스트: `--silent --install-dir <TEMP>\NaeilERPUpdaterInstallSmoke_v1.1.4 --no-shortcuts` exit 0, `NaeilERPUpdater.exe`/`config.json`/`_internal/assets/loading.gif` 존재 확인.
  - 배포 인스톨러 SHA256: `0715C9D3FCEA56828F01C8A5856B67F860FAD1AAF3A37519F8F390CE27520883`

## 15. GitHub latest.json 기반 자동 업데이트 프로세스 추가 + v1.1.5 (2026-05-29)

- **목표 프로세스**: 앱 시작 → GitHub `latest.json` 확인 → 새 버전 알림 → 사용자 승인 → 설치본 다운로드 → SHA256 검증 → 별도 updater 실행 → 기존 앱 종료 대기 → silent 설치 → 새 앱 재실행.
- **신규 파일**:
  - `update_client.py`: `latest.json` 조회, 버전 비교, 설치본 다운로드, SHA256 검증 담당.
  - `updater.py`: 작은 updater 프로세스. 기존 앱 PID 종료 대기 후 설치본을 `--silent --install-dir <현재 앱 폴더> --no-shortcuts`로 실행하고 새 `NaeilERPUpdater.exe` 재실행.
  - `latest.json`: GitHub에 올릴 manifest 템플릿. `version`, `download_url`, `sha256`, `notes` 스키마 사용.
- **GUI 반영**(`gui.py`):
  - `APP_VERSION = "v1.1.5"` 상수화.
  - 시작 0.8초 후 `update_latest_url`을 비동기 확인. URL이 비어 있으면 조용히 건너뜀.
  - 새 버전이면 변경 내용과 함께 업데이트 승인 팝업 표시.
  - 다운로드 완료 후 `NaeilERPUpdaterUpdater.exe`를 임시 폴더로 복사해 실행하고, 메인 앱은 종료. updater를 앱 폴더에서 직접 실행하지 않아 설치 중 updater EXE 잠금 문제를 회피.
- **설정 추가**(`config.json`):
  - `update_enabled`
  - `update_latest_url`
  - `update_check_timeout`
  - `update_download_timeout`
- **빌드 반영**:
  - `NaeilERPUpdater.spec`에 두 번째 EXE(`NaeilERPUpdaterUpdater.exe`) 추가. 포터블 release 폴더에 메인 EXE와 updater EXE가 함께 들어감.
  - `build_setup.ps1` 버전 v1.1.5로 상향. 빌드 후 `release/latest.json`의 `version`/`sha256`을 설치본 기준으로 갱신하도록 보강.
  - `make_splash.py`, `packaging/setup_installer.py` 버전 v1.1.5로 상향.
- **최종 산출물**:
  - 포터블 앱: `release/NaeilERPUpdater/NaeilERPUpdater.exe`
  - updater: `release/NaeilERPUpdater/NaeilERPUpdaterUpdater.exe`
  - 배포 설치본: `release/ERP 항공요금수정_배포용_v1.1.5.exe`
  - GitHub manifest 템플릿: `release/latest.json`
- **검증**:
  - `gui.py`, `update_client.py`, `updater.py`, `excel_loader.py`, `check_chrome.py`, `packaging/setup_installer.py`, `make_splash.py` 문법 체크 통과.
  - 버전 비교 테스트 통과.
  - 로컬 `latest.json` manifest 판별 테스트 통과(v1.1.4 → v1.1.5는 업데이트, v1.1.5 → v1.1.5는 미업데이트).
  - 설치본 다운로드 + SHA256 검증 로직 테스트 통과(로컬 file URI, ASCII 파일명 설치본 기준).
  - silent 설치 테스트: `--silent --install-dir <TEMP>\NaeilERPUpdaterInstallSmoke_v1.1.5 --no-shortcuts` exit 0, `NaeilERPUpdater.exe`/`NaeilERPUpdaterUpdater.exe`/`config.json`/`_internal/assets/loading.gif` 존재 확인.
  - 포터블 EXE 및 silent 설치본 EXE 각각 6초 이상 프로세스 유지 확인.
  - GUI 초기화 시 업데이트 설정 병합 확인.
  - 배포 인스톨러 SHA256: `D76D07B466B72A96094CA28550F1A3924D974E392EF304E8591FF87AA645D0D7`

> 남은 연결 작업: `release/ERP 항공요금수정_배포용_v1.1.5.exe`를 GitHub Release에 업로드한 뒤, `release/latest.json`의 `download_url`을 실제 Release asset URL로 바꾸고, 설치본의 `config.json` 또는 설치된 앱의 `config.json`에 raw `latest.json` URL을 `update_latest_url`로 넣으면 자동 업데이트 확인이 활성화된다.

## 16. mock ERP 제거 + GitHub 배포 레포 연결 (2026-05-29)

- **mock 제거**:
  - `templates/mock_erp.html`은 실제 앱 코드에서 참조되지 않는 테스트 화면이라 삭제.
  - 빈 `templates/` 폴더와 배포 폴더의 `release/NaeilERPUpdater/templates/`도 제거.
- **자동 업데이트 URL 연결**:
  - `config.json`의 `update_latest_url`을 `https://raw.githubusercontent.com/KAKNAKIAK/naeil-erp-updater/main/latest.json`으로 설정.
  - `latest.json`의 `download_url`을 GitHub Release asset URL로 변경.
  - 설정 변경과 mock 제거를 설치본에 반영하기 위해 setup 재생성.
  - 최신 배포 인스톨러 SHA256: `F17ECC5AE193E93F49E24DEAC47690F1B51DE621B17BCD5DE779BEEBA1BAFDEF`
- **GitHub 준비**:
  - `.gitignore` 추가: `.venv/`, `ChromeProfile/`, `release/`, `build/`, `dist/`, `logs/` 등 로컬/빌드 산출물 제외.
  - `README.md` 추가: 자동 업데이트 manifest와 배포 절차 기록.

## 17. 중지 시 불필요한 결과 요약 팝업 미노출 수정 + v1.1.6 (2026-05-29 14:38)

- **버그 해결 (중지 시 팝업 미노출)**:
  - RPA 작업 진행 중 사용자가 `중지` 버튼을 클릭하여 강제 중단했을 때, 비어있는 검은 화면 상태의 `[작업 완료] 결과 요약 보고서` 팝업창이 불필요하게 뜨는 현상을 수정했습니다.
  - `gui.py`에 `self.is_user_stopped` 플래그를 도입하여 중지 요청 시 `True`로 설정하고, 결과 보고서 생성 함수(`generate_and_show_report`)에서 이 플래그가 설정되어 있다면 결과 팝업창(`show_popup`) 생성을 건너뛰고 리턴하도록 조치했습니다.
- **업데이트 URL 안정화 및 버전 상향 (v1.1.5 → v1.1.6)**:
  - GitHub Release에 한글 파일명을 올리면 asset URL이 안전하지 않게 변형될 수 있어, 자동 업데이트용 asset은 ASCII 파일명 `NaeilERPUpdater_Setup_v1.1.6.exe`를 사용하도록 템플릿을 정리했습니다.
  - `gui.py`, `make_splash.py`, `build_setup.ps1`, `packaging/setup_installer.py`, `latest.json` 버전을 `v1.1.6`으로 일괄 상향했습니다.
- **재빌드 및 배포**:
  - `make_splash.py`를 통해 스플래시 이미지 v1.1.6으로 갱신 후, PyInstaller 메인 프로그램을 빌드하여 `release/NaeilERPUpdater` 포터블 릴리즈를 업데이트했습니다.
  - Google Drive dist 폴더 삭제 잠금이 발생하여 PyInstaller `--distpath`/`--workpath`를 TEMP 경로로 지정해 재빌드했습니다.
  - `build_setup.ps1`을 실행하여 최종 셋업 파일인 `release/NaeilERPUpdater_Setup_v1.1.6.exe` 및 업데이트 매니페스트 `latest.json`을 성공적으로 빌드했습니다.
  - 사용자가 바로 다운받아 쓸 수 있도록 한글 파일명 사본 `release/ERP 항공요금수정_배포용_v1.1.6.exe` 생성을 완료했습니다.
  - silent 설치 스모크 테스트 통과: exit 0, 메인 EXE/updater EXE 존재, `templates/` 미생성, GitHub raw `update_latest_url` 반영, 설치본 EXE 6초 이상 유지 확인.
  - 최종 셋업 파일 SHA256: `BFCD5434D5BAFF10F1B0F124FD27AEDB3EC9CAE7F24E23266AE7C8427DEAF040`

## 18. GitHub API 기반 latest.json 확인 + v1.1.7 (2026-05-29 14:50)

- GitHub raw URL이 push 직후 이전 `latest.json`을 캐시해 돌려주는 상황을 확인.
- `update_client.py`가 GitHub Contents API 응답(`encoding=base64`, `content`)도 해석하도록 보강.
- `config.json`의 `update_latest_url`을 raw URL에서 `https://api.github.com/repos/KAKNAKIAK/naeil-erp-updater/contents/latest.json?ref=main`으로 변경.
- `gui.py`, `make_splash.py`, `build_setup.ps1`, `packaging/setup_installer.py`, `latest.json` 버전을 `v1.1.7`로 상향.
- Google Drive release 포터블 폴더 잠금이 있어, TEMP에 빌드된 onedir 결과를 payload로 직접 묶어 설치본 생성.
- `build_setup.ps1` 보강: `release/NaeilERPUpdater/` 포터블 폴더가 없으면 TEMP 경로에서 onedir 앱을 자동 빌드해 payload로 사용.
- silent 설치 스모크 테스트 통과: exit 0, 메인 EXE/updater EXE 존재, `templates/` 미생성, GitHub Contents API `update_latest_url` 반영, 설치본 EXE 6초 이상 유지 확인.
- v1.1.7 설치본 SHA256: `B0264AAE19C979D5533EC41279B739A1753D978F7C1BC03E1B4B49E9C7682C95`

## 19. 언인스톨러 timeout 무한 반복 및 Uninstall.exe 잔존 오류 해결 + v1.1.8 (2026-05-29 15:10)

- **버그 해결 (언인스톨러 오류 및 잔존)**:
  - 언인스톨(제거) 실행 시, 윈도우 환경에 따라 배치 파일 내부의 `timeout` 명령어가 제어를 벗어나 화면에 반복적으로 깜빡이며 출력되는 현상을 수정했습니다.
  - `timeout /t 1 /nobreak >nul` 대신 어떠한 콘솔창도 유발하지 않는 `ping 127.0.0.1 -n 2 >nul` 지연 방식을 채택하여 콘솔창 노출 버그를 원천 해결했습니다.
  - 제거 완료 화면("제거가 완료되었습니다." GUI 및 "확인" 버튼)이 떠 있는 동안에는 `Uninstall.exe` 프로세스가 여전히 실행 중이어서, 백그라운드 삭제 배치가 파일 잠금 상태인 `Uninstall.exe` 및 상위 폴더를 즉시 삭제하지 못하고 60초 대기 후 조기 종료(자폭)되어 결국 파일이 지워지지 않고 남아있던 문제를 해결했습니다.
  - `Uninstall.exe`의 프로세스 ID(PID)를 직접 구하여, 배치 파일 내에서 `tasklist /fi "PID eq <PID>"`로 제거기 프로세스가 완전히 종료될 때까지 대기(최대 120초)했다가 안전하게 폴더째 삭제하도록 로직을 보완했습니다.
- **버전 표기 상향 (v1.1.7 → v1.1.8)**:
  - `gui.py`, `make_splash.py`, `build_setup.ps1`, `packaging/setup_installer.py`, `latest.json` 버전을 `v1.1.8`로 일괄 상향했습니다.
- **재빌드 및 배포**:
  - `make_splash.py`를 실행하여 스플래시 이미지 v1.1.8로 갱신 완료.
  - `build_setup.ps1`을 실행하여 메인 프로그램 재빌드, 셋업 인스톨러 패키징, `latest.json` 업데이트를 한 번에 완료했습니다.
  - 최종 셋업 설치본 `release/NaeilERPUpdater_Setup_v1.1.8.exe` 및 전달용 한글 사본 `release/ERP 항공요금수정_배포용_v1.1.8.exe` 생성을 완료했습니다.
  - v1.1.8 설치본 SHA256: `E45D5FED4B9256CF9555A4F412F85558460C52278BE34AEF109E249FCC993EDB`

## 20. 메인 프로그램의 검은색 스플래시 가림 버그 해결 + v1.1.9 (2026-05-29 15:15)

- **버그 해결 (메인 창 뒤로 겹침 현상)**:
  - 프로그램 실행 시 메인 GUI 윈도우 앞에 `Naeil ERP Fare Updater` 라는 검은색 빈 윈도우 창이 떠서 메인 GUI를 가리거나 두 개의 창이 겹쳐 보이는 현상을 수정했습니다.
  - 원인은 메인 프로그램 `NaeilERPUpdater.exe` 에도 PyInstaller 스플래시 화면(`pyi_splash`)이 묶여 있었고, 윈도우 환경에서 Tkinter 메인 루프 실행 타이밍과 충돌하며 스플래시 창이 고스트 창(검은 화면)으로 닫히지 않고 남았던 버그였습니다.
  - 메인 프로그램은 포터블(onedir) 방식으로 빌드되어 실행 지연이 거의 없으므로, 메인 프로그램 빌드 스펙([NaeilERPUpdater.spec](file:///g:/내%20드라이브/안티그래비티/erp_updater/NaeilERPUpdater.spec))에서 불필요한 `Splash` 설정을 완전히 제거했습니다. (인스톨러 스플래시는 유지됨)
- **버전 표기 상향 (v1.1.8 → v1.1.9)**:
  - `gui.py`, `make_splash.py`, `build_setup.ps1`, `packaging/setup_installer.py`, `latest.json` 버전을 `v1.1.9`로 일괄 상향했습니다.
- **재빌드 및 배포**:
  - `make_splash.py`를 실행하여 스플래시 이미지 v1.1.9로 갱신 완료.
  - `build_setup.ps1`을 실행하여 메인 프로그램 재빌드, 셋업 인스톨러 패키징, `latest.json` 업데이트를 완료했습니다.
  - 최종 셋업 설치본 `release/NaeilERPUpdater_Setup_v1.1.9.exe` 생성을 완료했습니다. (한글 배포용 사본은 단일화 규칙에 따라 생성하지 않았습니다.)
  - v1.1.9 설치본 SHA256: `6DFA61BE2B47ED38B36F17E1DE770AFDBCD9B48AE536C06995A8AA035443D891`

## 21. 스플래시 제거로 인한 pyi_splash 임포트 에러(KeyError) 해결 + v1.1.10 (2026-05-29 15:21)

- **버그 해결 (프로그램 실행 불가 크래시)**:
  - 메인 프로그램에서 스플래시 화면을 제거하면서, 프로그램 실행 파일(`NaeilERPUpdater.exe`)에 스플래시 IPC 기능이 탑재되지 않았습니다.
  - 하지만 실행 코드([gui.py](file:///g:/내%20드라이브/안티그래비티/erp_updater/gui.py)) 내부에 여전히 `import pyi_splash`를 시도하는 코드가 남아있어, 스플래시 환경이 아닐 때 이 모듈을 로드하는 과정에서 `KeyError: '_PYI_SPLASH_IPC'` 예외(Traceback)가 터지며 프로그램이 켜지지 않고 크래시가 나는 현상을 수정했습니다.
  - 메인 코드(`gui.py`) 내부에서 `pyi_splash`와 관련된 모든 임포트 및 함수 호출 코드를 완전히 안전하게 제거했습니다.
- **버전 표기 상향 (v1.1.9 → v1.1.10)**:
  - `gui.py`, `make_splash.py`, `build_setup.ps1`, `packaging/setup_installer.py`, `latest.json` 버전을 `v1.1.10`으로 일괄 상향했습니다.
- **재빌드 및 배포**:
  - `make_splash.py`를 통해 스플래시 이미지 v1.1.10으로 갱신 완료.
  - `build_setup.ps1`을 실행하여 메인 프로그램 재빌드, 셋업 인스톨러 패키징, `latest.json` 업데이트를 완료했습니다.
  - 최종 셋업 설치본 `release/NaeilERPUpdater_Setup_v1.1.10.exe` 생성을 완료했습니다.
  - v1.1.10 설치본 SHA256: `CF8DE91B4B84FE57B53CDFA86891CA20EFB82504C56F6062727B27E29BBAB1FD`

## 22. 엑셀 다중 시트 및 C열 빈값 관련 피드백 기록 (2026-05-29 15:28)

- **피드백 내용**:
  - 사용자가 C열(알선수익/아동요금)을 비우거나 `0`을 넣어도 오류가 발생한다고 보고함.
  - 사용자가 "시트가 많아서 그런가?"라고 의문을 제기하였으며, 테스트 시트가 여러 개일 때 첫 번째 시트만 읽어오는 문제와 연관된 것으로 추정됨.
- **분석 및 기술적 판단**:
  1. **다중 시트 문제**: `pd.read_excel(file_path)`는 기본적으로 엑셀의 첫 번째 시트만 읽어오므로, 작업 대상 시트가 첫 번째가 아니면 빈 값을 읽어 오류가 발생함.
  2. **C열 빈값 필터링 문제**: `excel_loader.py` 내의 `pd.isna(raw_child)` 검사로 인해 C열이 비어있으면 행을 스킵함. 또한 `0`을 입력했을 때도 엑셀 서식에 따라 텍스트 공백이나 수식 오류가 섞여 있으면 형변환(`ValueError`) 에러로 스킵됨.
- **향후 개선 계획**:
  - 다중 시트인 경우 사용자가 시트를 선택할 수 있는 드롭다운/팝업 기능 추가.
  - C열 요금이 누락되었을 때 무조건 스킵하지 않고 `0`으로 처리하거나, 성인 요금만 반영될 수 있도록 유연하게 개선.

## 23. 날짜 필터 '기간 범위 지정' 옵션 추가 + v1.1.11 (2026-05-29 15:45)

- **기능 개발 (기간 범위 필터링)**:
  - 기존의 시작일 이후(`FROM_DATE`), 특정 날짜 나열(`SPECIFIC`) 옵션에 더하여, **"언제부터 언제까지"**의 범위를 처리할 수 있는 `기간 범위 지정`(`DATE_RANGE`) 옵션을 구현했습니다.
  - `excel_loader.py`에 시작일과 종료일(물결표 `~` 구분) 사이의 범위 내 날짜들만 안전하게 필터링하는 로직을 추가했습니다.
  - `gui.py` UI에 `기간 범위 지정` 라디오 단추를 추가하고, 필터 모드가 변경되거나 값을 입력할 때 실시간으로 팁 문구(예: `20260601~20260610`)를 갱신 및 실시간 필터 대상 건수가 상태바에 출력되도록 Trace 핸들러(`_on_filter_mode_change`, `_on_filter_value_change`)를 적용했습니다.
  - `gui.py`, `build_setup.ps1`, `make_splash.py`, `packaging/setup_installer.py` 버전을 `v1.1.11`로 미리 준비했습니다. (실제 설치본 빌드는 사용자 요청에 의해 생략)
  - 자동 업데이트 실사용자 교체 크래시를 방지하기 위해 깃허브 main의 배포용 `latest.json` 버전 정보는 이전 `v1.1.10`으로 안전하게 유지했습니다.

## 24. 업데이트 API 제한(Rate Limit) 회피 및 캐시 우회 적용 (2026-05-29 16:03)

- **문제 원인 (업데이트 미작동)**:
  - 기존 깃허브 Contents API URL(`https://api.github.com/repos/...`)을 사용할 때, 깃허브 토큰이 없는 일반 환경(혹은 사무실 공용 IP)에서 시간당 60회인 API 호출 제한(Rate Limit)에 걸려 `403 Forbidden` 에러가 조용히 터지고 업데이트 체크가 스킵되던 버그였습니다.
- **해결 조치**:
  - **호출 URL 복원**: `config.json`의 `update_latest_url` 주소를 API 제한이 없는 깃허브 Raw 파일 URL(`https://raw.githubusercontent.com/.../latest.json`)로 복원했습니다.
  - **캐시 차단 쿼리 적용**: `update_client.py`에서 `raw.githubusercontent.com` 호출 시, CDN 캐시 지연을 완벽히 방지하기 위해 주소 끝에 타임스탬프 파라미터(`?t=<현재초단위시간>`)를 동적으로 덧붙이도록 보완했습니다. 이로써 깃허브 갱신 즉시 딜레이 없이 신규 버전이 인지됩니다.

## 25. 기간 범위 지정 필드 분리(시작/종료 Entry 2개) 적용 및 v1.1.12 빌드 배포 (2026-05-29 16:14)

- **UI 개선 (기간 입력란 분리)**:
  - 기존에 물결표(`~`) 구분 기호와 함께 한 입력칸에 모든 시작~종료 날짜를 적어야 했던 방식을 개선했습니다.
  - `gui.py` 의 `filter_input_container` 내에서 필터 모드 선택에 따라 위젯이 동적으로 재구성되도록 보강했습니다.
  - `기간 범위 지정` 모드일 때만 `[시작 날짜 입력란] ~ [종료 날짜 입력란]` 형태로 두 개의 입력란이 가로로 표시되며, 다른 모드에서는 단일 입력란만 표시되도록 동적 폼을 적용했습니다.
  - 두 입력란의 상태 변화(글자 입력/삭제)가 일어날 때마다 실시간으로 엑셀 대상을 다시 필터링하도록 Trace 핸들러를 확장했습니다.
- **연동 및 빌드 배포**:
  - `make_splash.py`를 통해 스플래시 이미지를 `v1.1.12`로 갱신한 뒤, `build_setup.ps1`을 실행하여 `NaeilERPUpdater_Setup_v1.1.12.exe` 셋업 파일을 완성했습니다.
  - 깃허브 릴리스에 `v1.1.12`를 배포하고 셋업 파일을 업로드하였으며, 최신 manifest `latest.json`을 깃허브 main에 푸시하여 v1.1.10 버전 실행 시 즉각 자동 패치 알림이 뜨도록 처리 완료했습니다.

## 26. 메인 실행 파일의 CMD 콘솔창 숨김 처리 및 v1.1.13 빌드 배포 (2026-05-29 16:23)

- **UI 개선 (검은 콘솔창 제거)**:
  - 사용자가 메인 프로그램(`NaeilERPUpdater.exe`)을 구동할 때 뒤에 CMD(명령 프롬프트) 검은 창이 계속 떠 있어서 불편함을 호소했던 문제를 해결했습니다.
  - `NaeilERPUpdater.spec` 빌드 스펙 파일의 `exe` 인스턴스 설정에서 `console=True` 옵션을 `console=False`로 변경하여, 윈도우 환경에서 실행 시 검은 콘솔창이 뜨지 않고 오직 깔끔한 GUI 창만 단독으로 실행되도록 완전히 숨김 처리했습니다.
- **재빌드 및 배포**:
  - `make_splash.py`를 통해 스플래시 이미지를 `v1.1.13`으로 갱신 완료했습니다.
  - `build_setup.ps1`을 실행하여 `NaeilERPUpdater_Setup_v1.1.13.exe` 최신 셋업 패키지를 빌드했습니다.
  - 깃허브 릴리스에 `v1.1.13` 버전을 새로 생성하고 셋업 파일을 업로드하였습니다.
  - 최신 매니페스트 `latest.json`을 깃허브 main에 커밋&푸시하여 v1.1.11 및 이전 버전 사용자가 프로그램을 켰을 때 즉각 콘솔창이 사라진 v1.1.13 버전으로 자동 패치되도록 처리 완료했습니다.

## 27. 빌드 스크립트(build_setup.ps1) 캐시 누락 버그 핫픽스 및 v1.1.13 재배포 (2026-05-29 16:29)

- **문제 원인 (업데이트 루프 및 미작동)**:
  - 그동안 셋업 설치본을 설치하여 실행했음에도 불구하고 화면에 최신 코드가 나타나지 않거나 업데이트 알림이 반복되던 근본적인 원인이었습니다.
  - `build_setup.ps1` 내부의 1단계 빌드 로직에서 `release\NaeilERPUpdater\NaeilERPUpdater.exe` 파일의 존재 여부만 체크하고 빌드를 건너뛰는 구조로 작성되어 있어, 소스 코드가 수정되더라도 포터블 폴더에 구버전 파일이 남아있으면 **메인 프로그램을 재빌드하지 않고 이전 캐시 파일로 계속 셋업 payload를 묶던 버그**였습니다.
- **해결 조치**:
  - **빌드 강제화**: `build_setup.ps1` 에서 빌드 조건 분기를 제거하고, 빌드를 실행할 때마다 매번 포터블 release 디렉토리를 비운 뒤 PyInstaller를 호출하여 최신 바이너리를 강제로 새로 컴파일하도록 수정했습니다.
  - **동기화 보장**: 컴파일 성공 후 최신 빌드 결과물이 `release\NaeilERPUpdater\` 폴더로 안전하게 복사되어 셋업 패키지 payload에 포함되도록 동기화 로직을 보완했습니다.
  - **v1.1.13 재배포**: 수정된 빌드 스크립트로 정상 빌드를 진행하여 60MB급 `v1.1.13` 인스톨러 에셋을 재생성하고, 깃허브 릴리스를 다시 덮어씌워 완전히 정상 패키지 상태로 업로드를 완료했습니다.

## 28. 자동 업데이트 SHA256 불일치로 전 사용자 실패하던 버그 수정 (2026-05-29)

- **증상**: 사용자가 "자동 업데이트가 안 된다"고 보고. 새 버전 알림은 떠도 마지막에 실패.
- **근본 원인 (확정)**: `latest.json`에 적힌 `sha256`이 실제 업로드된 `v1.1.14` 설치본의 해시와 **불일치**.
  - 매니페스트 기재값: `3E99F5CE...1CF66D9B`
  - 실제 설치본(GitHub 자산 = 로컬 `release/NaeilERPUpdater_Setup_v1.1.14.exe`, 둘 다 61,740,747 bytes 동일) 해시: `8EEE8043...7BA13197`
  - 코드 흐름(`update_client.download_installer`)상 새 버전 감지 → 승인 → 61MB 다운로드까지 모두 성공하지만, 마지막 SHA256 무결성 검증에서 불일치 → `UpdateError` → GUI "업데이트 실패" 메시지박스 → 설치 중단. 즉 **모든 클라이언트가 동일하게 실패**.
  - `console=False`(v1.1.13~) + 에러를 `print`로만 처리하는 구조라 실패 원인이 화면/콘솔에 드러나지 않아 그동안 "그냥 안 됨"으로 보였음.
- **진단 방법**: 로컬 venv 파이썬으로 `update_client`를 라이브 URL에 직접 실행. latest.json 조회 → 버전 비교 → `download_url` HEAD(HTTP 200, 61MB) → 실제 자산 다운로드 후 해시 대조로 불일치 확인. 로컬 `release` 설치본 해시도 GitHub 자산과 동일함을 확인(설치본 자체는 정상, 매니페스트 해시만 오류).
- **해결 조치**: `latest.json`의 `sha256`을 실제 설치본 해시 `8EEE8043AB63081A210A39A888AAA233BFCA16833A7E6A4EFB0B391A7BA13197`로 교정 후 GitHub `main`에 커밋·푸시(`4e8c7b5`).
- **검증**: GitHub Contents API(권위 소스)에서 `main`의 `latest.json` sha256이 교정값으로 반영됨 확인. `raw.githubusercontent.com`은 약 5분간 이전 값을 캐시하는 CDN 지연이 있어, 전파 후 end-to-end(다운로드+SHA256 통과) 재검증을 추가 진행.
- **메모(미해결 관찰)**:
  - `update_client.fetch_latest_manifest`가 raw URL에 `?t=<timestamp>` 캐시버스트를 붙이는데도 raw CDN이 push 직후 한동안 이전 내용을 돌려줌. 실사용자는 앱 실행 시점엔 이미 TTL이 지나 영향 없지만, 배포 직후 즉시성은 보장되지 않음.
  - 향후: 빌드 스크립트가 `latest.json` 해시를 항상 "실제 업로드되는 그 설치본"에서 산출하도록 일치 보장 필요(이번 불일치는 빌드/업로드 사이 해시 산출 대상이 어긋난 것으로 추정).

## 29. v1.1.15 빌드·배포 + 자동 업데이트 테스트 준비 + 해시 불일치 재발 수정 (2026-05-29)

- **배경**: 자동 업데이트를 실제로 테스트하기 위해 버전을 v1.1.15로 올려 풀 배포 진행. 동시에 updater 실행파일명을 `NaeilERPUpdaterUpdater.exe` → `UpdateHelper.exe`로 변경하고 전용 아이콘(`helper_icon.ico`) 적용.
- **버전 상향(v1.1.15)**: `gui.py`(APP_VERSION), `build_setup.ps1`($Version), `make_splash.py`, `packaging/setup_installer.py`, `latest.json`. `NaeilERPUpdater.spec`의 updater EXE를 `name='UpdateHelper'` + `icon='helper_icon.ico'`로 변경. `gui.py`의 `UPDATER_EXE_NAME='UpdateHelper.exe'`로 일치.
- **빌드**: `make_splash.py`로 스플래시 v1.1.15 갱신 후 `build_setup.ps1` 실행. onedir 포터블(메인 + `UpdateHelper.exe`) + onefile 설치본 `release/NaeilERPUpdater_Setup_v1.1.15.exe` 생성(exit 0).
- **버그 (해시 3자 불일치) 발견·수정**:
  - GitHub v1.1.15 릴리스에 올라간 자산 해시(`47F16251...`), 커밋된 라이브 `latest.json` 해시(`8EEE8043...`, v1.1.14값), 로컬 재빌드 해시(`9AC64D26...`)가 **모두 달랐음**. PyInstaller 빌드는 매번 바이트가 달라 동일 소스라도 해시가 바뀜.
  - 그대로 두면 v1.1.15 업데이트도 SHA256 검증에서 실패. **재업로드 없이** 루트 `latest.json` 해시를 "이미 배포된 릴리스 자산"(`47F16251...`)에 맞춰 교정 후 `main` 푸시(`0aa5a15`).
  - 근본 교훈: `build_setup.ps1`이 해시를 `release/latest.json`에만 쓰는데 GitHub에 푸시되는 건 루트 `latest.json`이라 둘이 어긋남. + 릴리스에 올리는 자산과 매니페스트 해시를 산출한 빌드가 다를 수 있음 → **"릴리스에 실제 업로드한 그 파일"에서 해시를 떠 루트 latest.json에 박고 같이 푸시**하는 절차로 굳혀야 재발 방지.
- **검증**: GitHub Contents API로 `main` latest.json = v1.1.15 / `47F16251...` 확인(릴리스 자산과 일치). raw CDN 전파 후 `v1.1.14 → v1.1.15` 신버전 판정 + 자산 다운로드 + SHA256 통과 end-to-end 확인.
- **수정 파일**: `latest.json`(해시 교정), 버전 문자열 4개 파일, `NaeilERPUpdater.spec`(UpdateHelper), `작업일지.md`. 신규: `helper_icon.ico`, `release/NaeilERPUpdater_Setup_v1.1.15.exe`(release/는 git 제외).

## 30. 자동 업데이트 "Failed to load Python DLL" 에러 해결 + v1.1.16 (2026-05-29)

- **증상**: v1.1.14 설치본으로 자동 업데이트를 시도하니 다음 에러 대화상자가 뜸:
  `Failed to load Python DLL '...\CreatorTemp\NaeilERPUpdaterHelper_xxxx\_internal\python311.dll'. LoadLibrary: 지정된 모듈을 찾을 수 없습니다.`
- **근본 원인**: `gui.py`의 `launch_updater_and_exit`가 설치 중 파일 잠금을 피하려고 업데이트 도우미 EXE를 **임시 폴더에 "그 파일 하나만" 복사**해서 실행함. 그런데 기존 도우미는 **onedir** 방식이라 옆에 `_internal\python311.dll`이 함께 있어야 부팅됨. 단일 EXE만 복사하니 `_internal`이 없어 런타임 로드 실패.
  - v1.1.13/14/15 **전 배포본이 동일하게 깨짐**(모두 onedir 도우미). 이미 설치된 버전은 자동 업데이트로 자가 치유 불가 → 고친 버전을 **한 번은 수동 설치** 필요.
- **수정**: `NaeilERPUpdater.spec`의 `UpdateHelper` EXE를 **self-contained onefile**로 빌드(`updater_a.binaries`/`datas`를 EXE에 포함, `exclude_binaries` 제거). COLLECT에는 메인 앱만 넣고, 빌드 후 onefile `UpdateHelper.exe`를 앱 폴더로 복사. onefile은 실행 시 자체 temp(`_MEIPASS`)로 풀려서 단일 파일 복사로도 정상 작동.
- **검증(핵심)**: 빌드된 `UpdateHelper.exe`(7.2MB, onefile)를 `_internal` 없는 빈 temp에 단독 복사해 실행 → DLL 에러 없이 Python 런타임 로드, 업데이트 로그 정상 기록(0.6초 종료). 이전 1.7MB onedir 대비 self-contained 확인.
- **배포(불일치 재발 방지 절차 적용)**: ① v1.1.16 빌드 → ② GitHub 릴리스 v1.1.16 생성 + 설치본 업로드 → ③ **업로드된 자산을 다시 받아 해시 실측**(`2F5640D5...`, 로컬 빌드와 일치) → ④ 루트 `latest.json` sha256을 그 값으로 박고 커밋·푸시(`e540f99`). GitHub API로 main=v1.1.16/`2F5640D5` 확인, raw 전파 후 `v1.1.14 → v1.1.16` 판정 + 다운로드+SHA256 end-to-end 통과.
- **사용자 조치 필요**: 기존 설치본(v1.1.13~15)은 깨진 도우미 때문에 자동 업데이트로 못 넘어옴. **각 PC에서 `release/NaeilERPUpdater_Setup_v1.1.16.exe`를 1회 수동 설치**하면 이후부터는 onefile 도우미라 자동 업데이트 정상.
- **수정 파일**: `NaeilERPUpdater.spec`, `latest.json`, 버전 4개 파일, splash png, `작업일지.md`. 신규: `release/NaeilERPUpdater_Setup_v1.1.16.exe`(git 제외).

## 31. "요금수정 시작" 무반응(AttributeError) 버그 수정 + v1.1.17 (2026-05-29)

- **증상**: v1.1.16에서 엑셀 로드 성공(267건) 후 `요금수정 시작`을 눌러도 **아무 반응 없음** — 에러창도, 로그도, 진행도 없음.
- **근본 원인**: `start_rpa`와 `clean_up_ui_after_rpa`가 **존재하지 않는 `self.filter_val_entry`** 위젯을 `.config()` 함.
  - 날짜 필터 입력칸이 모드별 **동적 생성 위젯**(지역변수 `entry`/`start_entry`/`end_entry`)으로 바뀌면서 옛 이름 `self.filter_val_entry` 참조 2곳이 그대로 남음(할당은 0건).
  - 시작 시 `is_running=True`·버튼 토글까지 진행된 뒤 `self.filter_val_entry.config(...)`에서 **`AttributeError`로 즉사 → 작업 스레드 시작 전**. `console=False`라 트레이스백이 콘솔 없이 삼켜져 "무반응"으로 보임. 모드 무관하게 항상 발생(= 배포된 v1.1.16은 시작 불가).
- **수정**(`gui.py`): 동적 필터 입력칸(Entry)을 순회하며 활성/비활성 토글하는 `_set_filter_inputs_state(state)` 헬퍼 추가. `start_rpa`/정리 함수의 `self.filter_val_entry.config(...)` 2곳을 헬퍼 호출로 교체. `filter_val_entry` 참조 0건 확인.
- **검증**: 사용자가 v1.1.17 설치본으로 직접 테스트 → 시작 정상 작동 확인("잘된다").
- **배포**: 버전 v1.1.17 일괄 상향(4파일+latest.json). 빌드(설치본 SHA256 `8B0D7B93...`) → GitHub 릴리스 v1.1.17 + 업로드 → 업로드 자산 해시 재확인(일치) → 루트 latest.json 해시 일치 후 커밋·푸시(`8d86554`). API로 main=v1.1.17/`8B0D7B93` 확인, raw 전파 후 `v1.1.16 → v1.1.17` end-to-end 통과.
- **참고**: 빌드는 분류기(명령 안전성 판단) 일시 장애로 사용자가 직접 `make_splash.py`+`build_setup.ps1` 실행. 자동배포(릴리스/매니페스트/푸시)는 분류기 복구 후 에이전트가 마무리.
- **수정 파일**: `gui.py`(버그 수정+버전), `build_setup.ps1`, `make_splash.py`, `packaging/setup_installer.py`, `latest.json`, splash png, `작업일지.md`. 신규: `release/NaeilERPUpdater_Setup_v1.1.17.exe`(git 제외).

## 32. 요금조회 바로가기 버튼 + 배포 자동화(해시 동기화) + 토큰 정리 + v1.1.18 (2026-05-29)

### (1) UI: 요금조회 사이트 바로가기 버튼
- `gui.py` 헤더 버전 배지 밑에 작은 링크 버튼 **`요금조회 사이트가기 ↗`** 추가 → 클릭 시 기본 브라우저로 `https://fare-calculator-2026.web.app/` 오픈(`open_fare_site`, 상수 `FARE_SITE_URL`).

### (2) 빌드/배포 자동화 — 해시 불일치 영구 차단
- **근본 문제**: `build_setup.ps1`이 해시를 `release/latest.json`에만 쓰는데 GitHub로 푸시되는 건 루트 `latest.json`이라, 새 버전마다 손으로 맞춰야 했고 그 사이 불일치로 자동 업데이트가 깨짐(작업일지 #28·#29 반복 원인).
- **수정 1**: `build_setup.ps1`이 빌드 시 **루트 `latest.json`까지** `version`/`download_url`/`sha256`을 동기화하도록 변경(release 사본과 루트 둘 다 기록).
- **수정 2(신규)**: `publish_release.ps1` 추가 — `빌드 → gh 릴리스 업로드 → (업로드 자산 재다운로드 대조, 비치명적) → 루트 latest.json 동기화 → 커밋·푸시 → GitHub API 검증`을 원커맨드로. 매니페스트 해시는 "방금 업로드한 그 설치본"의 해시(`gh`는 바이트 그대로 업로드하므로 로컬 해시 == 자산 해시)로 박음.
- **스크립트 트러블슈팅 메모(PS 5.1)**: `Invoke-WebRequest`는 기본 TLS1.0이라 GitHub 다운로드 실패 → `gh release download`는 이 버전에서 인자 파싱 이슈 → 결국 **로컬 설치본 해시 사용 + (선택)임시 .py 파일로 재다운로드 대조(비치명적)**로 안정화. API 검증 단계는 UTF-8 BOM 때문에 `ConvertFrom-Json` 실패 → `TrimStart([char]0xFEFF)`로 BOM 제거.

### (3) GitHub 토큰 정리(보안)
- remote URL에 평문으로 박혀 있던 광범위 권한 PAT(`ghp_...`) 제거.
- `gh auth setup-git`으로 git 자격증명 헬퍼를 gh로 전환 후, remote를 토큰 없는 `https://github.com/KAKNAKIAK/naeil-erp-updater.git`로 교체. `git ls-remote` 및 이후 실제 푸시(`0c5fde1`,`761dcd5`)로 정상 동작 확인.
- **사용자 1회 수동 조치 필요**: GitHub Settings → Developer settings → Personal access tokens 에서 노출됐던 그 토큰을 **Revoke**(폐기). 폐기 후 gh가 재로그인을 요구하면 `gh auth login`.

### (4) 배포 v1.1.18
- 버전 4파일 + latest.json v1.1.18 상향. 빌드 설치본 SHA256 `B99903CB...`.
- `publish_release.ps1`로 릴리스 업로드+매니페스트 동기화+푸시(`0c5fde1`). 소스/스크립트 별도 커밋(`761dcd5`).
- **검증**: GitHub API main=v1.1.18/`B99903CB`, 실제 업로드 자산 해시=`b99903cb`(삼중 일치). raw 전파 후 `v1.1.17 → v1.1.18` end-to-end 통과.
- **수정 파일**: `gui.py`(버튼+버전), `build_setup.ps1`(하드닝+버전), `make_splash.py`, `packaging/setup_installer.py`, `latest.json`, splash png, `작업일지.md`. 신규: `publish_release.ps1`, `release/NaeilERPUpdater_Setup_v1.1.18.exe`(git 제외).

### (5) 사후: v1.1.18 자동 업데이트 404 (download_url 파일명 누락) 핫픽스
- **증상**: v1.1.18 자동 업데이트 시 "설치본 다운로드 실패: HTTP Error 404". 설치본·릴리스는 정상(200), 릴리스도 draft 아님.
- **원인**: 커밋된 `latest.json`의 `download_url`이 파일명 없이 잘림(`.../releases/download/v1.1.18/`). `publish_release.ps1`이 매니페스트를 쓸 때 URL 끝 자산 파일명이 비어 들어감. (스크립트 로직을 수차례 동일 재현했으나 항상 정상 URL이 나와 정확한 트리거는 미특정 — 환경적 현상으로 판단.)
- **즉시 복구**: 루트 `latest.json`의 download_url을 전체 주소로 교정 후 푸시(`f80e3d9`). API 확인.
- **재발 방지(가드)**: `publish_release.ps1`·`build_setup.ps1`에서 download_url을 `-f` 연산자로 생성하고, **`/NaeilERPUpdater_Setup_*.exe` 패턴/해시 64자/버전 일치를 푸시(쓰기) 전에 검증해 어긋나면 throw**(`9ac6a4f`). 깨진 매니페스트는 이제 배포 단계에서 차단됨.
- **검증**: raw 전파 후 `v1.1.17 → v1.1.18` 다운로드+SHA256 end-to-end 통과.

## 33. 결과 요약 팝업 제거 + 배포 스크립트 버그 2건 수정 + v1.1.19 (2026-05-29)

- **(1) 결과 요약 팝업 제거**: RPA 종료 후 뜨던 모달 `[작업 완료] 결과 요약 보고서`(`generate_and_show_report`의 `show_popup`)를 사용자 요청으로 제거. 동일 요약을 이미 '실시간 작업 내용' 로그에 `print`하고 있어 중복이었고, 화면도 비어 보였음. 리포트 생성/로그 출력은 유지, 팝업 표시 코드(및 `is_user_stopped` 분기)만 삭제.
- **(2) build_setup.ps1 파서 오류**: 24번 가드 추가 시 넣은 **한글 `throw` 문자열**이 이 파일 인코딩(Edit 저장)에서 PowerShell 파서를 깨뜨려(문자열 미종료→블록 `}` 누락 연쇄) 빌드가 죽음. 가드 문구를 **ASCII로 교체**해 해결. (같은 한글 문자열이라도 Write로 만든 publish_release.ps1은 정상이라, 파일 인코딩 차이가 원인으로 판단.)
- **(3) publish_release.ps1 `$AssetName` 빈 문자열**: `"...{0}.exe" -f $Version`(괄호 없는 대입)이 실행 컨텍스트에서 빈 문자열을 반환 → download_url 파일명 누락(이전 v1.1.18 404의 진짜 원인). **`$AssetName = Split-Path -Leaf $Setup`**(실제 산출물 경로에서 파일명 도출)로 교체해 확실히 해결. 푸시 전 `.exe` 가드가 이 오류를 실제로 잡아 깨진 매니페스트 푸시를 차단함(가드 유효성 입증).
- **배포 v1.1.19**: 버전 4파일+latest.json 상향. 빌드 SHA256 `A06397F0...`. 업로드 자산=로컬=root latest.json 삼중 일치, download_url 파일명 포함 정상. 커밋·푸시(`804f647`), API로 main=v1.1.19 확인. raw 전파 후 `v1.1.18 → v1.1.19` end-to-end 검증.
- **수정 파일**: `gui.py`(팝업 제거+버전), `build_setup.ps1`(가드 ASCII), `publish_release.ps1`($AssetName), `make_splash.py`, `packaging/setup_installer.py`, `latest.json`, splash png, `작업일지.md`. 신규: `release/NaeilERPUpdater_Setup_v1.1.19.exe`(git 제외).

---

## v2.0 개발 및 정식 배포 (2026-06-01)

v2.0의 핵심 변경: 엑셀 파일을 업로드해서 읽던 방식을 버리고, **앱 안에 스프레드시트(셀) 기능을 내장**해 거기에 직접 입력·수정한 값을 RPA가 읽어 ERP에 반영하도록 한다.

### 구조 (v1과의 차이)

- 신규 파일: `gui.py` (v2.0 GUI 진입점 - 기존 v1.1.19 운영본은 `gui_v1_backup.py`로 보존)
- 재사용: `excel_loader.py`(엑셀 import 및 3열 하이브리드 지원), `config.json`(셀렉터/포트/타임아웃), `chrome_debug.bat`, `assets/loading.gif`
- 신규 의존성: `tksheet` 7.6.0 (requirements.txt에 추가 완료)

### 작업 이력

#### 1. 엑셀 업로드 → 앱 내장 셀 그리드 (2026-06-01)

**`gui_v2.py` 신규 작성** (gui.py v1.1.19 기반, 엑셀 업로드 부분만 셀 그리드로 교체)

- **셀 그리드 위젯**: `tksheet` 채택. 진짜 스프레드시트처럼 클릭·탭 이동·**엑셀 표 블록 붙여넣기(Ctrl+V)**·우클릭 행 삽입/삭제·복사/잘라내기/되돌리기 지원. 다크 테마(`change_theme("dark")`).
  - 컬럼: `날짜(YYYY-MM-DD)` / `항공비(성인)` / `알선수익`. 초기 빈 행 12개.
- **툴바**: `+ 행 추가`, `− 선택 행 삭제`, `전체 지우기`, `엑셀 불러오기`(기존 엑셀을 그리드로 import — `excel_loader.load_and_validate_fares` 재사용).
- **데이터 흐름 교체**: `read_sheet_data()`가 그리드를 읽어 검증 후 `[{date, adult_fare, child_fare}]` 생성 → 기존 RPA 루프(`rpa_worker_loop`)·ERP 셀렉터·`config.json`은 **그대로 재사용**.
  - 날짜 정규화 `normalize_date()`: `YYYY-MM-DD`/`YYYY.M.D`/`YYYY/MM/DD`/`YYYYMMDD`/`'... 00:00:00'` 허용. 콤마 요금·빈 알선수익(→0) 처리. 빈 행은 조용히 스킵, 잘못된 행은 오류 목록으로 분리.
- **날짜 필터 유지**: 전체/특정일 이후/특정 날짜/기간 범위. 그리드 데이터에 콘솔 출력 없이 적용(`_apply_date_filter`). 셀 편집 시 `<<SheetModified>>` 이벤트로 대상 건수 자동 갱신(`refresh_count`).
- **실행 안전장치**: RPA 시작 시 오류 행 있으면 "건너뛰고 진행할지" 확인. 실행 중에는 그리드 편집/툴바/필터 입력 잠금(`_set_inputs_locked`).
- **테스트 버전 격리**: `APP_VERSION="v2.0.0-test"`, 자동 업데이트 확인 비활성(startup 체크 미호출), 단일 인스턴스 뮤텍스명 분리(`...V2Test...`)로 운영본과 동시 실행 충돌 방지.

#### 2. 요금 입력표를 오른쪽 펼침 패널로 분리 + 붙여넣기 자동 행 확장 (2026-06-01)

사용자 요청 2건 반영.

- **레이아웃 분리**: 요금 입력표를 본 UI 본문에서 빼고 **오른쪽 펼침(드로어) 패널**로 이동.
  - `build_ui`를 좌우 분할로 재구성: `body` 안에 왼쪽 `main_col`(헤더·필터·진행바·로그·컨트롤)과 오른쪽 `side_panel`(요금 입력표).
  - 헤더 우상단에 **`요금 입력표 펼치기 ▶` / `◀ 입력표 접기`** 토글 버튼(초록). `toggle_sheet_panel()`이 패널 pack/pack_forget + 창 너비 전환(접힘 860 ↔ 펼침 1400, 높이 820).
  - 기본은 **접힌 상태**로 시작. 첫 화면 상태표시줄에 펼치기 안내 문구.
- **붙여넣기 자동 행 확장**: 기존엔 표 행 수보다 많이 붙여넣으면 잘렸음. `set_options(paste_can_expand_y=True, paste_can_expand_x=False)`로 **세로(행)만** 자동 확장(컬럼 3개 고정이라 가로 확장은 차단). `<<SheetModified>>` 이벤트로 붙여넣은 행 수만큼 대상 건수 자동 갱신.

#### 3. 엑셀 불러오기 위치 이동 + 실행취소/다시실행 버튼 (2026-06-01)

사용자 요청 반영.

- **엑셀 불러오기 메인 UI로 복귀**: 오른쪽 패널 툴바에 있던 `엑셀 불러오기`를 메인 컬럼(헤더 아래, 날짜 필터 위)으로 이동. v1에서 엑셀 선택이 본문에 있던 자리와 동일한 위치감. 불러오기 성공 시 패널이 접혀 있으면 자동으로 펼쳐 결과를 바로 보이게 함.
- **실행취소/다시실행**: 요금 입력표 패널 툴바 뒤쪽에 `↶ 실행취소`(`sheet.undo()`)·`↷ 다시실행`(`sheet.redo()`) 버튼 추가. 키보드 Ctrl+Z / Ctrl+Shift+Z도 동작(tksheet undo 바인딩). 안내 문구에 단축키 표기.
- RPA 실행 중 입력 잠금 대상(`toolbar_buttons`)에 새 버튼들 포함(총 6개: 엑셀 불러오기 + 행 추가/삭제/지우기 + 실행취소/다시실행).

#### 4. 기능 버튼 줄 재배치 + 셀 사칙연산(`=` 수식) 지원 (2026-06-01)

- **버튼 배치(사용자 목업대로)**: 초록 토글 버튼을 헤더 우상단 → `엑셀 불러오기` 줄(메인 컬럼)로 이동. 헤더에는 `ERP 켜기`만. 한 줄에 `[엑셀 불러오기] 엑셀 파일을…  [직접 입력 하기 ▶] 직접 요금표를 입력합니다.` 형태로 두 기능을 버튼+설명 쌍으로 나란히. 토글 버튼 이름 `직접 입력 하기 ▶` / `◀ 입력표 접기`.
- **셀 사칙연산**: 항공비/알선수익 칸에 `=`로 시작하는 식 입력 시 계산. 예 `=320000+32000` → `352000`.
  - 모듈 함수 `eval_arithmetic()` — `ast` 기반 안전 평가기. 허용 연산은 `+ - * / % **`와 단항 부호뿐. 함수/이름/속성 접근은 차단.
  - 계산은 **읽기(실행) 시점**에만: `read_sheet_data`가 `_coerce_fare()`로 `=` 식을 계산해 RPA에 반영.

#### 5. 수식을 셀에 보존(자동 치환 제거) (2026-06-01)

사용자 요청: 식을 결과 숫자로 덮어쓰지 말고 **셀에 그대로 남겨두고 싶다**(셀을 계산 대상으로 유지).

- 편집/붙여넣기 시 `=` 셀을 결과로 치환하던 라이브 처리 제거. `<<SheetModified>>`는 다시 `refresh_count`만 호출.
- 셀에는 `=320000+32000`이 그대로 보이고, **계산은 실행/카운트 시점에만**(`read_sheet_data`의 `_coerce_fare`). ERP에는 계산된 값(예 352000)이 입력됨.
- 안내 문구에 "식은 그대로 두고 실행할 때 계산된 값이 입력됩니다" 추가.

#### 6. 수식에 칸 참조 지원 (`=앞의열*0.1`) (2026-06-01)

사용자 요청: 단순 사칙연산뿐 아니라 **같은 행의 다른 칸을 참조**해 계산하고 싶다(예: 알선수익 = 항공비의 10%).

- 안전 계산기 `eval_arithmetic(expr, resolver)`에 `ast.Name` 처리 추가. resolver가 참조 이름을 숫자로 풀어준다.
- 행 단위 평가 `_eval_cell(row_cells, col)` + `_name_to_col`:
  - 참조 이름: `앞의열`/`앞열`/`왼칸`, `뒤의열`/`오른칸`, `항공비`/`성인`, `알선수익`/`아동`, `A`·`B`·`C`(0/1/2). `날짜` 칸은 계산 참조 대상에서 제외.
  - 참조 칸이 또 `=`식이면 **재귀 계산**. `_stack`으로 **순환 참조 차단**(무한루프 방지 → 해당 셀 None 처리).
- `read_sheet_data`가 칸별로 `_coerce_fare(row_cells, col)` 호출하도록 변경. 식은 셀에 그대로 보존, 계산은 실행 시점.
- 안내 문구에 칸 참조 예시 추가.

#### 7. 엑셀식 클릭 참조 — 수식 입력줄(formula bar) (2026-06-01)

사용자 요청: 엑셀처럼 **클릭으로 참조 칸을 지정**하고 싶다.

- 셀 표 위에 **수식 입력줄**(`fx` + 편집대상 라벨 + Entry) 추가. tksheet 인셀 에디터는 클릭 시 편집을 종료시켜 클릭참조 구현이 어려우므로, formula bar로 우회.
- 동작(엑셀과 동일): 수식줄에 `=` 입력 → 참조 모드. 이 상태에서 표의 칸을 클릭하면 **편집 대상은 그대로 두고** 그 칸의 참조(`항공비`/`알선수익`/`날짜`)가 커서 위치에 삽입됨. 여러 번 클릭 가능. Enter로 확정, Esc로 취소.
- 클릭 참조는 칸(열) 기준 절대 참조(`항공비`/`알선수익`)를 넣음 → 같은 행 resolver와 일치.

#### 8. 셀에 계산 '결과' 표시 + 수식 보존 + 자동 재계산 (엑셀식) (2026-06-01)

사용자 요청(스크린샷): 알선수익 칸에는 **계산 결과 값**이 나와야 한다(식 텍스트가 아니라).

- 모델 전환: **셀 = 결과 표시 / 식 = 별도 보관(`self.formulas[(r,c)]`)**. 엑셀처럼 셀엔 숫자, 수식줄엔 식.
  - `_sync_formulas_from_cells`: `=`로 시작하는 셀은 수식 등록. 등록된 수식 칸이 마지막 결과와 다른 일반 값으로 바뀌면 **사용자 수동 수정**으로 보고 수식 해제.
  - `_recalc_formulas`: 보관 수식을 계산해 셀에 결과를 써넣음. 참조 연쇄 위해 최대 6회 반복. 계산 불가(빈 참조 등)면 식 텍스트를 그대로 표시.
  - `_load_active_into_fb`: 수식 칸 선택 시 수식줄엔 **식**을, 라벨에 `(수식)` 표시. 일반 칸은 값.
  - `_commit_formula_bar`: 식이면 보관소 등록 후 즉시 재계산(결과 표시), 아니면 해제.
- **트리거 훅 교체**: `<<SheetModified>>` 가상이벤트 대신 `extra_bindings("modified", …)`로 변경. programmatic `set_cell_data`엔 발화하지 않아 재계산 중 재귀가 없음. 항공비를 고치면 의존된 알선수익이 자동 재계산됨.
- 행 삭제 시 `_remap_formulas_after_delete`로 인덱스 재매핑. 전체지우기/엑셀불러오기 시 보관소 초기화. 우클릭 행삽입/삭제는 비활성(행 매핑 보존 위해 툴바 버튼 사용). undo/redo 버튼도 재계산 트리거.

#### 9. 셀 직접 입력 + 수식 더블클릭 전체 적용(fill-down) (2026-06-01)

사용자 요청: ① 수식줄 말고 **셀에서 바로** 수식 입력, ② **수식 셀 더블클릭 → "수식을 전체 적용하시겠습니까?" → 열 끝까지 통일**.

- ① 셀 직접 입력: 빈/일반 칸은 인셀 편집 그대로 → `=항공비*0.1` 입력·Enter 시 `modified` 훅이 수식 등록+재계산 → 셀에 결과 표시.
- ② 더블클릭 전체 적용: `extra_bindings('begin_edit_cell', _on_begin_edit)` 사용. tksheet는 begin_edit 콜백이 **None을 반환하면 편집을 취소**. 그래서 **수식이 든 칸의 편집 시작 시 None 반환 + 전체적용 다이얼로그**를 띄움.
  - `_apply_formula_to_column(col, formula)`: 데이터가 있는 마지막 행까지 그 열에 동일 수식을 넣고 재계산. 참조가 `항공비`/`앞의열` 같은 같은-행 상대참조라 **행마다 자기 줄 값으로 계산**됨.
- 안내 문구 업데이트(셀 직접 입력 / 더블클릭 전체 적용).

#### 10. 인셀 클릭 참조 (엑셀식: 셀에서 = 입력 → 칸 클릭 → 참조 삽입) (2026-06-01)

사용자 요청: 수식줄이 아니라 **셀 편집 중에** `=` 친 뒤 **다른 칸을 클릭**해 참조를 넣고 이어서 `*0.1`을 치고 싶다.

- 구현(빌드 시 1회 후킹):
  - `close_text_editor`를 래핑(`_wrapped_close_text_editor`): **편집기 내용이 `=`로 시작하고 keysym=='FocusOut'이면 닫지 않음**('break'). Enter/Esc/일반편집은 원본 위임.
  - MT 캔버스 bindtags 맨 앞에 커스텀 태그를 넣고 `<ButtonPress-1>`을 `_incell_ref_click`에 바인딩(우선 실행). 편집 중('='로 시작)이면 `identify_column`으로 클릭한 칸을 찾아 참조명(항공비/알선수익/날짜)을 편집기 커서에 삽입 후 재포커스, `'break'`로 b1_press(선택/종료) 차단.
- 결과 동작: 빈 칸 더블클릭 → `=` 입력 → 항공비 칸 클릭(=항공비) → `*0.1` 입력 → Enter → 셀에 결과. 식은 `self.formulas`에 보관·자동재계산은 기존대로.
- 안내 문구 갱신(셀 더블클릭→= →칸클릭→Enter).

#### 11. 요금 업데이트 7개 전체 필드 확장 및 RPA 연동 완료 (2026-06-01)

기존 2개(항공비, 알선수익) 요금 필드만 수정 및 RPA 주입하던 동작을 전체 7개 요금 필드(성인 항공비/호텔비/지상비/여행경비/알선수익, 소아요금, 유아요금)로 확장 및 RPA 연동을 완료하였습니다.

- **config.json 업데이트**: selectors 맵에 성인 5개 요금 필드 및 소아/유아 요금 필드 CSS 셀렉터 7종 설정 완료.
- **excel_loader.py 업데이트**: 8개 열(날짜 + 7개 요금) 전체를 읽어와 딕셔너리에 바인딩하고, 빈 요금은 기본값 `0`으로 채우는 파싱 안전장치를 구현하였습니다.
- **gui_v2.py 업데이트**:
  - UI 8개 열 확장 (헤더 한글명 반영: 날짜, 항공비, 호텔비, 지상비, 여행경비, 알선수익, 소아, 유아).
  - 수식 엔진/참조 식별자(`_name_to_col`)에 호텔비, 지상비, 여행경비, 소아, 유아 한글 컬럼 명칭 및 문자열 A~H 매핑을 추가하였습니다.
  - RPA 주입 루프(`rpa_worker_loop`)에서 7개 요금을 순차적으로 팝업 input 필드에 채워 넣도록 주입 매핑 로직을 확장하였습니다.
  - 그리드 헤더에 표시되던 괄호 텍스트(예: (성인), (YYYY-MM-DD) 등)를 모두 제거하여 가독성을 개선하였습니다.
  - 가로 스크롤바가 생기지 않고 전체 7종 요금이 한눈에 들어오도록 우측 드로어 패널 너비를 750px로 확장하고, 8개 열의 너비(날짜 100px, 각 요금 80px)를 화면에 맞게 최적화하였습니다.
- **실행 배치 파일 제공**: 가상환경 Python으로 스크립트를 기동하는 [run_gui_v2.bat](file:///g:/내%20드라이브/안티그래비티/erp_updater/run_gui_v2.bat)를 생성하였습니다. 한글 인코딩 문제로 인한 cmd.exe의 구문 해석 및 오작동을 방지하기 위해 순수 ASCII 영문 구문으로 간소화하여 보완했습니다.

#### 12. 셀레늄 기동 네트워크 Hanging 이슈 수정 (2026-06-01)

RPA 요금수정 시작 시 `ChromeDriverManager().install()`이 드라이버 정보를 조회하기 위해 외부 네트워크 요청을 보낼 때 지연/응답 대기로 인해 "ERP에 연결하고 있어요..." 화면에서 무한 대기(Hanging)하는 현상을 식별 및 조치했습니다.

- **조치 사항**: 외부 `ChromeDriverManager` 의존성 호출 코드를 제거하고, 가상환경에 설치된 셀레늄 v4.44.0의 내장 **Selenium Manager**(`webdriver.Chrome(options=options)`)가 로컬 크롬 버전에 맞춰 드라이버를 직접 핸들링하도록 개선했습니다.
- **결과**: 디버그 브라우저 attach 대기 시간이 완전히 사라지고 즉시 연결이 성립되는 것을 검증했습니다.

#### 13. 요금 업데이트 팝업 실제 input ID 매핑 보완 (2026-06-01)

RPA 작동 시 요금 팝업창에서 `#addHotel01` 요소를 찾지 못하는 예외(`Unable to locate element`)가 발생하여, 실시간으로 ERP 팝업 내 DOM을 정밀 분석했습니다.

- **분석 결과**: 7대 요금 입력 필드의 실제 ID 체계가 규칙적이지 않고 개별 매핑되어 있음을 확인했습니다.
  - 성인 항공비: `addAir01`
  - 성인 호텔비: `addHotel11`
  - 성인 지상비: `addLand21`
  - 성인 여행경비: `addExpense40`
  - 성인 알선수익: `addProfit41`
  - 소아 요금: `addChild90`
  - 유아 요금: `addInfant91`
- **조치 사항**: `config.json`의 셀렉터 정보를 실제 매핑된 ID로 일괄 보완 및 업데이트했습니다.

#### 14. 요금 입력표 데이터 엑셀 내보내기 기능 추가 (2026-06-01)

현재 입력표 그리드에 작성된 요금 데이터를 백업하거나 외부와 공유할 수 있도록 엑셀 다운로드 기능을 추가했습니다.

- **조치 사항**:
  - `gui_v2.py`에 `pandas`를 연동하여 그리드에 표시되고 있는 모든 요금 데이터(수식 결과값 포함)를 정제하여 `.xlsx` 형식으로 내보내는 `export_sheet_to_excel` 함수를 정의했습니다.
  - 저장할 때 파일 대화상자(`asksaveasfilename`)가 열려 사용자가 저장 위치를 지정할 수 있게 했습니다.
  - 우측 요금 입력표 드로어 패널 툴바의 '전체 지우기' 옆에 **'엑셀로 다운받기'** 파란색 버튼을 추가하여 쉽게 접근할 수 있게 배치했습니다.

#### 15. 수식 더블클릭 전체 적용(fill-down) 실행취소(Ctrl+Z) 연동 완료 (2026-06-01)

수식 셀을 더블클릭하여 열 전체에 수식을 일괄 적용(fill-down)했을 때, 이 동작이 '실행취소' 버튼 및 'Ctrl+Z' 단축키에 기록되지 않는 문제를 해결했습니다.

- **조치 사항**:
  - `_apply_formula_to_column` 함수 내부에서 수식을 전체 적용하기 직전, 적용 대상 열의 이전 셀 값과 수식을 딕셔너리로 백업해둡니다.
  - 백업된 이전 값 데이터를 `tksheet` 내부의 실행취소 저장소(`self.sheet.MT.undo_stack`)에 `edit_table` 이벤트 묶음으로 포맷팅하여 주입했습니다.
  - 실행취소(Undo) 시 셀 값이 이전으로 복구되면, `_sync_formulas_from_cells` 함수가 이를 감지하여 수식 보관소(`self.formulas`)의 수식까지 자동으로 복구(삭제)하도록 연동했습니다.
- **결과**: 사용자가 더블클릭으로 수식을 일괄 주입한 후, 단 한 번의 실행취소 조작으로 채우기 이전의 셀 상태와 수식 정보를 온전히 되돌릴 수 있게 되었습니다.

#### 16. 요금 입력표 툴바 정렬 및 팁 라벨 제거 (2026-06-01)

요금 입력표 화면을 보다 직관적이고 깔끔하게 구성하기 위해 레이아웃 변경을 진행했습니다.

- **조치 사항**:
  - **버튼명 텍스트 변경**: 특수기호/이모지를 제거하고 텍스트를 간소화했습니다.
    - `+ 행 추가` → `행 추가`
    - `− 선택 행 삭제` → `삭제`
    - `↶ 실행취소` → `실행취소`
    - `↷ 다시실행` → `다시실행`
    - `엑셀 다운받기` → `엑셀로 다운받기`
  - **버튼 배치 정렬**: `행 추가` / `삭제` / `전체 지우기` / `실행취소` / `다시실행` / `엑셀로 다운받기` 순으로 나란히 재배치했습니다.
  - **안내문 삭제**: 테이블 위쪽에 표시되던 긴 두 줄짜리 설명 팁(tip) 라벨을 제거하여 화면을 깔끔하게 다듬었습니다.

#### 17. 요금 입력표 툴바 행 추가 및 삭제 버튼 제거 (2026-06-01)

그리드 입력표에서 잘 사용되지 않는 행 관련 기능 버튼을 제외하여 툴바를 더욱 간소화했습니다.

- **조치 사항**:
  - 툴바 목록에서 **`행 추가`** 및 **`삭제`** 버튼을 완전히 제거했습니다.
  - 결과적으로 툴바에는 **`전체 지우기`**, **`실행취소`**, **`다시실행`**, **`엑셀로 다운받기`** 4개의 주요 액션 버튼만 깔끔하게 노출되도록 정리했습니다.

#### 18. 엑셀 양식 다운받기 기능 추가 및 요금 조회 버튼 재배치 (2026-06-01)

사용자 편의와 레이아웃 조화를 위해 메인 화면의 버튼 구성과 링크를 재배치했습니다.

- **조치 사항**:
  - **버튼 이동**: 원래 메인 타이틀 아래에 단독으로 배치되던 '요금조회 사이트가기 ↗' 버튼을 상단 타이틀 행의 **버전 정보 배지 우측 옆**으로 일렬 배치하여 시각적으로 더 정돈되게 하였습니다.
  - **양식 다운받기 버튼 추가**: 사이트 가기 버튼이 있던 빈 공간에 주황색 **`엑셀 양식 다운받기 ↗`** 버튼을 배치했습니다.
  - **다운로드 로직 구현**: `download_excel_template` 함수를 통해 사용자가 해당 버튼을 누르면 입력표와 완벽히 호환되는 8개 컬럼(`날짜`, `항공비`, `호텔비`, `지상비`, `여행경비`, `알선수익`, `소아요금`, `유아요금`)의 헤더만 명시된 빈 엑셀 템플릿 파일이 저장되도록 구현했습니다.

#### 19. 엑셀 양식 및 내역 다운로드 시 기본 파일명 제공 (2026-06-01)

엑셀 양식을 내려받거나 입력표를 엑셀로 백업할 때, 파일명 칸이 비어 있어 수동으로 입력해야 하는 불편함을 개선했습니다.

- **조치 사항**:
  - **양식 다운로드 기본명**: `download_excel_template` 함수 내 파일 대화상자(`asksaveasfilename`) 호출 시 `initialfile="ERP 요금수정 양식"`을 추가하여 대화상자가 열릴 때 자동으로 파일명이 입력되도록 조치했습니다.
  - **입력표 내보내기 기본명**: `export_sheet_to_excel` 함수 내에도 `initialfile="ERP 요금수정 내역"`을 함께 추가하여 저장 편의성을 높였습니다.

#### 20. 상단 설명 라벨 제거 (2026-06-01)

사용자 피드백에 따라 메인 화면의 군더더기를 최소화하기 위해 상단의 설명 글을 제거했습니다.

- **조치 사항**:
  - 메인 타이틀과 버전 배지 바로 아래에 위치하던 설명용 라벨(`desc_label`)과 관련된 생성 및 레이아웃 패킹 코드를 삭제했습니다.

#### 21. 상단 버튼 호버(마우스 오버) 스타일 개선 (2026-06-01)

상단 버튼에 마우스가 호버되었을 때 배경색과 글자색이 동일해져 글자가 보이지 않던 시각적 버그를 수정했습니다.

- **조치 사항**:
  - `_add_hover` 함수가 배경색(`bg`)뿐 아니라 글자색(`fg`)도 옵션으로 변경할 수 있도록 `normal_fg`와 `hover_fg` 매개변수를 추가했습니다.
  - **엑셀 양식 다운받기 ↗** 버튼: 호버 시 배경색은 주황색(`self.accent_orange`), 글자색은 하얀색(`white`)으로 선명하게 전환되도록 매핑했습니다. 클릭 시에도 동일하게 흰색 글자가 유지되도록 `activebackground`/`activeforeground` 설정을 조치했습니다.
  - **요금조회 사이트가기 ↗** 버튼: 호버 시 배경색은 파란색(`self.accent_color`), 글자색은 하얀색(`white`)으로 표시되도록 조치했습니다.

#### 22. 기능 버튼 옆 보조 설명 문구 제거 (2026-06-01)

메인 화면의 여백을 더 확보하고 직관적인 레이아웃을 구현하기 위해 기능 버튼 우측의 설명 라벨을 제거했습니다.

- **조치 사항**:
  - '엑셀 불러오기' 버튼 우측의 라벨과 '직접 입력 하기 ▶' 버튼 우측의 라벨을 레이아웃 구성에서 제거했습니다.
  - 두 버튼이 설명글 없이 나란히 밀착 배치되어 화면이 더욱 깔끔해졌습니다.

#### 23. 요금조회 버튼 일단 제거 (2026-06-01)

사용자 요청에 따라 요금조회(GDS 연동) 기능 및 버튼을 GUI에서 제거했습니다.

- **조치 사항**:
  - 직접 입력 하기 버튼 옆에 배치했던 `요금조회 ↗` 버튼 및 관련 마우스 호버 설정을 제거했습니다.
  - `open_gds_site` 함수와 `GDS_SITE_URL` 상수 및 webbrowser 연동 로직을 정리했습니다.

#### 24. 기존 3열 엑셀 포맷 호환성 예외 처리 추가 (2026-06-01)

사용자가 3개 열(날짜, 항공비, 알선수익)만 있는 기존 v1 형식의 엑셀 파일을 로딩할 때, 요금 순서 매핑 오류로 알선수익 값이 호텔비 칸에 입력되고 알선수익이 0이 되는 현상을 해결했습니다.

- **조치 사항**:
  - `excel_loader.py` 내의 `load_and_validate_fares` 함수에서 열의 개수를 체크하는 로직을 추가했습니다.
  - 열 개수가 3개인 경우, 3번째 열의 값을 호텔비(`raw_adult_hotel`)가 아닌 알선수익(`raw_adult_profit`)에 올바르게 할당하고, 나머지 미포함 요금(호텔비, 지상비, 여행경비, 소아, 유아)은 모두 `0`으로 자동 패딩하도록 조치했습니다.

#### 25. 엑셀 로드 시 그리드 행 크기(높이) 및 스크롤바 미갱신 오류 해결 (2026-06-01)

엑셀 파일로 많은 행(예: 267개 행)을 로드했음에도, 표 위젯에는 초기 상태인 12개 행만 보이고 스크롤바조차 나타나지 않던 버그를 수정했습니다.

- **조치 사항**:
  - `gui_v2.py` 내의 `set_sheet_data` 호출부에서 `reset_row_positions=False`로 되어 있던 설정을 모두 `reset_row_positions=True`로 수정했습니다.
  - 이로써 엑셀을 로드하거나 전체를 지우는 등 행 개수가 대폭 늘어나거나 줄어들 때, `tksheet` 위젯이 행의 실제 높이 정보와 스크롤바 가용 영역을 정확하게 자동 재계산하여 올바르게 렌더링되도록 개선했습니다.

#### 26. v2.0.0 정식 버전 릴리즈 빌드 완료 (2026-06-01)

- **gui_v2.py의 gui.py 덮어쓰기 및 백업**: v2 개발본 `gui_v2.py` 검증 완료 후 기존 v1.1.19 운영본 `gui.py`를 `gui_v1_backup.py`로 안전하게 백업 및 보존하고, `gui_v2.py`를 `gui.py` 이름으로 배포 진입점 설정 완료.
- **버전 설정 상향**: `gui.py`의 `APP_VERSION = "v2.0.0"`으로 변경하고, `build_setup.ps1`의 `$Version = "v2.0.0"`으로 변경 완료.
- **빌드 및 패키징 완료**: `build_setup.ps1` 스크립트를 통해 PyInstaller onedir 빌드 및 `NaeilERPUpdater_Setup_v2.0.0.exe` 설치본 빌드 완료.
- **매니페스트 자동 갱신**: 빌드 완료 후 `latest.json`에 `v2.0.0` 정보 및 빌드된 셋업 파일의 실제 SHA256 해시값 자동 연동 및 기록 완료.
- **작업일지 통합**: `작업일지_v2.md`를 `작업일지.md`에 완전 통합하고 `작업일지_v2.md`는 삭제 처리.

---

## 릴리스/자동업데이트 운영 원칙

앞으로 이 프로젝트에서 사용자에게 배포될 코드 변경, 버그 수정, 설정 변경을 처리할 때는 사용자가 별도로 "빌드", "커밋", "배포", "자동 업데이트 연동"을 말하지 않아도 아래 절차까지 완료하는 것을 기본 작업 완료 조건으로 본다.

### 기본 원칙

- 일반 사용자는 설치된 앱의 자동 업데이트로 새 버전을 받아야 한다.
- 자동 업데이트는 `update_client.is_newer_version(latest.version, APP_VERSION)` 비교를 사용하므로, 이미 배포된 사용자가 업데이트를 받으려면 반드시 현재 앱 버전보다 높은 버전으로 올려야 한다.
- 같은 버전 번호로 설치본만 다시 바꾸면 해당 버전을 이미 설치한 사용자는 자동 업데이트를 다시 받지 않는다. 같은 버전 재업로드는 사용자가 명시적으로 요청했거나, 아직 그 버전이 실사용자에게 퍼지기 전의 긴급 교체에만 사용한다.
- 배포 산출물과 GitHub 원격 저장소의 소스가 서로 맞아야 한다. 설치본을 만든 로컬 코드 변경은 `latest.json`만이 아니라 관련 소스와 빌드 설정까지 함께 커밋/푸시해야 한다.

### 필수 배포 체크리스트

1. 버전 상향
   - `gui.py`의 `APP_VERSION`
   - `build_setup.ps1`의 `$Version`
   - `packaging/setup_installer.py`의 `APP_VERSION`
   - 필요하면 `latest.json`의 notes

2. 빌드
   - `powershell -ExecutionPolicy Bypass -File .\build_setup.ps1`
   - 새 설치본 예: `release\NaeilERPUpdater_Setup_vX.Y.Z.exe`

3. GitHub 릴리스 및 매니페스트 배포
   - `powershell -ExecutionPolicy Bypass -File .\publish_release.ps1 -SkipBuild`
   - 기존 릴리스 자산을 덮어쓴 경우 GitHub/CDN 전파 지연으로 예전 파일 해시가 잠깐 내려올 수 있으므로, 실제 다운로드 URL의 SHA256과 `latest.json` SHA256이 같아질 때까지 재확인한다.

4. 검증
   - GitHub 릴리스 자산 digest 확인
   - GitHub `main`의 `latest.json` 확인
   - `update_client.fetch_available_update(latest_url, 이전버전)`가 새 버전을 반환하는지 확인
   - 실제 다운로드 URL 파일 SHA256과 `latest.json.sha256` 일치 확인

5. 커밋/푸시
   - 소스 변경, 빌드 설정 변경, 설치기 변경, `latest.json`, 작업일지 변경을 함께 커밋/푸시한다.
   - `publish_release.ps1`은 기본적으로 `latest.json`만 커밋하므로, 코드 변경 커밋은 별도로 챙긴다.

6. 사용 매뉴얼 최신화 및 이미지 동기화 (필수)
   - 프로그램 GUI 레이아웃이나 기능(예: 버튼 추가, 테이블 필드 조작 등)이 변경되는 경우, 반드시 가이드 매뉴얼([사용가이드.html](file:///g:/내%20드라이브/안티그래비티/erp_updater/사용가이드.html) 및 [Naeil-ERP-Updater-사용-매뉴얼.md](file:///g:/내%20드라이브/안티그래비티/Wiki/Entities/Naeil-ERP-Updater-사용-매뉴얼.md))을 최신 사양에 맞춰 수정 및 보정한다.
   - 필요 시 실제 프로그램을 기동해 변경된 GUI 화면을 캡처하고, `assets/gui_collapsed.png` 및 `assets/gui_expanded.png`를 교체한다. 또한 위키 폴더 `Wiki/Entities/` 아래에도 이미지를 동일하게 복사/동기화한 뒤 변경 내용(코드 및 리소스 이미지)을 레포지토리에 푸시하여 GitHub Pages 사이트와 로컬 위키 모두 최신 상태를 유지하게 한다.


### v1.1.19 -> v2.0.1 자동 업데이트 관련 메모

- 2026-06-01 기준 기존 사용자들은 주로 `v1.1.19`를 사용 중이다.
- `main/latest.json`은 `v2.0.1` 설치본을 가리키도록 설정되어 있어, `v1.1.19` 사용자는 자동 업데이트 확인 시 `v2.0.1`을 받아야 한다.
- `v2.0.1` 설치본에는 v1 설정 파일을 유지한 업그레이드에서 누락될 수 있는 v2 요금 입력 셀렉터를 설치기와 앱 실행 시점에 보정하는 로직이 포함되어야 한다.

---

## 27. RPA 대기시간 보수적 최적화안 (2026-06-01)

사용자 실행 영상 기준, ERP 자체 조회/저장 로딩은 유지되지만 그리드가 이미 준비된 뒤에도 고정 대기(`sleep`)가 누적되어 400건 대량 실행 시 전체 시간이 길어질 수 있음을 확인했다.

- **목표**: 시스템 안정성을 최우선으로 유지하면서 건당 약 2초 안팎 단축.
- **유지한 안전장치**:
  - `timeout: 25` 최대 대기 시간은 그대로 유지.
  - 조회 결과는 여전히 대상 날짜 단일 집합이 확인될 때만 성공 처리.
  - 요금수정 팝업은 고정 대기 대신 `WebDriverWait`로 실제 입력칸 표시를 기다림.
  - 저장 후 그리드는 차단막/로딩 마스크 상태를 계속 확인하며, 준비 전이면 계속 대기.
- **변경한 기본 타이밍**:
  - `erp_poll_interval: 0.25` 추가. 조회/저장 상태 감지를 1초 또는 0.5초 단위가 아닌 0.25초 단위로 확인.
  - `erp_short_pause: 0.2` 추가. 체크/입력/얼럿 후 최소 안정화 대기.
  - `erp_grid_ready_stable_seconds: 1.0` 추가. 저장 후 차단막이 관측되지 않는 경우 1초 동안 안정 상태를 확인한 뒤 다음 건 진행.
- **주의**:
  - 이 조정은 배포 전 10~20건 정도 LIVE 샘플 실행으로 ERP 환경에서 팝업/저장 누락이 없는지 확인하는 것이 좋다.
  - 문제가 없으면 다음 배포 버전은 `v2.0.2` 이상으로 올려 자동 업데이트 대상이 되게 해야 한다.

## 28. v2.0.2 대기시간 최적화 테스트 완료 및 배포 준비 (2026-06-01)

사용자 LIVE 테스트에서 대기시간 보수적 최적화안이 문제 없이 완료되었음을 확인했다. 이에 따라 해당 변경을 `v2.0.2`로 배포한다.

- **버전 상향**:
  - `gui.py` / `gui_v2.py`: `APP_VERSION = "v2.0.2"`
  - `build_setup.ps1`: `$Version = "v2.0.2"`
  - `packaging/setup_installer.py`: `APP_VERSION = "v2.0.2"`
- **배포 노트**:
  - ERP 조회/저장 상태 확인 간격을 촘촘하게 줄여 대량 실행 시간을 단축.
  - 기존 `timeout: 25` 최대 대기는 유지하고 준비 상태 감지만 빠르게 수행해 안정성을 우선.
- **배포 원칙**:
  - `v2.0.1` 사용자가 자동 업데이트를 받으려면 반드시 `latest.json`을 `v2.0.2`로 갱신해야 한다.
  - 빌드 후 `publish_release.ps1 -SkipBuild` 실행 및 GitHub 자산 SHA / `main/latest.json` SHA / 실제 다운로드 SHA 일치를 확인한다.

## 29. v2.0.2 자동 업데이트 미표시 원인 확인 (2026-06-01)

일부 PC에서 `v2.0.2` 자동 업데이트 알림이 뜨지 않는 현상을 확인했다.

- **원인**:
  - 해당 PC에 설치된 `Uninstall.exe` SHA256이 `70E2E088...`로 확인됨.
  - 이는 `v2.0.1` 최종 교체본(`957D5DB8...`)이 아니라, 잠깐 배포되었던 초기 `v2.0.1` 설치본이다.
  - 초기 `v2.0.1`은 v2 GUI로 전환되는 과정에서 자동 업데이트 확인 루틴이 빠져 있어 `latest.json` 자체를 조회하지 않는다.
- **영향 범위**:
  - `v1.1.19` 사용자는 현재 `latest.json`이 `v2.0.2`를 가리키므로 바로 `v2.0.2`로 자동 업데이트 가능.
  - 최종 `v2.0.1` 교체본(`957D5DB8...`)을 설치한 사용자는 `v2.0.2` 자동 업데이트 가능.
  - 초기 `v2.0.1`(`70E2E088...`)을 이미 설치한 사용자는 자동 업데이트 루틴이 없으므로 한 번만 `v2.0.2` 설치본을 수동 실행해야 한다.
- **조치**:
  - 해당 PC는 `NaeilERPUpdater_Setup_v2.0.2.exe --silent --install-dir ... --no-shortcuts` 방식으로 덮어 설치 완료.
  - 설치 후 레지스트리 `DisplayVersion = v2.0.2`, 설치본/제거기 SHA256 `6F9B6FDAD845D6EE6D870CDECCD548D63A55735552B2D95873BE275334FC6F05` 확인.

## 30. v2.0.3 요금 입력 자릿수 뒤섞임 버그 수정 (2026-06-01)

### 증상
사용자 EXE 실행 중 항공비/알선수익이 자릿수만 뒤섞여 저장됨.
- 항공비: 입력 의도 `271750` → 저장 `271507`
- 알선수익: 입력 의도 `27175` → 저장 `27157`
- 두 값 모두 들어간 숫자 집합은 동일하고 순서만 어긋남.

### 근본 원인
ERP 금액칸(`#addAir01`, `#addProfit41` 등)에는 입력할 때마다 천 단위 콤마를 다시 찍는 JS 핸들러가 있다. 기존 코드(`rpa_worker_loop`)는 `inp.send_keys(val)`로 한 글자씩 타이핑했는데, 네 자리째에서 콤마가 삽입되는 순간 커서 위치가 튀어 뒤이어 입력되는 숫자가 엉뚱한 자리에 들어가 자릿수가 섞였다. 빠른 PC에서 더 잘 재현된다.

### 수정 (`gui.py`, `gui_v2.py` 동일 적용)
- **입력 방식 교체**: `value=''` 후 `send_keys` 한 글자 입력을 폐기하고, `execute_script`로 값을 한 번에 주입한 뒤 `input/keyup/change/blur` 이벤트를 발생시켜 콤마 포매터가 완성된 숫자에 한 번만 동작하도록 변경.
- **저장 전 검증 추가**: 저장 직전 각 입력칸 값을 다시 읽어 숫자만 추출해 의도값과 대조. 하나라도 다르면 모달을 닫고 그 날짜는 저장하지 않고 `FAIL`로 기록 후 다음 날짜로 진행(잘못된 값 저장 차단).

### 버전/빌드/배포
- 버전 상향 v2.0.2 → **v2.0.3**: `gui.py`, `gui_v2.py`, `build_setup.ps1`(`$Version`), `packaging/setup_installer.py`(`APP_VERSION`).
- `build_setup.ps1` onedir 빌드 + 설치본 생성 완료(exit 0): `release\NaeilERPUpdater_Setup_v2.0.3.exe`(약 64.5MB).
- 설치본 SHA256: `D332396810C288C6AE4E2F6FFF864AB93E7E63711339CD335B48BB79C4B95CBE`
- `latest.json` notes를 이번 수정 내용으로 갱신(version/download_url/sha256은 배포 스크립트가 업로드 자산 기준으로 동기화).

### 검증
- `gui.py`, `gui_v2.py`, `packaging/setup_installer.py` 문법 검사 통과.
- 배포 전 LIVE 10~20건 샘플 실행으로 콤마가 끼는 네 자리 이상 금액(예: `271750`, `27175`)이 정확히 저장되는지 확인 권장.

---

## 31. v2.0.4 가이드 매뉴얼 웹 연동 및 배포 (2026-06-01)

### 변경 사항
- **HTML 사용 가이드 제작**:
  - `g:\내 드라이브\안티그래비티\erp_updater\사용가이드.html` 단독형 고품질 HTML 매뉴얼 페이지 제작.
  - 카테고리별로 접을 수 있는 FAQ 아코디언 메뉴와 좌측 목차 스크롤 하이라이팅 탑재.
  - 사용자 피드백에 따라 푸터 영역의 카피라이트 및 기술지원 정보 텍스트를 제거.
- **실제 프로그램 스크린샷 연동**:
  - 실제 파이썬 가상환경에서 프로그램을 구동하여 직접 입력 패널을 켠 화면을 테두리 오프셋을 포함한 깔끔한 스크린샷(`gui_collapsed.png`, `gui_expanded.png`)으로 자동 캡처하여 `assets/` 및 위키 디렉토리에 저장.
  - HTML 매뉴얼 내의 이미지를 깃허브 페이지에 업로드하여 정상 노출되도록 상대 경로(`assets/...`)로 구성.
- **GitHub Pages 호스팅 설정**:
  - `KAKNAKIAK/naeil-erp-updater` 레포지토리에 GitHub Pages 설정을 `main` 브랜치, 루트 `/` 경로로 활성화하여 `https://kaknakiak.github.io/naeil-erp-updater/사용가이드.html` 주소로 연동 완료.
- **GUI 헤더 버튼 탑재**:
  - `gui.py` 상단 헤더 영역의 버전 정보 배지 옆에 주황색 `가이드 매뉴얼 보기 ↗` 버튼을 추가. 클릭 시 기본 웹 브라우저를 통해 호스팅된 GitHub Pages 사용 가이드 링크가 열리도록 연동.

### 버전/빌드/배포
- 버전 상향 v2.0.3 → **v2.0.4**: `gui.py`, `build_setup.ps1`(`$Version`), `packaging/setup_installer.py`(`APP_VERSION`).
- `build_setup.ps1` onedir 빌드 + 설치본 생성 완료(exit 0): `release\NaeilERPUpdater_Setup_v2.0.4.exe` (설치본 SHA256: `090265F75CA843F2F81654796A782B39EE490B534C3B9F8E01EFC11D8687E2D6`).
- `publish_release.ps1 -SkipBuild` 실행을 통해 GitHub 릴리즈 `v2.0.4` 생성, 설치본 업로드, 해시 검증 및 `latest.json` 매니페스트 동기화 완료.
- 변경된 모든 소스 코드와 자산 파일(`gui_collapsed.png`, `gui_expanded.png`, `사용가이드.html`) 및 작업일지를 GitHub 원격 레포지토리에 `push` 완료.

---

## 32. 매뉴얼에서 이중 안전장치 및 검증 시스템 섹션 제거 (2026-06-01)

### 변경 사항
- **이중 안전장치 섹션 제거**:
  - `사용가이드.html` 및 `Naeil-ERP-Updater-사용-매뉴얼.md` 파일에서 `6. 이중 안전장치 및 검증 시스템` 섹션 전체 삭제.
  - 보안 및 내부 시스템 작동 로직의 문서 노출을 줄이기 위한 보안성 조치.
- **FAQ 및 목차 번호 재조정**:
  - 기존 7번이었던 FAQ 섹션을 6번으로 상향 조정 (마크다운).
  - `사용가이드.html` 목차(사이드바)에서도 `5. 이중 안전장치 기능`을 삭제하고 `6. FAQ / 트러블슈팅`을 `5. FAQ / 트러블슈팅`으로 한 단계씩 앞당김.
  - 스크롤 하이라이팅 스크립트(`navItems`)에서 `safety` 항목 바인딩 제거 및 동기화.

### 프로그램 업데이트 시 매뉴얼/캡처 최신화 대응 원칙
- **원칙**: 향후 프로그램에 신규 UI 요소나 로직 변경 등이 적용되어 업데이트를 배포할 때, 사용자용 매뉴얼도 그에 걸맞게 최신화해야 함.
- **조치 사항**:
  - 변경 사항에 맞는 가이드 문구 수정 (`사용가이드.html` 및 `Naeil-ERP-Updater-사용-매뉴얼.md` 동시 반영).
  - 필요할 경우 로컬에서 직접 프로그램을 기동하여 신규 UI 화면을 캡처한 뒤 `gui_collapsed.png` 및 `gui_expanded.png` (또는 신규 이미지)로 대체.
  - `assets/` 디렉토리와 Obsidian Vault의 `Wiki/Entities/` 아래에 리소스 파일을 동기화하여 커밋 및 푸시 수행 (GitHub Pages 웹 라이브 자동 반영).

---

## 33. v3.0.0 시작일/종료일 분리 기간 모드 작업 설계 및 이관 (2026-06-02)

- **작업 개요**: 기간 입력 모드(시작일/종료일 분리) 동적 8열/9열 제어, 엑셀 파싱 및 자동 감지, RPA 범위 검색 주입 기본 구조 구현 완료.
- **인수인계**: 다음 AI가 수식 모듈의 동적 열 인덱싱 등 잔여 작업을 이어받아 v3.0.0 빌드 및 배포를 완료할 수 있도록 별도의 인수인계 작업일지인 [작업일지_v3.md](file:///g:/내%20드라이브/안티그래비티/erp_updater/작업일지_v3.md)를 생성하여 작업을 이관함.


---

## 34. v3.0.0 기간 모드 완성 + 정식 배포 (2026-06-02)

섹션 33에서 이관한 잔여 작업을 모두 마치고 v3.0.0을 정식 배포했다. 상세 변경 내역은 [작업일지_v3.md](file:///g:/내%20드라이브/안티그래비티/erp_updater/작업일지_v3.md) 참고.

### 코드 변경 (gui.py / excel_loader.py)
- **수식 모듈 동적화**: `_name_to_col`을 인스턴스 메서드로 전환하고 기간 모드 offset(+1) 적용 → 기간 모드(9열)에서 `=항공비+호텔비` 등 이름 참조가 한 칸씩 밀리던 버그 수정. `_ref_name_for_col`은 실시간 헤더(`self.sheet.headers()`) 기반으로 교체. `_eval_cell` 리졸버·`_commit_formula_bar`의 날짜열 판정을 `self.is_date_col()`로 통일. `_apply_formula_to_column` 상태 메시지를 헤더명 기반으로 교체.
- **엑셀 양식 다운로드 선택창**: `download_excel_template`에 라디오 선택창(`_ask_template_format`) 추가 — `단일 날짜 양식` / `기간 입력 양식` + 다운받기/취소. 현재 모드 기본 선택, Enter/Esc 단축키.
- **기간 엑셀 자동 감지 개선**: 헤더 키워드/9열 조건 대신 1·2번째 열의 날짜 비율을 열 개수와 무관하게 검사(신규 `_is_date_like`, 연/월/일 범위 확인으로 8자리 요금 오인 방지). 헤더 없는 7열 기간 파일도 정상 감지.
- **날짜 필터 3종 기간 대응**: `_apply_date_filter`를 종료일까지 보는 겹침 기준으로 변경(이후=`date_end>=기준`, 특정=`date<=지정<=date_end`, 범위=`date<=e and date_end>=s`). 단일 모드는 `date_end==date`라 동작 동일.
- **RPA 종료일 입력 견고화**: ERP 날짜위젯이 시작일 변경 시 종료일을 자동 리셋해 기간 종료일이 누락되던 문제 수정. 시작일 입력 후 change/blur 선발생 → 종료일 후입력 + 표시값 재확인하며 최대 4회 재시도 → 불일치 시 경고 로그(`rpa_worker_loop`). 실기동 1행 스모크 테스트로 종료일 정상 반영 확인.
- 결정 근거(사용자 확정): 실무 기간 파일은 9열 정석 유지(열 매핑 불변), 날짜 필터는 "겹치기만 하면 포함".

### 버전/빌드/배포
- 버전 상향 v2.0.4 → **v3.0.0**: `gui.py`, `build_setup.ps1`(`$Version`), `packaging/setup_installer.py`(`APP_VERSION`).
- `latest.json` notes를 v3.0.0 기능(기간 모드/양식 선택/필터/종료일 안정화) 기준으로 교체.
- `publish_release.ps1`로 재빌드 + GitHub 릴리스 `v3.0.0` 생성/업로드 + latest.json 해시 동기화 + 커밋/푸시 완료(exit 0).
  - 릴리스: https://github.com/KAKNAKIAK/naeil-erp-updater/releases/tag/v3.0.0
  - 설치본 `NaeilERPUpdater_Setup_v3.0.0.exe`(약 64.5MB) SHA256: `732871B4613FD7C1C766FB62F110798EB397554B7FBAAADC25F2B3C6EA2EE118` (로컬=업로드 자산 재다운로드 대조 일치, main latest.json 동기 확인).
  - 소스 커밋 `9338c43`, 매니페스트 동기 커밋 `565a077` → origin/main 푸시 완료.
- 검증: `py_compile`(gui.py / excel_loader.py / setup_installer.py) 통과. 기간 감지(`괌 대한항공 테스트.xlsx` → is_period=True, 시작/종료일 정렬) 및 단일 파일 회귀 없음 확인. 필터 겹침 로직 단위 확인.

### 매뉴얼 최신화 (섹션 32 원칙 준수)
- `사용가이드.html`(GitHub Pages): 버전 v2.0.3 → v3.0.0, 양식 다운로드 설명을 단일/기간 선택창 기준으로 갱신, 기간 입력 모드 카드 추가, 날짜 필터에 `기간 범위 지정` 추가 및 기간 겹침 동작 설명 보강.
- `Wiki/Entities/Naeil-ERP-Updater-사용-매뉴얼.md`(vault 사본): 동일 내용 반영(버전/양식 선택/기간 모드 TIP/필터).
- 참고: 스크린샷(`assets/gui_expanded.png`)은 기간 체크박스가 새로 생긴 UI라 추후 재캡처 권장(현재는 기존 이미지 유지).


