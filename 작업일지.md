# erp_updater 작업일지 (현재 안정판: v5.0.12 / 이전 안정판: v5.0.11)

> 최종 갱신: 2026-07-01
> ✅ v5.0.12: 작업 전환 직후 첫 조회에서 이전 호텔의 hidden `hotelSeq`가 남아 다른 호텔로 조회되던 문제를 수정.
> ✅ v5.0.11: 호텔 DB 선택값은 hidden `hotelSeq` 강검증이 아니라 ERP 그리드의 호텔명 텍스트 일치 기준으로 검증하도록 수정.
> ✅ v5.0.10: 호텔명을 텍스트로 입력한 작업은 ERP 자동매칭 `hotelSeq`를 신뢰하고, DB 후보 직접 선택 작업만 `hotelSeq` 불일치 시 중단하도록 수정.
> ✅ v5.0.9: 작업목록 실행에서 첫 작업의 `hotelSeq`가 뒤쪽 호텔 작업에 fallback되어 매칭 검증을 깨뜨리던 문제를 수정.
> ✅ v5.0.8: 단일 입력표 실행 시 프로그램 조건이 비어 있으면 ERP 화면의 요금구분/항공사/호텔명 조건을 가져올지 확인하는 안전장치를 반영.
> ✅ v5.0.7: 호텔명 ERP DB 자동검색/후보 선택, 검색 결과 없음 표시, 선택 hotelSeq 작업목록 저장 및 실행 전 검증을 반영.
> ✅ v5.0.6: 작업 목록 수동 추가 조건을 `요금구분/항공사코드/호텔명 중 1개 이상`으로 완화하고, 추가 전 확인창을 추가.
> ✅ v5.0.5: 다중 상품 작업 목록/큐, 작업별 진행상황, 조건열 엑셀 불러오기, 호텔명 조건 필터, 진행구분 입력 필드 제거를 반영.
> ✅ v5.0.4: 요금구분 검색 조건, 예약마감 진행구분 자동 정보일괄수정, 전체 예약마감 페이지 요금 업데이트 스킵, Firestore 스냅샷 우선 로드를 반영.
> ✅ v5.0.3: `전체 지우기` 시 항공사코드 선택값을 `_선택_`으로 초기화하고, 항공사코드 옵션 목록을 다시 새로고침. 호텔명 필드 추가안은 릴리스 범위에서 제외.
> ✅ v5.0.2: v5.0.1 빌드 중 반영된 최신 소스 상태 기준 재빌드·배포 패치.
> ✅ v5.0.1: 운임/시즌 DB 로드 안정화, 항공사코드 전달, TOPAS AC1 귀국편 110% 제안, 입력표 undo/redo 확장, 디버그 Chrome 이전 탭 복원 방지.
> ✅ v5.0.0: TOPAS 원문 수집 → 박수별 왕복 요금 계산 → ERP 요금수정 입력표 전달까지 이어지는 V5 정식 워크플로우 반영.
> ✅ v4.0.2: TOPAS 항공사-only 조회(`/AZE`)에서 AC1 응답을 0개로 잘못 판단하던 파서 버그 수정.
> ✅ v4.0.1: TOPAS AC1 전송 시 비활성/숨김 입력창을 잡아 `element not interactable`로 중단되던 버그 수정.
> ✅ v4.0.0: 요금수정/토파스조회 탭 분리 + TOPAS AC1 10건 묶음 연속 조회 + 원문 전체 복사 팝업.
> ✅ v3.0.3: 페이지 저장 일시오류(408 등) 자동 재시도 + 부분 실패(어느 페이지) 정직 보고 + 페이지 간 텀.
> ✅ v3.0.2: 조회 매칭 false negative(특정 날짜 누락) 수정 + 실패 시 팝업 안내 추가.
> ✅ v3.0.1: 페이징(500건 초과 다중 페이지) 처리 추가.
> 📁 v2.x 이전의 상세 작업 이력은 [작업일지_archive.md](legacy_archive_20260618/legacy_gui_and_docs/작업일지_archive.md)로 분리 보관했습니다. (이 문서에는 현재 상태·구조·셀렉터·최신 변경만 유지)

## v5.0.12 릴리즈 — 작업 전환 첫 조회 stale hotelSeq 수정 (2026-07-01)

- 배경:
  - 사용자 로그에서 `두짓비치 리조트` 작업 첫 행이 `ERP hotelSeq=7904`로 조회되어 `괌 두짓타니 리조트` 행을 가져오는 문제가 확인됐다.
  - 같은 실행에서 뒤쪽 행은 `ERP hotelSeq=2264`로 정상 처리됐고, 다음 작업 `괌 니코 호텔` 첫 행은 다시 이전 작업의 `ERP hotelSeq=2264`로 조회되어 `두짓비치 리조트` 행을 가져왔다.
  - 즉 작업 A에서 B로 넘어갈 때 첫 번째 조회만 이전 호텔 hidden `hotelSeq`가 남아 있는 패턴이었다.
- 원인:
  - `_set_erp_hotel_filter()`가 새 호텔명을 넣기 전에 ERP hidden 필드 `#hotelSeq`를 먼저 비우지 않았다.
  - 기존 wait 로직은 `hotelSeq`가 비어 있을 때만 새 자동매칭 값을 기다렸기 때문에, 이전 작업의 `hotelSeq`가 남아 있으면 "이미 값이 있다"고 보고 바로 조회 버튼을 눌렀다.
- `gui.py`:
  - 새 호텔명을 입력하기 전에 `#hotelSeq`를 먼저 공란으로 만들고 `input/change` 이벤트를 발생시킨다.
  - 이후 `autoHotel()` 호출 결과로 새 `hotelSeq`가 다시 채워질 때까지 기존 polling 로직이 기다리게 했다.
  - 로그/검증은 v5.0.11 방식 그대로 호텔명 텍스트 기준을 유지한다.
- `tests/test_fare_engine.py`:
  - 이전 `hotelSeq=2264`가 남아 있는 상태에서 `괌 니코 호텔`을 입력하면 hidden seq를 먼저 비우고 새 `hotelSeq=4610`을 기다리는 회귀 테스트를 추가했다.
- 검증:
  - `python -m py_compile gui.py tests\test_fare_engine.py` 통과.
  - `python -m unittest tests.test_fare_engine -v` 통과(26개).
  - `python -m unittest discover -s tests -v` 통과(55개).
  - `python -m json.tool config.json`, `python -m json.tool latest.json`, `python -m json.tool release\latest.json` 통과.
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_fare_engine.py tests\test_excel_jobs.py` 통과.
  - `git diff --check` 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.12.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `ABAA6300A3C57A82F7F736CA810E801F5E0D4031989A24936085C6A62EFF5CA7`
  - silent 설치 검증: 설치본을 `%TEMP%\NaeilV512InstallTest2`에 `--silent --install-dir ... --no-shortcuts`로 설치 후 레지스트리 `DisplayVersion=v5.0.12`, `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `price_desc_input/#priceDesc`, `airline_select/#air2Cd`, `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제 및 기존 사용자 설치 레지스트리 복원.
- 배포:
  - GitHub 릴리스 태그: `v5.0.12`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.12/NaeilERPUpdater_Setup_v5.0.12.exe`
- 남은 리스크:
  - 실제 ERP 저장은 이번 패치 후 다시 수행하지 않았다. 첨부 로그 기반 원인 분석과 자동화 회귀 테스트를 우선 반영했다.

## v5.0.11 릴리즈 — 호텔명 텍스트 기준 검증 / hotelSeq 참고값 전환 (2026-07-01)

- 배경:
  - v5.0.10에서 DB 후보 직접 선택 작업은 `hotelSeq` 불일치 시 중단하도록 정리했지만, 실제 운영 관점에서는 DB 선택의 목적이 hidden ID 고정이 아니라 ERP DB에 저장된 정확한 호텔명 텍스트 선택에 가깝다는 점을 재확인했다.
  - 사용자가 선택한 DB 호텔명과 ERP 조회 결과 호텔명이 같으면, ERP 내부 hidden `hotelSeq`가 달라도 저장을 막지 않는 쪽으로 방향을 정정했다.
- `gui.py`:
  - `_set_erp_hotel_filter()`에서 선택 `hotelSeq`와 ERP 자동완성 `hotelSeq`가 다르다는 이유만으로 실패시키던 검증을 제거했다.
  - RPA 실행 후 조회 결과 그리드 검증 기준을 `hotelSeq`에서 `hotelKorNm` 호텔명 텍스트로 변경했다.
  - 호텔명 비교는 공백 차이를 무시해 `괌 PIC 리조트`와 `괌PIC리조트` 같은 표기 차이는 같은 값으로 본다.
  - 로그는 `ERP hotelSeq`를 참고값으로 남기되, 검증 문구는 `DB선택 텍스트 검증` 또는 `호텔명 텍스트 검증`으로 표시한다.
  - 호텔명이 비워진 조건 가져오기/초기화 경로에서 `current_hotel_seq_strict`가 남지 않게 함께 초기화했다.
- `tests/test_fare_engine.py`:
  - 선택 `hotelSeq=7864`인데 ERP 자동완성이 `9001`을 반환해도 호텔명이 같으면 통과하는 회귀 테스트를 추가했다.
  - 호텔명 비교에서 공백 차이를 무시하고, 다른 호텔명은 실패로 보는 테스트를 추가했다.
- 검증:
  - `python -m py_compile gui.py tests\test_fare_engine.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(54개).
  - `python -m json.tool config.json`, `python -m json.tool latest.json`, `python -m json.tool release\latest.json` 통과.
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_fare_engine.py tests\test_excel_jobs.py` 통과.
  - `git diff --check` 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.11.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `78910E12750BE6392A4096689D6CCEA03504A665BBF529442AE342EB1307878A`
  - silent 설치 검증: 설치본을 `%TEMP%\NaeilV511InstallTest`에 `--silent --install-dir ... --no-shortcuts`로 설치 후 레지스트리 `DisplayVersion=v5.0.11`, `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `price_desc_input/#priceDesc`, `airline_select/#air2Cd`, `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제 및 기존 사용자 설치 레지스트리 복원.
- 배포:
  - GitHub 릴리스 태그: `v5.0.11`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.11/NaeilERPUpdater_Setup_v5.0.11.exe`
- 남은 리스크:
  - 실제 ERP 저장은 이번 패치 후 다시 수행하지 않았다. 이름 기준 검증 로직, 빌드, 설치본 검증까지만 수행했다.

## v5.0.10 릴리즈 — 호텔명 텍스트 기준 실행 / DB 선택만 hotelSeq 강검증 (2026-07-01)

- 배경:
  - 사용자가 호텔명을 직접 텍스트로 넣는 경우까지 DB 선택 `hotelSeq`처럼 강검증하면, ERP 화면에서 자동완성된 실제 호텔과 프로그램 내부 기대값이 어긋날 때 작업이 과하게 중단될 수 있었다.
  - 운영 방향은 "그냥 호텔 이름으로만 넣으면 ERP 자동매칭 결과를 신뢰하고, 사용자가 검색 후보를 직접 고른 경우만 hotelSeq 불일치를 위험 신호로 본다"로 확정했다.
- `gui.py`:
  - 호텔 조건에 `hotel_seq_strict` 플래그를 추가했다.
  - 호텔 DB 후보를 마우스/콤보박스로 직접 선택한 경우만 `hotel_seq_strict=True`로 저장한다.
  - 사용자가 텍스트를 입력했거나 ERP 화면 조건 가져오기로 들어온 호텔명은 `hotelSeq`를 참고값으로 보존하되 `hotel_seq_strict=False`로 처리한다.
  - RPA 실행 시 strict 작업은 기존처럼 `#hotelSeq` 기대값 불일치 시 저장 전에 중단한다.
  - 텍스트 호텔명 작업은 ERP 자동완성으로 잡힌 `hotelSeq`를 실제 조회/그리드 검증 기준으로 사용한다.
  - 작업 목록 조건 표시와 확인창에서 DB 직접 선택 호텔만 `(hotelSeq ...)`로 표시하고, 일반 텍스트 호텔은 호텔명만 표시한다.
- `tests/test_fare_engine.py`:
  - DB 후보 직접 선택은 `hotel_seq_strict=True`로 작업/행에 보존되는지 검증했다.
  - 텍스트 호텔명은 `hotelSeq`가 있어도 strict 검증 대상이 아니며 작업 목록 표시에도 `hotelSeq`가 붙지 않는지 검증했다.
  - ERP 화면 조건 가져오기와 작업목록 fallback 방지 테스트에 `hotel_seq_strict` 기대값을 추가했다.
- 검증:
  - `python -m py_compile gui.py tests\test_fare_engine.py` 통과.
  - `python -m unittest tests.test_fare_engine -v` 통과(23개).
  - `python -m json.tool config.json`, `python -m json.tool latest.json`, `python -m json.tool release\latest.json` 통과.
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_fare_engine.py tests\test_excel_jobs.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(52개).
  - Tk UI 스모크: `APP_VERSION == v5.0.10`, `hotel_seq_strict` 상태값과 strict 선택 헬퍼 존재 확인.
  - `git diff --check` 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.10.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `F2864A7C51BCD5D49DEC6CE5B0FAF8948997B2EB23789003944C41AA661EE2F2`
  - silent 설치 검증: 설치본을 `%TEMP%\NaeilV510InstallTest`에 `--silent --install-dir ... --no-shortcuts`로 설치 후 레지스트리 `DisplayVersion=v5.0.10`, `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `price_desc_input/#priceDesc`, `airline_select/#air2Cd`, `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제 및 기존 사용자 설치 레지스트리 복원.
- 배포:
  - GitHub 릴리스 태그: `v5.0.10`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.10/NaeilERPUpdater_Setup_v5.0.10.exe`
- 남은 리스크:
  - 실제 ERP 저장은 이번 패치 후 다시 수행하지 않았다. 텍스트/DB선택 조건 해석, 빌드, 설치본 검증까지만 수행했다.

## v5.0.9 릴리즈 — 작업목록 hotelSeq fallback 누수 수정 (2026-07-01)

- 배경:
  - 사용자 실행 로그에서 첫 작업 `괌 플라자 호텔 (hotelSeq 6195)` 이후, `hotelSeq`가 없는 뒤쪽 호텔 작업들이 계속 `선택=6195`로 검증되는 현상을 확인했다.
  - 예: `괌 니코 호텔`, `두짓비치 리조트`, `괌 두짓타니 리조트` 등에서 실제 ERP 자동완성 `hotelSeq`는 각각 다르지만, 프로그램 기대값이 첫 작업의 `6195`로 남아 실패했다.
  - 대부분은 저장 전 검증에서 실패해 막혔으며, 로그 기준 성공은 앞쪽 `괌 플라자 호텔` 31건과 `괌 PIC 리조트` 일부 4건, 실패 203건이었다.
- 원인:
  - RPA 루프에서 `row.hotel_seq`가 비어 있으면 `self.selected_hotel_seq`를 fallback으로 쓰고 있었다.
  - 작업목록 실행에서는 `self.selected_hotel_seq`가 첫 작업 기준값이므로, 뒤쪽 작업의 빈 `hotelSeq`가 첫 작업의 `hotelSeq`를 물려받는 문제가 생겼다.
- `gui.py`:
  - `_rpa_row_conditions()`를 추가해 작업목록 행과 단일 입력표 행의 조건 해석을 분리했다.
  - 작업목록 행(`_job_index`가 있는 행)은 자기 행에 들어 있는 `요금구분`, `항공사`, `호텔명`, `hotelSeq`, `진행구분`만 사용한다.
  - 단일 입력표 실행 행만 기존처럼 선택된 전역 조건을 fallback으로 사용한다.
  - 따라서 `hotelSeq`가 없는 호텔명 작업은 이전 작업의 `hotelSeq`를 물려받지 않고, ERP 자동완성 결과 기준으로 처리된다.
- `tests/test_fare_engine.py`:
  - 작업목록 행이 첫 작업의 `hotelSeq`를 fallback으로 가져오지 않는 회귀 테스트를 추가했다.
  - 단일 입력표 행은 기존처럼 전역 선택 조건을 fallback으로 쓰는 것도 함께 검증했다.
- 검증:
  - 첨부 실행 로그 분석으로 첫 작업 `hotelSeq=6195` fallback 누수 패턴 확인.
  - `python -m json.tool config.json`, `python -m json.tool latest.json` 통과.
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_fare_engine.py tests\test_excel_jobs.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(51개).
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.9.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `BF96CD3C4E72AC538BA49BAE6170F9AE990E71CB89164813B15BC6D434CB7A12`
  - silent 설치 검증: 설치본을 임시 ASCII 경로 `%TEMP%\NaeilV509InstallTest`로 복사해 `%TEMP%\NaeilV509Verify`에 설치 후 `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `price_desc_input/#priceDesc`, `airline_select/#air2Cd`, `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제.
- 배포:
  - GitHub 릴리스 태그: `v5.0.9`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.9/NaeilERPUpdater_Setup_v5.0.9.exe`
- 남은 리스크:
  - 실제 ERP 재실행/저장은 이번 패치 후 다시 수행하지 않았다. 로그 기반 원인 재현과 단위 테스트, 설치본 검증까지만 수행했다.

## v5.0.8 릴리즈 — ERP 화면 조건 가져오기 확인창 (2026-07-01)

- 배경: 사용자가 프로그램의 요금구분/항공사/호텔명 조건을 비워두고 ERP 화면 필터에 직접 값을 넣은 상태로 실행하면, 프로그램 기준으로는 빈 조건이므로 날짜별 조회 시 ERP 필터가 초기화되는 혼선이 있었다.
- 방향:
  - 작업 목록 실행은 등록된 작업 조건이 원본이므로 변경하지 않는다.
  - 현재 입력표를 바로 실행하는 단일 실행에서만, 프로그램 조건이 비어 있고 ERP 화면 조건이 있으면 가져올지 묻는다.
- `gui.py`:
  - 단일 실행 시작 직전 ERP 디버그 브라우저의 현재 `#priceDesc`, `#air2Cd`, `#hotelKorNm`, `#hotelSeq` 값을 읽는다.
  - 프로그램에서 비어 있는 조건만 가져오기 후보로 표시한다. 이미 프로그램에 입력된 조건은 ERP 화면값으로 덮어쓰지 않는다.
  - 확인창에는 요금구분, 항공사, 호텔명과 `hotelSeq`를 함께 표시한다.
  - `예`를 누르면 해당 조건을 프로그램 입력값과 이번 실행 조건에 반영한다.
  - `아니오`를 누르면 기존처럼 프로그램 조건 기준으로 실행한다.
  - ERP 화면에서 가져온 호텔명은 `hotelSeq`까지 내부 호텔 후보로 기억해 실행 중 호텔 필터 검증에 사용할 수 있게 했다.
- `tests/test_fare_engine.py`:
  - 프로그램 조건 공란 상태에서 ERP 화면 조건을 가져오면 요금구분/항공사/호텔명/`hotelSeq`가 실행 조건으로 보존되는 테스트를 추가했다.
  - 프로그램에 이미 입력된 조건은 ERP 화면값으로 덮어쓰지 않고, 빈 조건만 후보로 잡는 테스트를 추가했다.
- 검증:
  - `python -m json.tool config.json`, `python -m json.tool latest.json` 통과.
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_fare_engine.py tests\test_excel_jobs.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(50개).
  - 라이브 ERP 디버그 브라우저 `127.0.0.1:9223`에서 현재 화면 조건 읽기 확인: `hotelKorNm=윙크 다낭 센터, 언스크립티드 바이 하얏트`, `hotelSeq=30002616`, 요금구분/항공사는 공란.
  - Tk UI 스모크: `APP_VERSION == v5.0.8`, 조건 가져오기 헬퍼 존재, 작업목록 `진행상황` 컬럼 확인 통과.
  - `git diff --check` 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.8.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `0DE8098B1BBF4095E974759083FD44D2959001BC056B9BA18C8047472424F19F`
  - silent 설치 검증: 설치본을 임시 ASCII 경로 `%TEMP%\NaeilV508InstallTest`로 복사해 `%TEMP%\NaeilV508Verify`에 설치 후 `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `price_desc_input/#priceDesc`, `airline_select/#air2Cd`, `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제.
- 배포:
  - GitHub 릴리스 태그: `v5.0.8`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.8/NaeilERPUpdater_Setup_v5.0.8.exe`
- 남은 리스크:
  - 실제 ERP 저장은 실행하지 않았다. 실행 전 조건 감지/가져오기와 설치본 검증까지만 수행했다.

## v5.0.7 릴리즈 — 호텔명 ERP DB 자동검색/선택 (2026-07-01)

- 배경: 호텔명을 직접 텍스트로 입력하면 ERP 자동완성 매칭 실수 가능성이 있어, ERP 호텔 DB에서 검색 후 선택하는 방식으로 보강 요청됨.
- `gui.py`:
  - 호텔명 입력칸을 검색형 `ttk.Combobox`로 바꾸고, 입력 후 0.45초 뒤 자동으로 ERP DB를 검색하게 했다.
  - 검색은 현재 입력한 키워드를 기준으로 라이브 ERP 세션에서 `POST /erp/sy/sy02/sy02_108_list.json`을 호출한다.
  - 검색 파라미터는 `searchType=pop`, `infoCd=H`, `infoTitle=<검색어>`이며, 응답의 `infoSeq`를 내부 `hotelSeq`로 사용한다.
  - 입력칸 아래에 후보 리스트를 표시하고, 후보 목록은 `호텔명    호텔 > 국가 > 도시 · hotelSeq ...` 형태로 보여준다.
  - 매칭 후보가 없으면 후보 리스트에 `검색 결과 없음`을 표시한다.
  - 후보 선택 후 입력칸에는 실제 호텔명만 남긴다.
  - 작업 목록에는 기존 `hotel_name`과 함께 선택된 `hotel_seq`를 저장한다.
  - 작업 조건 표시도 호텔 선택값이 있으면 `호텔명 : ... (hotelSeq ...)` 형태로 보여준다.
  - RPA 실행 시 선택된 `hotelSeq`를 기대값으로 넘겨, ERP 자동완성이 다른 `hotelSeq`를 잡으면 저장 전에 중단한다.
  - 엑셀에서 들어온 기존 `호텔명` 텍스트만 있는 작업은 이전처럼 ERP 자동완성 매칭 방식으로 계속 동작한다.
- `tests/test_fare_engine.py`:
  - ERP 호텔 DB 선택값이 `hotel_seq`로 작업과 행에 보존되는 테스트를 추가했다.
  - 호텔 검색 결과가 없을 때 `검색 결과 없음` 행을 표시하는 테스트를 추가했다.
- 검증:
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_fare_engine.py tests\test_excel_jobs.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(48개).
  - 라이브 ERP 디버그 브라우저 `https://erp.naeiltour.co.kr/erp/main/main`에서 `아미아나` 검색 확인: `hotelSeq 7864 / infoCd H / 아미아나 나트랑 / 베트남 / 나트랑 / Y`.
  - 라이브 ERP 디버그 브라우저에서 `없는호텔테스트XYZ` 검색 결과 0건 확인.
  - Tk UI 스모크: `APP_VERSION == v5.0.7`, 호텔 결과 리스트 `검색 결과 없음`, 작업목록 `진행상황` 컬럼 확인 통과.
  - `git diff --check` 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.7.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `F3150575C20E955430E23A95BB1E572298D0BDD7CA568AE1CF37C9EBBCAA8EBF`
  - silent 설치 검증: 설치본을 임시 ASCII 경로 `%TEMP%\NaeilV507InstallTest`로 복사해 `%TEMP%\NaeilV507Verify`에 설치 후 `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제.
- 배포:
  - GitHub 릴리스 태그: `v5.0.7`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.7/NaeilERPUpdater_Setup_v5.0.7.exe`
- 남은 리스크:
  - 실제 ERP 저장은 실행하지 않았다. 검색/선택/검증 로직과 DB 응답 확인까지만 수행했다.

## v5.0.6 릴리즈 — 작업 목록 수동 추가 조건 완화 (2026-07-01)

- 배경: `작업 목록에 추가`를 누를 때 기존에는 `요금구분 + 항공사코드`가 모두 있어야 했지만, 실제 작업에서는 호텔명만으로도 특정 상품 범위를 만들거나 항공사/요금구분 중 하나만으로 먼저 큐를 구성할 필요가 있었다.
- `gui.py`:
  - `작업 목록에 추가` 조건을 `요금구분`, `항공사코드`, `호텔명` 중 하나 이상 값이 있으면 통과하도록 완화했다.
  - 세 조건이 모두 비어 있으면 `요금구분, 항공사코드, 호텔명 중 하나 이상 입력` 안내로 차단한다.
  - 작업 목록에 추가하거나 기존 작업을 수정 반영하기 전에 확인창을 띄운다.
  - 확인창에는 `요금구분`, `항공사`, `호텔명`, `대상 행 수`를 표시하고, 비어 있는 조건은 `전체 요금구분`, `전체 항공사`, `전체 호텔`로 명시한다.
  - 확인창에서 취소하면 작업 목록, 입력표, 선택 상태를 변경하지 않는다.
- `tests/test_fare_engine.py`:
  - 호텔명만 입력해도 작업 목록에 추가되는 테스트를 추가했다.
  - 확인창에서 취소하면 큐가 변경되지 않고 입력표도 지워지지 않는 테스트를 추가했다.
- 버전/배포 파일:
  - `APP_VERSION`, `build_setup.ps1`, `packaging/setup_installer.py`, `latest.json`을 `v5.0.6`으로 동기화했다.
- 검증:
  - `python -m json.tool config.json`, `python -m json.tool latest.json`, `python -m json.tool release\latest.json` 통과.
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_fare_engine.py tests\test_excel_jobs.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(46개).
  - Tk UI 스모크: `RpaGuiApp` 생성 후 `APP_VERSION == v5.0.6`, 호텔명 필드, 작업목록 `진행상황` 컬럼 확인 통과.
  - `git diff --check` 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.6.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `AB47B2ABC6C727DC5E2E4928C7D37AB57B59291EE5B05BC37F785ADE546B764F`
  - silent 설치 검증: 설치본을 임시 ASCII 경로 `%TEMP%\NaeilV506InstallTest`로 복사해 `%TEMP%\NaeilV506Verify`에 설치 후 `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제.
- 배포:
  - GitHub 릴리스 태그: `v5.0.6`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.6/NaeilERPUpdater_Setup_v5.0.6.exe`
- 남은 리스크:
  - 이번 변경은 작업 목록 등록 조건과 확인창에 한정되어, 실제 ERP 저장 소량 테스트는 별도로 실행하지 않았다.

## v5.0.5 릴리즈 — 다중 상품 작업 목록/호텔명 조건 필터 (2026-07-01)

- 배경: 기존 GUI는 상단 `요금구분 + 항공사코드` 조건을 한 번 고정한 뒤 입력표 전체를 실행하는 구조라, `3박_다낭/LJ` 실행 후 `3박_나트랑/7C`를 실행하려면 다시 세팅해야 했다.
- 방향: 입력표 컬럼은 늘리지 않고, 조건열이 있는 엑셀만 다중 작업으로 인식해 작업 목록에 자동 적재한다.
- UI 피드백 반영:
  - `현재 입력표 작업 추가` 버튼명을 `작업 목록에 추가`로 바꾸고 요금 입력표 툴바의 `엑셀로 다운받기` 옆으로 이동했다.
  - 작업 목록이 등록되면 기존 `요금수정 시작` 버튼명이 `작업목록 수정 시작`으로 자동 변경되게 했다.
  - 작업 목록 내부의 별도 `작업목록 실행` 버튼은 제거했다.
  - 작업 목록 등록 후 요금 입력표는 자동으로 비워지게 했다.
  - 작업 조건 표시는 `요금구분 : ... / 항공사 : ... / 호텔명 : ...` 형태로 바꿨다.
  - 작업 목록에 `진행상황` 컬럼을 추가해 작업별 `대기/진행 중/완료/실패/건너뜀` 건수를 볼 수 있게 했다.
  - 상단 `진행구분` 입력 필드는 제거하고, 그 자리에 `호텔명` 입력 필드를 추가했다.
  - 항공사코드 필드는 클릭하거나 포커스가 들어오면 현재 텍스트를 전체 선택해 바로 새 코드로 덮어쓸 수 있게 했다.
  - 선택한 작업을 `입력표로 불러오기`로 다시 편집할 수 있게 하고, 수정 후 `작업 목록에 추가`를 누르면 기존 항목을 교체한다.
  - 작업 목록에서 입력표로 다시 불러올 때 요금칸에 입력했던 `예약마감` 텍스트가 원래 칸에 복원되도록 했다.
  - `위로`/`아래로` 버튼으로 대기열 실행 순서를 바꿀 수 있게 했다.
- `gui.py`:
  - 메인 요금수정 탭에 `작업 목록` 영역을 추가했다.
  - `현재 입력표 작업 추가`로 현재 입력표와 상단 조건을 작업 큐에 수동 등록할 수 있게 했다.
  - 작업 목록은 조건(`요금구분 / 항공사코드 / 호텔명`), 진행상황, 건수, 출처를 표시한다.
  - 작업 목록이 있으면 기존 `요금수정 시작` 버튼이 `작업목록 수정 시작`으로 바뀌고 작업 목록을 우선 실행한다. 실행 전 총 작업 수와 건수를 확인한다.
  - 기존 RPA 루프는 행별 `price_desc`, `airline_code`, `hotel_name`, `progress_status` 우선값을 활용해 작업 1 → 작업 2 순서로 조건을 바꿔가며 실행한다.
  - ERP에서 읽어둔 항공사 옵션이 있으면 `진에어`, `[LJ] 진에어` 같은 표시값을 코드 `LJ`로 정규화한다.
  - 호텔명 조건은 ERP `#hotelKorNm` 입력 후 `#hotelSeq` hidden key 매칭을 확인하고, 조회 결과 그리드의 `hotelSeq`가 다르면 저장 전에 중단한다.
- `excel_loader.py`:
  - `요금구분` + `항공사코드` 열이 모두 있는 엑셀을 다중 작업 엑셀로 감지하는 `load_fare_jobs_from_excel()`을 추가했다. `호텔명` 열이 있으면 작업 조건으로 함께 읽는다.
  - `진행구분` 열은 작업 조건으로 쓰지 않는다. 예전 파일 호환과 `예약마감` 처리만 유지하며, 요금칸에 `예약마감`이 있으면 진행구분과 원래 입력 칸 정보를 함께 보존한다.
  - 조건열이 없는 기존 엑셀은 기존 `load_and_validate_fares()` 흐름으로 그대로 입력표에 로드된다.
- ERP 필드 확인:
  - 라이브 디버그 Chrome `127.0.0.1:9223`의 `framego01_104_list`에서 확인.
  - 호텔명 입력 필드: `#hotelKorNm`, `onblur=autoHotel(event, this)`.
  - 호텔 hidden key: `#hotelSeq`.
  - 행사요금일괄수정 그리드 dataField: `hotelKorNm`, `hotelSeq`.
- 안전장치:
  - 조건열 엑셀은 입력표를 복잡하게 만들지 않고 작업 목록으로만 들어간다.
  - 단일 엑셀을 입력표로 불러올 때 기존 작업 목록이 남아 있으면 비울지 확인한다.
  - 최종 리포트 날짜값에 작업 조건을 붙여 같은 날짜라도 다른 상품/항공사 작업을 구분한다.
- 검증:
  - `python -m json.tool config.json`, `python -m json.tool latest.json`, `python -m json.tool release\latest.json` 통과.
  - `python -m py_compile gui.py excel_loader.py packaging\setup_installer.py tests\test_excel_jobs.py tests\test_fare_engine.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(44개).
  - Tk UI 스모크: `RpaGuiApp` 생성 후 호텔명 필드와 작업목록 `진행상황` 컬럼 확인 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.5.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `ECAAB8DA6B1AE4576BAF20DFCFB8EADABAC90971696E15F8DA6642CC9CA7779E`
  - silent 설치 검증: `%TEMP%\NaeilERPUpdater_v505_verify`에 설치 후 `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 존재 및 `hotel_name_input/#hotelKorNm`, `hotel_seq_input/#hotelSeq` 확인. 검증 후 임시 폴더 삭제.
- 배포:
  - GitHub 릴리스 태그: `v5.0.5`
  - 설치본 URL: `https://github.com/KAKNAKIAK/naeil-erp-updater/releases/download/v5.0.5/NaeilERPUpdater_Setup_v5.0.5.exe`
- 남은 리스크:
  - 라이브 ERP에서 DOM/그리드 ID는 확인했지만, 실제 저장 소량 테스트는 이번 릴리즈 전 별도로 실행하지 않았다.

## v5.0.4 릴리즈 — 요금구분 검색 조건 입력 필드 추가 (2026-06-30)

- 배경: `ERP-행사요금일괄수정-구글시트-매핑-정리.md` 기준으로 ERP `행사요금일괄수정` 화면은 `요금구분(priceDesc)`과 `항공사(air2Cd)` 조합을 구글시트 요금 블록 매칭 키로 사용한다.
- `gui.py`: 요금 입력표 상단의 항공사코드 옆에 `요금구분` 입력칸을 추가했다.
- `gui.py`: 실행 시작 시 입력된 요금구분을 `selected_price_desc`로 고정하고, RPA 조회 직전 ERP 검색 조건 `#priceDesc`에 값을 주입하도록 `_set_erp_text_filter()`를 추가했다.
- 요금구분은 ERP에 저장된 텍스트 그대로 입력해야 한다. 2026-06-30 라이브 테스트 기준 `3박 다낭`은 0건, `3박_다낭`은 `3박_다낭_금까기` 24건 조회됨.
- 빈 요금구분이면 ERP `#priceDesc`를 비워 전체 요금구분 대상으로 조회한다.
- `전체 지우기` 시 요금구분 입력값도 함께 초기화한다.
- `config.json` 및 `packaging/setup_installer.py` 기본 selector에 `price_desc_input: "#priceDesc"`를 추가했다.

## v5.0.4 릴리즈 — 진행구분 `예약마감` 자동 정보일괄수정 (2026-06-30)

- 배경: 입력 필드에 `예약마감` 텍스트가 들어가면 ERP `정보일괄수정 > 진행구분 > 예약마감`으로 바꾸도록 요청됨.
- 라이브 ERP 디버그 Chrome(9223)에서 저장 없이 DOM/HTML만 확인했다.
  - `정보일괄수정` 버튼: `#eventModify`
  - 팝업 URL: `/go/go01/sub/go01_103_03_pop`
  - 진행구분 체크박스: `#procCdChk`
  - 진행구분 선택값: `#procCd`
  - 팝업 저장 버튼: `#appSave`
  - 저장 처리 endpoint: `/go/go01/go01_100_04_spu.sp`
- `gui.py`: 요금 입력표 상단에 `진행구분` 입력칸을 추가했다.
- `진행구분` 입력값에 `예약마감` 또는 `예약 마감`이 포함되면 `procCd=05`로 판정한다.
- 요금 입력표의 요금칸에 `예약마감`이 들어간 경우에도 요금값 `0`으로 저장하지 않고 `progress_status`로 보존해 진행구분 변경으로 처리한다.
- ERP 날짜 필드는 `send_keys` 대신 JS + `input/change/blur` + jQuery `change/blur`를 함께 발생시키는 `_set_erp_date_input()`으로 입력하고, 시작일/종료일 표시값이 기대값과 다르면 저장 전 중단한다.
- 각 조회/페이지 처리 시:
  - 요금값이 있으면 기존대로 요금 업데이트를 먼저 실행한다.
  - `예약마감` 판정이 있으면 같은 페이지로 다시 이동해 현재 페이지 전체선택 후 `정보일괄수정` 팝업에서 `procCdChk`를 체크하고 `procCd=05`를 저장한다.
  - 요금값이 없어도 `예약마감`만 있으면 진행구분 변경만 실행한다.
  - 현재 페이지가 이미 목표 진행구분이면 중복 저장 없이 통과한다.
  - ERP 정책상 `예약마감` 행은 요금 일괄수정 대상에서 제외된다. 일부만 예약마감이면 ERP가 수정 가능한 행만 처리하므로 RPA는 원래대로 진행하고, 전체가 예약마감이라 수정 가능한 행이 0개이면 해당 페이지의 요금 업데이트만 건너뛴다.
- `전체 지우기` 시 진행구분 입력값도 초기화한다.
- `config.json` 및 `packaging/setup_installer.py` 기본 selector에 `event_modify_button`, `bulk_update_save_button`, `progress_status_checkbox`, `progress_status_select`를 추가했다.
- 라이브 ERP 검증(2026-06-30):
  - 조건: `2026-07-15`, 항공사 `ZE`, 요금구분 `3박_다낭`.
  - 저장 전: 총 24건, `05|예약마감` 19건 + `04|예약신청` 5건.
  - `정보일괄수정 > 진행구분 > 예약마감` 저장 후 재조회: 총 24건 전부 `05|예약마감`.
  - `요금구분=3박 다낭`은 0건으로 확인되어 테스트 입력은 `3박_다낭`으로 정정함.
  - 저장 후 ERP 화면 문구 `※ 진행구분이 예약마감 인 경우 일괄수정으로 수정하실 수 없습니다.` 및 전체 예약마감 상태에서 `업데이트할 요금을 선택하세요.` 알림으로, 수정 가능한 행이 0개이면 요금업데이트가 진행되지 않는 정책을 확인함.

## v5.0.4 릴리즈 검증 기록 (2026-06-30)

- 검증:
  - `python -m py_compile gui.py packaging\setup_installer.py fare\store.py tests\test_fare_store.py tests\test_fare_engine.py` 통과.
  - `python -m json.tool config.json`, `python -m json.tool latest.json` 통과.
  - `RpaGuiApp._progress_status_from_text()` 스모크 테스트 통과: `예약마감`, `예약 마감 처리` -> `procCd=05`.
  - 요금칸 `예약마감` 원문이 진행구분으로 보존되고 요금 입력값은 비는 스모크 테스트 통과.
  - 현재 페이지 진행구분 카운트에서 예약마감 행을 감지하고, 일부 예약마감은 계속 진행/전체 예약마감만 요금 스킵하는 회귀 테스트 추가.
  - `python -m unittest discover -s tests -v` 통과(38개).
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.4.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `C6F05F5470D923D096B64D95A1F2D876F211DD9A70D28198F7C3FE6AAC3B3399`
  - `%TEMP%\NaeilERPUpdater_v5_0_4_verify`에 silent 설치 후 `config.json`에서 `price_desc_input=#priceDesc`, `event_modify_button=#eventModify`, `bulk_update_save_button=#appSave`, `progress_status_checkbox=#procCdChk`, `progress_status_select=#procCd`, `airline_select=#air2Cd` 확인.
  - 검증용 temp 설치 폴더 정리 완료.

## 파일 정리 — 구버전 설치본/생성 캐시 보관 (2026-06-30)

- 삭제 없이 `legacy_archive_20260630/` 아래로 보관 이동했다.
- `release/`의 구버전 설치본 `NaeilERPUpdater_Setup_v5.0.0.exe`, `v5.0.1.exe`, `v5.0.2.exe`는 `legacy_archive_20260630/release_old_installers/`로 이동했다.
- 루트 및 패키지별 `__pycache__` 폴더는 `legacy_archive_20260630/generated_pycache/`로 이동했다.
- 활성 `release/`에는 현재 안정판 `v5.0.3` 설치본, `NaeilERPUpdater_Setup.exe`, `latest.json`, `NaeilERPUpdater/` 빌드 폴더만 유지했다.
- `.venv/`, Chrome 디버그 프로필, 운임 캐시, 로그 증적, 현재 수정 중인 소스 파일은 건드리지 않았다.

## v5.0.4 릴리즈 포함 — 운임/시즌 Firestore 읽기 절감, 스냅샷 문서 우선 (2026-06-18)

- 배경: 운임 DB(Firestore `fare-calculator-2026`)가 무료 read 한도(5만/일) 반복 초과. erp_updater는 새로고침마다 `fares`(2,360)+`seasons`(922) 컬렉션 전량을 읽어 **회당 약 3,282 reads**.
- `fare/store.py` 변경(**하위호환**): `meta/fares-snapshot`·`meta/seasons-snapshot` 단일 문서를 먼저 읽고(**읽기 2회**), 있으면 사용 / 없으면(404) 기존 컬렉션 전량 fetch로 **자동 fallback**. `_fetch_fares_and_seasons`, `_fetch_snapshot_doc` 추가. `load_fare_snapshot`의 fetch 라인을 `_fetch_fares_and_seasons` 호출로 교체.
- 이 스냅샷 문서는 웹앱(fare-calculator)이 운임/시즌 저장 시 함께 발행한다(웹앱 발행 로직은 2026-06-18 배포 완료).
- 효과: 스냅샷 존재 시 회당 **3,282 → 2 reads**. 스냅샷이 없거나 1MB 초과로 발행 실패 시 기존과 동일 동작(안전).
- 검증: `python -m unittest tests.test_fare_store -v` **7개 통과**(스냅샷 우선 사용 + 미존재 시 fallback + 캐시/429 기존 동작 포함). 라이브 `_fetch_snapshot_doc` 호출로 실 Firestore 404→None fallback 동작 확인.
- v5.0.4 설치본에 포함됨.

## v5.0.3 패치 반영 (2026-06-18)

- `전체 지우기` 동작을 보강했다.
  - 입력표 행, 수식, 계산 결과, 병합 복원 스냅샷을 지운 뒤 항공사코드 선택값도 `_선택_`으로 되돌린다.
  - ERP 디버그 Chrome이 열려 있으면 항공사코드 목록을 다시 읽도록 `refresh_airline_options_from_erp(silent=True)`를 호출한다.
  - 항공사코드는 빈 값일 때도 ERP `#air2Cd`를 `_선택_`으로 초기화해 이전 조회 조건이 남지 않게 했다.
- 요금 입력표 상단의 `호텔명` 입력칸 추가안은 이번 릴리스 범위에서 제외했다.
- 검증:
  - `python -m py_compile gui.py packaging\setup_installer.py` 통과.
  - `python -m json.tool config.json` 통과.
  - `python -m unittest discover -s tests -v` 통과(34개).
  - `clear_sheet()` 항공사코드 초기화 스모크 테스트 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.3.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `BF46A9F2A2A7E98468DFDBF0A49B57853DB8C267DFD16C2AE164E421804E253A`
  - `%TEMP%\NaeilERPUpdater_v5_0_3_verify3`에 silent 설치 후 `config.json`에서 `airline_select=#air2Cd`, 호텔명 셀렉터 제거 확인.
  - 검증용 temp 설치 폴더와 Windows 제거 항목은 정리 완료.

## v5.0.2 패치 반영 (2026-06-12)

- v5.0.1 빌드 중/직후 반영된 최신 소스 상태를 기준으로 버전을 `v5.0.2`로 올려 재빌드·배포했다.
- 포함 상태:
  - TOPAS 계산 결과 중 마감이라 ERP 입력표에서 제외된 출발일/귀국일/박수 목록을 팝업에서 확인하고 복사할 수 있다.
  - 항공사코드 전달, 귀국편 AC1 110% 제안, 입력표 undo/redo 확장, 요금구간 병합 빈 날짜 분리, 운임/시즌 DB 로드 안정화, 디버그 Chrome 이전 탭 복원 방지 수정이 포함된다.
- 검증:
  - `python -m py_compile gui.py fare\store.py tests\test_fare_store.py tests\test_sheet_merge.py packaging\setup_installer.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(34개).
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.2.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `808D0F86EF842FE2E03CC517568E2F5F21DE8589047A13536ECA757EFC20ED60`
  - `%TEMP%\NaeilERPUpdater_v5_0_2_verify`에 silent 설치 검증 통과.
  - 기존 `NaeilERPUpdater.exe` 실행 중이라 단일 실행 mutex 오판을 피하기 위해 설치본 실행 유지 검사는 생략했다.

## v5.0.1 패치 반영 (2026-06-12)

- 운임/시즌 DB 로드 안정화:
  - `fare/store.py`의 Firestore 호출을 requests/certifi 경로에서 표준 라이브러리 urllib + Windows 인증서 저장소 기반 SSL 컨텍스트로 전환했다.
  - 429 쿼터 초과는 한국어 안내 메시지로 변환한다.
  - 시작 시 자동 로드는 12시간 이내 캐시를 우선 사용하고, 사용자가 누르는 ↻ 새로고침은 강제 새로고침으로 유지했다.
- 요금 입력표/ERP 전달:
  - `ERP 항공사` 표기를 `항공사코드`로 정리했다.
  - ERP 화면의 `#air2Cd` 항공사코드 옵션을 읽어 드롭다운에 반영하고, 요금수정 실행 시 선택 항공사코드를 ERP 조회 조건으로 함께 전달한다.
  - TOPAS 요금 조회 결과를 `요금수정 보내기`로 넘길 때 노선에서 추론한 항공사코드를 입력표에 같이 반영한다.
  - TOPAS 계산 결과 중 마감이라 ERP 입력표에서 제외된 건은 팝업에서 출발일/귀국일/박수 목록을 확인하고 복사할 수 있게 했다.
- TOPAS AC1 반복:
  - 출발편 AC1 반복 실행 횟수를 기억하고, 귀국편 첫 엔트리에서 AC1 자동 반복을 열면 출발편 횟수의 110%를 기본값으로 제안한다. 예: 출발편 100회 → 귀국편 110회.
- 요금 입력표 실행취소/다시실행:
  - tksheet 기본 undo 의존을 보강해 앱 스냅샷 기반으로 확장했다.
  - 셀 편집, 수식줄 입력, 수식 전체 적용, 행 추가/삭제, 전체 지우기, 기간 모드 전환, 요금구간 병합, 엑셀 불러오기, TOPAS 결과 전달까지 버튼 undo/redo가 복원한다.
- 요금구간 병합:
  - 중간에 비는 날짜가 있으면 같은 요금이라도 한 기간으로 붙이지 않고 구간을 나누며, 빈 날짜 목록을 안내한다.
- 디버그 Chrome:
  - `chrome_debug.bat` 실행 시 이전 세션 탭 파일을 정리하고 `--disable-session-crashed-bubble`을 적용해, 지난 탭 복원으로 요금조회 사이트가 자동 팝업되는 현상을 방지했다.
- 검증:
  - `python -m py_compile gui.py fare\store.py tests\test_fare_store.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(34개).
  - 입력표 undo/redo GUI 스모크 테스트 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
    - 설치본: `release\NaeilERPUpdater_Setup_v5.0.1.exe`
    - 별칭: `release\NaeilERPUpdater_Setup.exe`
    - SHA256: `ECA7ACDE3F878E9A63663C7F6011D47D25143D4D5C5079DD973B602DBF931F5F`
  - `%TEMP%\NaeilERPUpdater_v5_0_1_verify2`에 silent 설치 검증 통과.
  - 설치된 `NaeilERPUpdater.exe`가 5초 이상 실행 상태로 유지되는지 확인 후 종료.

## 운임/시즌 DB 로드 실패 — 진단 및 수정 반영 (2026-06-12)

- 증상: 토파스 요금조회 탭에서 `운임/시즌 로드 실패`.
- 원인 2건 확정 (재현·격리 테스트 완료):
  1. `fare/store.py`가 requests로 Firestore 호출 → certifi `cacert.pem`이 구글 드라이브(G:) venv 안에 있어 `load_verify_locations()`가 약 28초 소요 → 그 사이 구글 서버가 TCP를 끊어 `SSLEOFError`. 표준 라이브러리 urllib은 Windows 인증서 저장소를 써서 정상.
  2. Firestore `fare-calculator-2026` 프로젝트가 429 Quota exceeded(무료 일일 읽기 한도 소진 추정). 한국시간 오후 4시 리셋 예상.
  - 보조: 성공 이력이 없어 `cache/fares_snapshot.json` 미존재 → 캐시 폴백 불가.
- 상세 진단·수정 내역은 [운임시즌_DB_로드실패_수정지시서.md](운임시즌_DB_로드실패_수정지시서.md) 참조.
- 수정 반영 완료:
  - `fare/store.py`: `_http_json()`을 requests → 표준 라이브러리(urllib + Windows 인증서 저장소, SSL 컨텍스트 1회 생성 재사용)로 교체. 429는 한국어 안내 메시지(`운임 DB 호출 한도 초과(429)...`)로 변환.
  - `fare/store.py`: `load_fare_snapshot(prefer_cache_within_hours=...)` 추가 — 신선한 캐시가 있으면 Firestore 호출 생략(쿼터 절감).
  - `gui.py`: 시작 0.7초 후 자동 로드는 12시간 이내 캐시 우선(`force=False`), ↻ 버튼은 항상 강제 새로고침(`force=True`).
  - `tests/test_fare_store.py` 신설(6개): 429 한국어 변환, 기타 HTTP 오류 전파, 캐시 신선/만료/부재/강제 로드 분기.
- 검증:
  - `python -m py_compile gui.py fare\store.py tests\test_fare_store.py` 통과.
  - `python -m unittest discover -s tests -v` 통과(31개 = 기존 25 + 신규 6).
  - 실서버 호출: 기존 28초+SSLEOFError → 0.3초 만에 Firestore 응답 수신. 현재는 429 상태라 한국어 한도 초과 안내가 정상 출력됨(TLS 문제 해소 확인).
- 남은 일: Firebase 콘솔에서 읽기 사용량 원인 점검.

## v5.0.0 TOPAS 요금조회 정식 반영 (2026-06-11)

- 별도 `erp_updater_v5` 폴더에서 구현/검증한 V5 기능을 원본 `erp_updater` 정식 소스에 반영했다.
- 신규 `fare/` 패키지를 추가했다.
  - TOPAS availability 원문 파싱, Firestore 운임/시즌 캐시 로드, 박수별 왕복 조합 계산, TSV/Excel/ERP 입력표 변환을 담당한다.
- `토파스 요금조회` 워크플로우를 정식화했다.
  - 출발편/귀국편 원문 슬롯을 채운 뒤 항공노선과 박수를 기준으로 요금을 조회한다.
  - 항공노선은 TOPAS 원문에서 자동 추론하고, `I` 입력 시 `ICN...` 후보가 뜨는 유연 검색을 지원한다.
  - 박수는 단일 선택 방식이며, 최초 조회 후 박수를 바꾸면 별도 조회 버튼 없이 자동 재조회한다.
- 결과 UI를 정리했다.
  - `계산 과정 보기`, `엑셀 다운로드`, `요금수정 보내기`, `복사`를 조회 결과 액션으로 통합했다.
  - 사용자에게 불필요한 `토파스/계산 작업 내용` 로그 영역은 제거했다.
  - 계산 과정 팝업의 왕복 조합은 박수별로 나눠 표시한다.
- ERP 전달 정책을 확정했다.
  - `요금수정 보내기`는 항공비만 입력하고 알선수익/호텔비/지상비/여행경비/소아/유아는 비워 둔다.
- 요금 입력표에 `요금구간 병합`을 추가했다.
  - 같은 요금이 연속되는 일자별 데이터를 시작일/종료일 구간으로 병합한다.
  - 병합 후 `시작일/종료일 분리` 체크를 해제하면 병합 전 일자별 셀 화면으로 복원한다.
- TOPAS/ERP 브라우저 분리 연결을 보강했다.
  - TOPAS는 기본 `127.0.0.1:9222`, ERP는 기본 `127.0.0.1:9223`으로 열고, URL/타이틀/셀렉터 점수로 해당 작업의 브라우저를 매칭한다.
- V5 정식 배포용으로 버전을 `v5.0.0`으로 올리고 자동 업데이트 설정을 다시 활성화했다.

### 검증

- `python -m py_compile gui.py update_client.py excel_loader.py topas\availability.py topas\collector.py topas\pacing.py fare\parser.py fare\calculator.py fare\exporter.py fare\store.py fare\route_select.py` 통과.
- `python -m unittest discover -s tests -v` 통과(25개).
- `python -m json.tool config.json`, `python -m json.tool latest.json` 통과.
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\build_setup.ps1` 통과.
  - 설치본: `release\NaeilERPUpdater_Setup_v5.0.0.exe`
  - 별칭: `release\NaeilERPUpdater_Setup.exe`
  - SHA256: `E296D4E2C29BFBE4F46B7EE100EFE048209340AD2E7FC2C4C22E27E036ACD748`
- `%TEMP%\NaeilERPUpdater_v5_verify`에 silent 설치 검증 통과.
  - `NaeilERPUpdater.exe`, `UpdateHelper.exe`, `config.json`, `chrome_debug.bat` 설치 확인.
  - 설치된 `NaeilERPUpdater.exe`가 5초 이상 실행 상태로 유지되는지 확인 후 종료.

## v4.0.2 TOPAS 항공사-only 조회 파싱 버그 수정 (2026-06-11)

- 증상: TOPAS 화면에는 `AN18SEPICNCNX/AZE -AC-`, `AN19SEPICNCNX/AZE -AC-` 등 AC1 응답이 출력됐지만, 프로그램은 `TOPAS 묶음 응답 대기 120초 초과: 10개 중 0개만 확인`으로 중단.
- 원인: `NO FLIGHT` 날짜 때문이 아니라, TOPAS 요청 라인이 `/AZE`처럼 편명 없이 항공사 코드만 있는 형식일 때 파서의 `REQUEST_RE`가 응답 블록 시작으로 인식하지 못함. 기존 파서는 `/AZE517`, `/AZE0917`처럼 항공사+편명을 기대.
- 수정: 요청 정규식을 편명 선택(optional)으로 완화해 `/AZE`와 `/AZE517` 모두 응답 블록으로 인식.
- 조정: TOPAS 묶음 응답 최대 대기 시간을 120초에서 80초로 단축.
- 회귀 테스트: CNX 샘플 기준으로 `NO FLIGHT`와 정상 운항이 섞인 `/AZE` AC1 응답을 추가. 2026-09-18 비운항/2026-09-19 ZE517 운항 블록을 모두 파싱 확인.
- 검증: `python -m py_compile topas\availability.py gui.py`, `python -m unittest tests.test_topas_availability` 통과.

## v4.0.1 TOPAS 입력 안정화 버그 수정 (2026-06-11)

- 증상: TOPAS 조회 중 Selenium `element not interactable` 오류가 팝업으로 노출되며 AC1 연속 조회가 중단됨.
- 원인: TOPAS Entry 화면 갱신/로딩 타이밍에 같은 selector의 숨김 또는 비활성 프롬프트가 캐시에 남거나 먼저 선택될 수 있었음.
- 수정: AC1 전송 전 프롬프트가 실제 입력 가능한 상태인지(`displayed/enabled/크기/visibility/pointer-events/readOnly`) 확인하고, 불가능하면 캐시를 버린 뒤 다시 탐색.
- 보강: 전송 전 scroll/focus/value 초기화/input 이벤트를 수행하고, 일반 `send_keys` 실패 시 `ActionChains` 클릭+키 입력으로 재시도.
- 보고 개선: 최종 실패 시 긴 Selenium stacktrace 대신 TOPAS 화면 로딩/포커스 상태를 확인하라는 한국어 안내 메시지를 표시.
- 검증: `python -m py_compile gui.py`, `python -m unittest tests.test_topas_availability` 통과.

## v4.0 TOPAS 좌석조회 정식 반영 (2026-06-10)

- 기존 안정판 보호를 위해 원본 `erp_updater` 작업트리는 수정하지 않고, 별도 worktree `erp_updater_v4_topas_lab` / 브랜치 `feature/v4-topas-seat-checker`에서 검증 후 정식 반영.
- 신규 기능은 ERP 행사요금 저장 루프와 분리된 `topas/` 패키지로 시작.
- 1차 추가 범위: 날짜별 `AN...` 명령 생성, SellConnect availability 텍스트 파서, 응답 완료 대기 정책, 로컬 CLI(`topas_cli.py`), 단위 테스트.
- 핵심 방향: 첫날짜는 `AN...`으로 조회하고 이후는 다음날 조회 엔트리 `AC1`을 사용하되, 매크로 빠른 복붙 대신 `명령 1건 전송 -> 프롬프트 복귀/로딩 종료 감지 -> 다음 명령 전송` 구조.
- 2026-06-10 실제 디버깅 크롬에서 `AC1` 1건 전송 테스트 성공: 2026-07-01 결과 상태에서 `AC1` 입력 후 `AN02JULICNGUM/ALJ0915 -AC-` 응답 파싱 확인.
- GUI v4 초안 반영: `요금수정` / `토파스조회` 탭 분리, 기존 버튼명 `요금불러오기` / `요금직접입력하기`로 변경, 공용 `브라우저 켜기` 버튼이 선택 탭에 따라 ERP 또는 TOPAS 로그인 URL을 열도록 수정.
- 탭 선택 UI 개선: 기본 `ttk.Notebook` 탭 대신 커스텀 세그먼트 탭바를 사용해 선택 상태를 하단 포인트 라인과 차분한 배경으로 표시.
- TOPAS 조회 흐름 반영: 사용자가 첫 조회를 직접 완료한 뒤 `토파스 조회하기`에서 AC1 횟수를 입력하면 현재 원문 + 반복 조회 원문을 누적하고, 완료 후 전체 원문을 복사 가능한 팝업으로 표시.
- AC1 응답 완료 판정 보강: `AC1`/`>` echo만 출력된 중간 상태를 결과로 인정하지 않고, 새 `AN...` 응답 블록이 출력되어 안정화된 뒤 다음 AC1을 전송하도록 수정.
- TOPAS 속도 최적화: Entry shell/prompt 요소 캐시, 최근 화면 tail 파싱, 안정화 대기 기본값 단축, AC1 묶음 전송은 live test 결과 기준으로 10건 고정.
- 묶음 전송 대기 보강: TOPAS가 요청 묶음 전체를 모두 응답해야 다음 묶음을 전송하며, 최대 80초 동안 `N개 중 M개` 상태를 추적한 뒤 미완료 시 오류로 중단.
- 2026-06-10 live test: 시작 엔트리 `AN20JULDADICN/AZE594`, 30회 조회 기준 묶음 10은 15.286초(평균 0.510초/AC1), 묶음 30은 15.445초(평균 0.515초/AC1)로 큰 차이 없음. 묶음 30도 `AN21JUL~AN19AUG` 30개 전체 응답 확인 후 완료.
- 상세 설계 문서: `docs/v4_topas_availability_workflow.md`.

## 목표

내일투어 ERP 시스템(erp.naeiltour.co.kr)에서 항공 요금을 엑셀 기반으로 자동 입력·저장하고, TOPAS 출발편/귀국편 원문을 기반으로 박수별 왕복요금을 계산해 ERP 입력표로 넘기는 RPA 도구.
Selenium + Python으로 디버깅 모드 크롬에 붙어서, 날짜별 ERP 조회 → 전체 선택 → 요금 모달 오픈 → 값 주입 → 저장을 반복하고, 별도 TOPAS 요금조회 탭에서 AC1 원문 수집·운임/시즌 계산을 수행한다.

## 현재 상태

**v5.0.3 정식 배포 버전 (2026-06-18). v5 흐름은 유지하고, `전체 지우기` 시 항공사코드 선택값을 `_선택_`으로 초기화하도록 보강. 호텔명 필드 추가안은 제외.**

- **입력 모드 2가지**: 단일 날짜(8열) / 기간 입력(시작일·종료일 분리, 9열). 표 위 체크박스로 전환, 엑셀 불러오면 자동 인식.
- **날짜 필터**: 전체 / 특정일 이후 / 특정 날짜 지정 / 기간 범위 지정 (기간 모드는 겹침 기준).
- **요금 입력표**: 항공사코드 선택, 실행취소/다시실행, 요금구간 병합(빈 날짜 분리), 엑셀 다운로드 지원.
- **대상 ERP 화면**: 해외상품 → **행사요금일괄수정** (출발일자 검색 → 항공사코드 조건 → 전체 선택 → 요금 입력 → 저장 반복).
- **TOPAS 요금조회**: 사용자가 출발편/귀국편 첫 날짜를 직접 조회한 뒤 `AC1`을 10건씩 묶어 자동 반복. 출발편 원문과 귀국편 원문을 슬롯에 보관하고, 운임/시즌 데이터와 항공노선·박수 기준으로 왕복요금 계산.
- **TOPAS 계산 결과**: 계산 과정 보기, 엑셀 다운로드, TSV 복사, `요금수정 보내기` 지원. 마감 행은 ERP 입력표에서 제외하고 출발일/귀국일/박수 목록을 팝업으로 확인·복사 가능.
- **배포**: GitHub 릴리스 + 자동 업데이트(`latest.json`) 활성. 매뉴얼은 `사용가이드.html`(GitHub Pages) + `Wiki/Entities/Naeil-ERP-Updater-사용-매뉴얼.md`.
- v3.0.0 상세 변경 내역은 아래 과거 이력 참고. (이전 인수인계 문서 `작업일지_v3.md`는 통합되어 제거됨 — 필요 시 git 이력에서 복구 가능.)

## 프로젝트 구조

```
erp_updater/
├── gui.py              ← GUI 앱 (tkinter 다크 모드). 실사용 진입점
├── excel_loader.py     ← 엑셀 파싱 + 유효성 검사. GUI/CLI 공용
├── config.json         ← 셀렉터, 포트, dry_run 등 전체 설정
├── chrome_debug.bat    ← 디버깅 크롬 기동 (포트 9222, ERP/TOPAS URL 인자 지원)
├── topas/              ← TOPAS availability 파서/대기 정책
├── fare/               ← TOPAS 원문 파싱 후 운임/시즌 기반 왕복요금 계산·내보내기
├── topas_cli.py        ← TOPAS 파서/조회 정책 로컬 실험용 CLI
├── docs/               ← v4 TOPAS 조회 설계 문서, v5 기획서
├── tests/              ← TOPAS/Fare/입력표 병합 회귀 테스트
├── build_setup.ps1     ← Windows EXE + SETUP 파일 생성 스크립트
├── packaging/          ← 설치용 IExpress payload 스크립트
├── requirements.txt    ← selenium, pandas, python-dotenv, webdriver-manager
├── .venv/              ← Python 가상환경
├── ChromeProfile/      ← 디버깅 크롬 전용 프로필 (세션 유지용)
├── logs/
│   ├── update_history.csv   ← 실행 이력 (날짜, 요금, 상태, 에러)
│   └── screenshots/         ← 저장 전후 증적 캡처
└── cache/              ← 운임/시즌 스냅샷 캐시
```

## 핵심 작동 흐름

1. `chrome_debug.bat` 또는 GUI의 `브라우저 켜기` → 선택 탭에 맞는 ERP/TOPAS 디버깅 크롬을 실행
2. 사용자가 ERP 또는 TOPAS에 수동 로그인 → 필요한 화면으로 이동
3. GUI 실행 후 엑셀 파일 선택 또는 TOPAS 요금조회 결과를 `요금수정 보내기`로 입력표에 전달
4. GUI에서 날짜 필터와 항공사코드를 확인한 뒤 "요금수정 시작" 클릭
5. 날짜마다: 시작일/종료일 주입 → 조회 → 전체 선택 체크박스 → 요금업데이트 모달 → 항공비·알선수익 입력 → 이중 검증 → 저장 → 얼럿 수락

TOPAS 요금조회는 별도 탭에서 진행한다. 사용자가 TOPAS Entry 화면에서 출발편/귀국편 첫 날짜를 각각 직접 조회한 뒤, GUI의 `AC1 자동반복`으로 원문을 슬롯에 모은다. 운임/시즌 데이터가 준비되면 항공노선과 박수를 선택해 `요금 조회`를 실행하고, 결과를 엑셀/복사/ERP 입력표 전달로 활용한다.

## config.json 셀렉터 매핑

| 키 | 값 | 대상 |
|---|---|---|
| `search_date_input` | `#searchStDate` | 출발 시작일 입력 |
| `search_date_end_input` | `#searchEnDate` | 출발 종료일 입력 |
| `search_button` | `a:has(#gridMain_r), #gridMain_r` | 조회 버튼 |
| `header_all_checkbox` | `td.aui-grid-row-check-header input` | 전체 선택 체크박스 |
| `update_button` | `#priceUpdate` | 요금업데이트 버튼 |
| `adult_fare_input` | `#addAir01` | 항공비 입력 필드 |
| `child_fare_input` | `#addProfit41` | 알선수익 입력 필드 |
| `save_button` | `#priceSave` | 저장 버튼 |
| `cancel_button` | `#popCloseBtn` | 취소/닫기 버튼 |

## GUI 버튼 구성 (최종)

| 버튼 | 기능 |
|------|------|
| 브라우저 켜기 | 선택 탭에 따라 chrome_debug.bat 실행 (요금수정=ERP 로그인, 토파스조회=TOPAS 로그인) |
| 찾기... | 엑셀 파일 선택 대화상자 |
| 요금수정 시작 | RPA 루프 시작 (백그라운드 스레드) |
| 일시 중지 / 다시 시작 | 현재 행 처리 완료 후 대기/속행 토글 |
| 중지 | 안전 중단 (확인 팝업 후 플래그 설정) |
| 토파스 조회하기 | TOPAS 첫 조회 이후 AC1 반복 조회 실행 |

## 엑셀 포맷 (v3.0.0)

- **단일 날짜 양식(8열)**: 날짜 / 항공비 / 호텔비 / 지상비 / 여행경비 / 알선수익 / 소아요금 / 유아요금
- **기간 입력 양식(9열)**: 시작일 / 종료일 / (이하 위 7개 요금 열 동일)
- 날짜: `YYYY-MM-DD` / `YYYYMMDD` / Timestamp 허용. 요금: 정수(콤마 허용).
- 불러올 때 단일/기간 형식은 1·2열 날짜 여부로 자동 인식(`excel_loader._is_date_like`). 구버전 3열(날짜·항공비·알선수익)도 호환(나머지 0 처리).

## 주의사항

- 크롬은 반드시 **모든 일반 크롬 창을 닫은 뒤** 디버깅 모드로 기동해야 한다. 안 그러면 디버깅 포트에 접근 불가.
- GUI(`gui.py`)와 CLI(`main.py`)는 로직이 거의 동일하지만 **별도 유지보수**가 필요한 코드 이중화 상태다. 한쪽을 고치면 다른 쪽도 동기화해야 한다.
- `gui.py`의 `rpa_worker_loop`는 daemon 스레드에서 돌아가므로, tkinter 위젯 조작은 `self.root.after(0, ...)` 를 통해서만 해야 한다. 현재 `status_lbl.config(...)` 등 일부는 직접 호출하고 있어서, 드물게 스레드 안전성 이슈가 생길 수 있다.
- ERP DOM 구조가 업데이트되면 config.json의 셀렉터를 다시 확인해야 한다.
- 배포용 인스톨러 생성 시, 한글 사본 파일명(예: `ERP 항공요금수정_배포용_v1.x.x.exe`)은 생성하지 않고, 오직 **영문 인스톨러 파일(`NaeilERPUpdater_Setup_v1.x.x.exe`)만 단독 생성 및 관리**한다. (사용자 요청 사항, 2026-05-29 15:09)

## 다음에 할 수 있는 개선 작업

- [ ] `gui.py`와 `main.py`의 로직 이중화 해소 (공통 모듈 추출)
- [ ] tkinter 위젯 조작의 스레드 안전성 전면 정비 (`root.after` 통일)
- [ ] 엑셀 1행 헤더 자동 감지 또는 명시적 스킵 옵션
- [x] 이미 성공한 날짜 자동 스킵 기능 (현재 GUI 쪽은 history 연동 없이 전부 실행)
- [x] 실행 결과 요약 리포트 (성공/실패 날짜 목록 자동 생성)
- [ ] 다중 시트 또는 다중 엑셀 연속 처리

## 최근 변경 — v3.0.3 (2026-06-02)

v3.0.2로 실제 멀티페이지(2,779건/6페이지) 실행 중 5페이지에서 **서버 408(응답 지연) 얼럿**이 떠 `unexpected alert open` 예외 → 해당 날짜 전체가 FAIL 처리(1~4페이지는 이미 저장됨). 일시적 서버 과부하성 오류 + 부분 성공이 전체 실패로 보고되는 문제.

### 페이지 저장 견고화 (gui.py rpa_worker_loop)
- **페이지 처리 메서드 분리**: `_save_current_page`(전체선택→모달→주입→검증→저장→얼럿→모달닫기→그리드대기), `_dismiss_alert_and_modal`(재시도 전 얼럿·모달 정리).
- **페이지별 재시도**: 각 페이지를 `page_max_retries`(기본 3)회까지 재시도. 실패 시 얼럿/모달 정리 후 해당 페이지로 재이동(`navigate_to_grid_page`)하고 점증 백오프(`page_pause+attempt`) 대기.
- **오류성 얼럿 감지**: 저장 후 얼럿 텍스트에 `408/지연/연결이 원활/관리자에게 문의/오류가/실패`가 있으면 '저장 미반영 가능'으로 보고 예외 → 재시도 유도(서버 지연 시 저장 누락 방지).
- **부분 실패 정직 보고**: 페이지별 성공/실패를 추적해, 일부 페이지 실패 시 `status=FAIL` + "실패 페이지: N, M / 성공 K페이지, 재실행 권장" 메시지. (구버전은 전체 FAIL로만 보고)
- **페이지 간 텀**: `erp_page_pause`(기본 1.0s)로 연속 저장 사이에 간격 → 서버 408 과부하 완화.

### config/버전
- config.json: `page_max_retries`(3), `erp_page_pause`(1.0) 추가.
- v3.0.2 → **v3.0.3**: gui.py, build_setup.ps1, setup_installer.py. `py_compile`·config JSON 유효.

## 최근 변경 — v3.0.2 (2026-06-02)

v3.0.1 멀티페이지 실사용 테스트(17일 처리) 중 `2026-06-11` 1건이 "조회결과 없음"으로 실패 → 실측 진단으로 **페이징과 무관한 기존 조회-매칭 버그**를 발견·수정.

### 조회 매칭 false negative 수정 (gui.py rpa_worker_loop)
- **증상**: 06-11은 실제 13건 존재(검색·로드 정상, 알림창 없음)인데 "조회결과 없음"으로 건너뜀 → 13건 누락.
- **원인**: 기존 매칭은 `.aui-grid-default-column` DOM에서 날짜 형태 텍스트를 모두 긁어 "전부 검색범위 안"인지 검사. 06-11 행사엔 출발일 외 **부가 날짜(06-08)** 컬럼이 섞여 단일일 범위를 벗어나 영원히 매칭 실패. 16건이 성공한 건 부가 날짜까지 우연히 범위 안이었기 때문. v3.0.0부터 잠복.
- **수정**: 매칭을 `AUIGrid.getGridData('#gridMain')[].startDay`(데이터 모델, 출발일)만 보도록 교체. 부가 날짜 영향 제거, 행 바인딩 지연은 폴링으로 흡수. CDP 시뮬레이션으로 구=실패/신=06-11 13건 매칭 성공 확인.

### 실패 알림 팝업 추가 (generate_and_show_report)
- 콘솔 요약으로만 끝내지 않고, 실패/스킵이 1건이라도 있으면 작업 종료 후 `messagebox.showwarning`으로 실패 날짜·사유 목록(최대 20건+초과 N건)을 띄워 사용자에게 분명히 안내. 전건 성공 시 완료 `showinfo`.

### 버전/검증/배포
- v3.0.1 → **v3.0.2**: gui.py, build_setup.ps1 `$Version`, packaging/setup_installer.py `APP_VERSION`. `py_compile` 통과.
- 빌드 → `publish_release.ps1 -SkipBuild`로 GitHub 릴리스 `v3.0.2` 생성/업로드 + latest.json 동기 + push 완료.
  - 릴리스: https://github.com/KAKNAKIAK/naeil-erp-updater/releases/tag/v3.0.2
  - `NaeilERPUpdater_Setup_v3.0.2.exe` SHA256: `F24E15A5854B92E8D3705004439016649EBEF1B0F4A1A393571466F769169E03` (재다운로드 대조 일치, main latest.json 동기 확인).
  - 커밋 `17be941`(fix/feat)·`abb4579`(release) → origin/main 푸시 완료.
- latest.json notes를 v3.0.2(조회 누락 수정·실패 팝업·페이징) 기준으로 교체.
- **후속 필요**: 2026-06-11 13건은 v3.0.1로 실행 시 누락된 상태 → v3.0.2로 06-11 재실행해 채워야 함.

## 최근 변경 — v3.0.1 (2026-06-02)

500건 초과 조회 시 페이징을 순회하며 누락 없이 저장하는 기능을 추가했다. 작업 전 디버깅 크롬(포트 9222)에 직접 붙어 ERP 그리드를 계측해 초안 지시서의 가정을 검증·수정했다.

### 계측으로 확인된 사실 (출발일 2025-10-01~10-31 → 3,536건/8페이지)
- 그리드 iframe `#framego01_104_list`, AUI Grid id `#gridMain`, 페이지당 500행 고정.
- 행 데이터에 `totRows`(전체 건수) 즉시 포함. 고유키 후보 `basePriceSeq/eventSeq/eventCd/goodSeq`.
- `moveToPage(N,'this','#gridMain_r')`로 현재 위치와 무관하게 N페이지 직접 점프 가능(1→5 실측).
- **`totalPage` 변수는 행 바인딩보다 5~8초 늦게 채워짐** → grid-ready 직후 읽으면 0 → 조기 종료 → 2페이지 이후 통째 누락 위험. `usePaging`·`gridData` 전역은 없음(초안 표현 부정확).

### 코드 변경 (gui.py)
- `get_grid_page_state()` 추가: `totalPage` 대신 `AUIGrid.getGridData('#gridMain')[0].totRows`로 `total_pages = ceil(totRows/500)` 계산(지연 버그 차단). next 버튼 노출도 보조 신호로 반환.
- `navigate_to_grid_page()` 추가: `moveToPage` 직접 점프 후 `currentPage==target` 검증(최대 3회), 실패 시 RuntimeError로 해당 기간 중단(누락 은폐 금지).
- `rpa_worker_loop`: 기존 "전체선택→모달→입력→이중검증→저장→모달닫기→grid-ready" 블록을 **로직 변경 없이** 페이지 `while` 루프로 감쌈. 2페이지 이후만 진입 시 이동. 사용자 중지 시 부분 처리=실패 기록.
- `APP_VERSION` v3.0.0 → **v3.0.1**.

### config.json
- `grid_id`(`#gridMain`), `grid_page_size`(500), `paging_search_button`(`#gridMain_r`) 추가.

### 검증 (CDP 직접 계측, 2026-06-02)
- `py_compile gui.py` 통과, `config.json` JSON 유효.
- **Test A 페이징 메커니즘(저장 0회, 무손상)**: 3,536건/8페이지에서 `ceil(totRows/500)=8` 정확, 8페이지 전부 `moveToPage('this','#gridMain_r')` 점프·전체선택·모달 진입 성공(행수 500×7+36). **5페이지 점프가 1회 간헐 실패(→1페이지로 떨어짐)** 재현 → `currentPage` 검증+3회 재시도로 복구(반복 5/5) → 검증 로직이 누락을 실제로 차단함을 입증.
- **Test B 멤버십(무손상 단일행 동일값 저장)**: `basePriceSeq=34635700` 1행만 현재값 그대로 재저장(성분합=adultPrice 게이트). 결과 `totRows` 3536 불변, 저장행 결과셋 잔존(index 0), `adultPrice` 642192 변동 없음 → **멤버십 변동 없음, 인덱스 순회 안전**. 데이터 무손상 확인.
- 검증 중 사이드노트: 저장 시 ERP가 네이티브 `confirm`을 띄워 CDP가 막힐 수 있음(실사용 GUI는 Selenium `switch_to.alert`로 처리하므로 무관). 계측은 `window.confirm/alert` 가로채기로 우회.

### 배포 (2026-06-02 완료)
- 버전 동기화 v3.0.1: gui.py, build_setup.ps1 `$Version`, packaging/setup_installer.py `APP_VERSION`.
- `build_setup.ps1`로 설치본 빌드 → `publish_release.ps1 -SkipBuild`로 GitHub 릴리스 `v3.0.1` 생성/업로드 + latest.json 동기화(업로드 자산 해시 기준) + 커밋/푸시.
  - 릴리스: https://github.com/KAKNAKIAK/naeil-erp-updater/releases/tag/v3.0.1
  - 설치본 `NaeilERPUpdater_Setup_v3.0.1.exe` SHA256: `B0099144EAF88B0E073E34D9A601E6AB5FF116FAA8140BE6248ECE7419933D0F` (로컬=업로드 자산 재다운로드 대조 일치, main latest.json 동기 확인).
  - 소스 커밋 `c3c83a4`(feat 페이징), 매니페스트 커밋 `e3450c8`(release) → origin/main 푸시 완료.
- latest.json notes를 v3.0.1(페이징/누락 방지) 기준으로 교체.

### 남은 작업
- [ ] (선택) 실제 엑셀로 GUI 멀티페이지 1회 구동해 콘솔 `[페이지 k/N]` 로그 육안 확인

## 최근 변경 — v3.0.0 (2026-06-02)

기간 입력 모드를 완성하고 v3.0.0을 정식 배포했다. (이전 버전 v1.x~v2.0.4의 상세 이력은 [작업일지_archive.md](legacy_archive_20260618/legacy_gui_and_docs/작업일지_archive.md) 참고.)

### 코드 변경 (gui.py / excel_loader.py)
- **수식 모듈 동적화**: `_name_to_col`을 인스턴스 메서드로 전환하고 기간 모드 offset(+1) 적용 → 기간 모드(9열)에서 `=항공비+호텔비` 등 이름 참조가 한 칸씩 밀리던 버그 수정. `_ref_name_for_col`은 실시간 헤더(`self.sheet.headers()`) 기반으로 교체. `_eval_cell` 리졸버·`_commit_formula_bar`의 날짜열 판정을 `self.is_date_col()`로 통일. `_apply_formula_to_column` 상태 메시지를 헤더명 기반으로 교체.
- **엑셀 양식 다운로드 선택창**: `download_excel_template`에 라디오 선택창(`_ask_template_format`) 추가 — `단일 날짜 양식` / `기간 입력 양식` + 다운받기/취소. 현재 모드 기본 선택, Enter/Esc 단축키.
- **기간 엑셀 자동 감지 개선**: 헤더 키워드/9열 조건 대신 1·2번째 열의 날짜 비율을 열 개수와 무관하게 검사(신규 `_is_date_like`, 연/월/일 범위 확인으로 8자리 요금 오인 방지). 헤더 없는 7열 기간 파일도 정상 감지.
- **날짜 필터 3종 기간 대응**: `_apply_date_filter`를 종료일까지 보는 겹침 기준으로 변경(이후=`date_end>=기준`, 특정=`date<=지정<=date_end`, 범위=`date<=e and date_end>=s`). 단일 모드는 `date_end==date`라 동작 동일.
- **RPA 종료일 입력 견고화**: ERP 날짜위젯이 시작일 변경 시 종료일을 자동 리셋해 기간 종료일이 누락되던 문제 수정. 시작일 입력 후 change/blur 선발생 → 종료일 후입력 + 표시값 재확인하며 최대 4회 재시도 → 불일치 시 경고 로그(`rpa_worker_loop`). 실기동 1행 스모크 테스트로 종료일 정상 반영 확인.
- 결정 근거(사용자 확정): 실무 기간 파일은 9열 정석 유지(열 매핑 불변), 날짜 필터는 "겹치기만 하면 포함".

### 버전/빌드/배포
- 버전 상향 v2.0.4 → **v3.0.0**: `gui.py`, `build_setup.ps1`(`$Version`), `packaging/setup_installer.py`(`APP_VERSION`).
- `latest.json` notes를 v3.0.0 기능(기간 모드/양식 선택/필터/종료일 안정화) 기준으로 교체.
- `publish_release.ps1`로 재빌드 + GitHub 릴리스 `v3.0.0` 생성/업로드 + latest.json 해시 동기화 + 커밋/푸시 완료(exit 0).
  - 릴리스: https://github.com/KAKNAKIAK/naeil-erp-updater/releases/tag/v3.0.0
  - 설치본 `NaeilERPUpdater_Setup_v3.0.0.exe`(약 64.5MB) SHA256: `732871B4613FD7C1C766FB62F110798EB397554B7FBAAADC25F2B3C6EA2EE118` (로컬=업로드 자산 재다운로드 대조 일치, main latest.json 동기 확인).
  - 소스 커밋 `9338c43`, 매니페스트 동기 커밋 `565a077` → origin/main 푸시 완료.
- 검증: `py_compile`(gui.py / excel_loader.py / setup_installer.py) 통과. 기간 감지(`괌 대한항공 테스트.xlsx` → is_period=True, 시작/종료일 정렬) 및 단일 파일 회귀 없음 확인. 필터 겹침 로직 단위 확인.

### 매뉴얼 최신화
- `사용가이드.html`(GitHub Pages): 버전 v3.0.0, 양식 다운로드 설명을 단일/기간 선택창 기준으로 갱신, 기능 카드별 예시 이미지 배치 + 실제 버튼명 통일, 대상 ERP 화면(`행사요금일괄수정`) 명시, 날짜 필터에 `기간 범위 지정`·겹침 설명 추가, 기술 용어(포트/디버깅/세션 등) 제거.
- `Wiki/Entities/Naeil-ERP-Updater-사용-매뉴얼.md`(vault 사본): 동일 내용 반영.
- 스크린샷 5종 재캡처 완료(`assets/`): 양식 선택창(dialog_template)·단일 8열(gui_single_v3)·기간 9열(gui_period_v3)·기간 범위 필터(filter_range_v3).
